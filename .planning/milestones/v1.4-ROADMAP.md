# v1.4 — M5 (Construction-phase skills: debugging · QA · security depth) — ARCHIVED

Completed & verified 2026-06-09. 3 phases (17-19), 3 plans, 5/5 requirements. `diagnose` + `qa` skills + `security-controls.md` reference + `security-design` enrichment + per-agent invocation docs. No engine contract change.

## Milestone v1.4 — M5 (Construction-phase skills: debugging · QA · security depth)

**Goal:** Give the build phase three quality skills flagged in live M4 dogfooding — a stack-agnostic debugging loop (`diagnose`), a relentless repo-wide correctness sweep (`qa`), and concrete security-control coverage (`security-design` enrichment) — keeping the thin-orchestrator discipline and the 5-skill auto-budget. Hand-authored skill phases (the M4 pattern); no/minimal engine work.

**Build order:** `diagnose` (Phase 17) → `qa` (Phase 18) → `security-design` controls enrichment + per-agent invocation docs (Phase 19). M5-CC (thin-orchestrator shape · `disable-model-invocation` on new skills · doctor auto-budget held at 5 · `validate skills` clean) is cross-cutting across all three.

### Phases

- [x] **Phase 17: `diagnose` skill** - Stack-agnostic debugging loop (reproduce → isolate → hypothesis → fix → verify) over the engine + `.sovereign/` state; composes with `tdd`/`verify-self`/`sentinel` (DIAG-01, M5-CC) ✅ 2026-06-09
- [x] **Phase 18: `qa` skill** - Relentless repo-wide correctness sweep (static correctness · tests · dep/wiring/import integrity · routing · cross-workspace consistency · API contract) over the project's own toolchain; ✅/❌/⚠️ report with `file:line` (QA-01, M5-CC) ✅ 2026-06-09
- [x] **Phase 19: `security-design` controls enrichment + docs** - Agnostic `security-controls.md` reference (input validation · injection · authz/IDOR · secrets · rate-limiting) the skill consults + per-agent skill-invocation docs note (SEC-01, DOCS-01, M5-CC) ✅ 2026-06-09

### Phase Details

### Phase 17: `diagnose` skill
**Goal**: A user can run `diagnose` to debug a failure through a disciplined, recommendation-first loop instead of guessing.
**Requirements**: DIAG-01, M5-CC
**Success Criteria** (what must be TRUE):
  1. Running `diagnose` walks reproduce → isolate → hypothesis → fix → verify, recommendation-first, using the project's OWN test/run tooling (stack-agnostic — no hardcoded toolchain).
  2. It orients with a single `sovereign-tools init diagnose` call and composes with the shipped skills (failing test via `tdd`; unconfirmed root cause → `verify-self` marker; standards pass → `sentinel`).
  3. Core-tier thin-orchestrator shape; `disable-model-invocation: true` (doctor auto-budget held at 5); `validate skills` passes.
**Plans**: 1 (17-01) — ✅ Complete 2026-06-09 (verified 3/3; doctor 19/5/14, 164 engine tests green)

### Phase 18: `qa` skill
**Goal**: A user can run `qa` to catch errors, type mismatches, broken imports/wiring, and contract drift across the whole repo before they hit a running build.
**Requirements**: QA-01, M5-CC
**Success Criteria** (what must be TRUE):
  1. `qa` sweeps each workspace/module using its OWN toolchain across: static correctness, tests, dependency & wiring integrity (DI/wiring, import resolution, version alignment, example-config↔code), navigation/routing, and cross-workspace consistency (single shared-runtime version, shared/contract types, schema↔types, API contract vs `.sovereign/docs/api/API_SPEC.md`).
  2. Reports ✅/❌/⚠️ grouped by module then category, ❌ with exact error + `file:line`, ending in a one-line verdict; delegates to the project's own `qa` command when present, else per-module equivalents — stack-agnostic.
  3. Core-tier thin-orchestrator shape; `disable-model-invocation: true` (doctor auto-budget held at 5); `validate skills` passes; composes with `diagnose` + complements `sentinel`.
**Plans**: 1 (18-01) — ✅ Complete 2026-06-09 (verified 3/3; doctor 20/5/15, 164 engine tests green)

### Phase 19: `security-design` controls enrichment + docs
**Goal**: `security-design` drives concrete security-control coverage, and non-Claude users know how to invoke SOVEREIGN's skills.
**Requirements**: SEC-01, DOCS-01, M5-CC
**Success Criteria** (what must be TRUE):
  1. A new agnostic `engine/references/security-controls.md` enumerates control *classes* (input validation, injection, authz/IDOR, secrets, rate-limiting) + what-to-verify per class — NOT framework APIs; `security-design` consults it and offers the checklist, surfacing uncovered classes on re-run.
  2. Per-agent skill-invocation differences (Claude `/`-autocomplete vs other agents read SKILL.md / invoke by name) are documented in the README + `npx sovereign-cli init` output.
  3. `security-design` keeps the thin-orchestrator shape + `disable-model-invocation: true`; `validate skills` passes; doctor auto-budget held at 5.
**Plans**: 1 (19-01) — ✅ Complete 2026-06-09 (verified 3/3; doctor 20/5/15, 164 engine tests green)

### M5 Progress (final)

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 17. `diagnose` skill | 1/1 | ✅ Complete | 2026-06-09 |
| 18. `qa` skill | 1/1 | ✅ Complete | 2026-06-09 |
| 19. `security-design` enrichment + docs | 1/1 | ✅ Complete | 2026-06-09 |

### Milestone Summary

- **Delivered:** the construction/quality skills — `/diagnose` (disciplined reproduce→isolate→hypothesis→fix→verify loop, composing with tdd/verify-self/sentinel), `/qa` (stack-agnostic repo-wide correctness sweep over the project's own toolchain: static correctness, tests, dependency/wiring integrity, routing, cross-workspace consistency incl. API_SPEC.md; ✅/❌/⚠️ report with file:line), and a `security-controls.md` reference + `security-design` enrichment driving concrete control-class coverage. Plus the per-agent skill-invocation docs (DOCS-01).
- **Decisions:** skills drive the project's OWN toolchain (no bundled runner/linter/type-checker — engine stays zero-dep); everything agnostic (control *classes*, check *categories* — never library names); `qa` complements `sentinel` (mechanical correctness vs standards) and feeds `diagnose`. No new ADR.
- **Stats:** 20 skills total (was 18; +`diagnose` +`qa`), +1 reference (`security-controls.md`), no engine lib change, 164 engine tests (unchanged — no engine contract change), deps stay `{}`. Auto-trigger budget held at 5.
- **Issues/tech debt:** none functional. Deferred to M6+: a separate `security-review` skill (audit existing code against `security-controls.md`); other v1 §6 construction skills (`vertical-slice`, `zoom-out`, `improve-architecture`, `code-patterns`); a pre-flight deploy-gate that BLOCKS on failing `qa` / stale anchors / unresolved markers (M4/M5 surface only). Tooling note: GSD `roadmap`-mutating commands truncate this repo's hand-authored ROADMAP.md — phase/milestone completion marked manually throughout.
