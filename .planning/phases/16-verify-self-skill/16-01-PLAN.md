---
phase: 16-verify-self-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/skills/verify-self/SKILL.md
autonomous: true
requirements: [VERIFY-01, VERIFY-02, ANCHOR-02, M4-CC]

must_haves:
  truths:
    - "Triggering verify-self on a low-confidence signal hard-stops and runs a retroactive audit of code since the last verified anchor, surfacing each unverified claim as file:line + what's uncertain (SC1 / VERIFY-01)."
    - "The skill presents exactly three choices — (A) hand off to anchor-docs, (B) mark SOVEREIGN:UNVERIFIED and continue, (C) discard+restart — and on B emits the exact marker form sentinel scans (SC2 / VERIFY-02)."
    - "The footer closes the anchor->verify->sentinel loop: it names /anchor-docs (choice A) and notes /sentinel scans the SOVEREIGN:UNVERIFIED markers choice B writes (SC3 / ANCHOR-02)."
    - "The skill orients with a single `init verify-self` call, follows the core-tier thin-orchestrator shape, sets `disable-model-invocation: true` (doctor budget held at 5), and `validate skills` passes (SC4 / M4-CC)."
  artifacts:
    - path: "engine/skills/verify-self/SKILL.md"
      provides: "The hand-authored verify-self thin-orchestrator skill (frontmatter + Why/When/flow/footer)"
      min_lines: 70
      contains: "disable-model-invocation: true"
    - path: "engine/skills/verify-self/SKILL.md"
      provides: "One-call orient via the literal engine path"
      contains: "init verify-self"
    - path: "engine/skills/verify-self/SKILL.md"
      provides: "Choice B marker token in the exact spec form"
      contains: "SOVEREIGN:UNVERIFIED"
    - path: "engine/skills/verify-self/SKILL.md"
      provides: "Choice A handoff target (loop companion)"
      contains: "anchor-docs"
  key_links:
    - from: "engine/skills/verify-self/SKILL.md"
      to: "engine/skills/anchor-docs/SKILL.md"
      via: "choice A handoff (/anchor-docs) + footer"
      pattern: "anchor-docs"
    - from: "engine/skills/verify-self/SKILL.md"
      to: "engine/references/unverified-marker.md"
      via: "choice B writes the exact SOVEREIGN:UNVERIFIED marker form sentinel scans"
      pattern: "SOVEREIGN:UNVERIFIED — "
    - from: "engine/skills/verify-self/SKILL.md"
      to: ".claude/sovereign-engine/sovereign-tools.cjs init verify-self"
      via: "one-call orient with @file: spill guard, literal engine path"
      pattern: "init verify-self"
---

<objective>
Author the M4 capstone skill: `engine/skills/verify-self/SKILL.md` — a core-tier thin orchestrator that, when the agent or user hits a low-confidence signal, **hard-stops**, runs a **retroactive audit** of code written since the last verified anchor (surfacing each unverified claim as `file:line` + what's uncertain), and forces a deliberate **3-way resolution** (A anchor-docs / B mark `SOVEREIGN:UNVERIFIED` / C discard+restart) before more wrong code ships. It composes with `anchor-docs` (Phase 15) and emits the existing `SOVEREIGN:UNVERIFIED` marker form (M1 spec) that `sentinel` already scans — closing the anchor→verify→sentinel loop.

Purpose: Make the whole anti-hallucination loop *fire at the right moment*. The unique value is hard-stop discipline + the 3-way forcing function; the audit and marker-writing reuse shipped contracts.
Output: ONE file — `engine/skills/verify-self/SKILL.md`. Nothing else (no engine change, no new ADR, no change to anchor-docs/sentinel/marker form).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-verify-self-skill/16-CONTEXT.md
@.planning/phases/16-verify-self-skill/16-RESEARCH.md
@.planning/phases/16-verify-self-skill/16-VALIDATION.md
@CLAUDE.md

# The pattern to mirror beat-for-beat (composition target = choice A)
@engine/skills/anchor-docs/SKILL.md
# Canonical thin-orchestrator template
@engine/skills/bridge/SKILL.md
# What scans the markers verify-self writes (loop language to echo)
@engine/skills/sentinel/SKILL.md
# The EXACT marker form choice B emits — copy verbatim, invent nothing
@engine/references/unverified-marker.md
# Authoring contract (sections, frontmatter, line gate, footer)
@engine/references/skill-format.md

<interfaces>
<!-- Everything below is shipped/verified (16-RESEARCH.md, HIGH confidence). Use directly — no codebase exploration needed. -->

LITERAL frontmatter block (drop-in, ~340 chars description, well under the 1024 cap):
```yaml
---
name: verify-self
description: Catch your own uncertainty before it ships — when you're about to assert a version/endpoint/config you haven't confirmed, this hard-stops, audits the code you've written since your last anchored doc, and forces a deliberate choice: anchor the real docs, mark it SOVEREIGN:UNVERIFIED, or discard and restart. Use the moment you notice you're guessing about an external API.
disable-model-invocation: true
argument-hint: "[file or scope to audit]"
---
```

The ONE-CALL orient (literal path, NOT $ENGINE; copy verbatim, only the skill name differs):
```bash
INIT=$(node ".claude/sovereign-engine/sovereign-tools.cjs" init verify-self)
if [[ "$INIT" == @file:* ]]; then INIT=$(cat "${INIT#@file:}"); fi
```
`init verify-self` returns (parse these, no other orientation reads):
- `paths.unverified_marker_spec` = `references/unverified-marker.md` (the runtime path the agent reads for the marker form — package-root-relative, NOT `engine/references/...`)
- `paths.external_docs_dir` = `.sovereign/external-docs` (where anchor list/check read)
- `config.commit_docs` — delegate the commit decision to `sovereign-tools commit`; never branch on it in-body.

The "last verified anchor" BOUNDARY recipe (skill-side; NO new engine command):
```bash
# 1) most-recent anchor date = "last verified anchor" timestamp
node ".claude/sovereign-engine/sovereign-tools.cjs" anchor list
#    → JSON array; each item has .date_retrieved (the JSON key). Take the MAX.
#    If [] (no anchors) → boundary = "all un-anchored work in this session/branch";
#    treat every claim touching an external API/version/config as unverified.
# 2) bound the code to audit (uncommitted + commits since that date)
git status --short          # uncommitted/untracked work
git diff                    # unstaged hunks
git diff --staged           # staged hunks
git log --since=<date> --oneline   # commits since the last anchor
git diff <ref>..HEAD        # the committed diff to read
```

The THREE recognizable low-confidence SIGNALS to NAME in the body (VERIFY-01 trigger
recognition; reconciles with disable-model-invocation:true — the agent RECOGNIZES the
signal and runs the flow, it does not auto-fire). These are the 3 valid-context classes
from unverified-marker.md, phrased as triggers:
  1. About to assert a version / endpoint / config / signature you haven't confirmed against current docs (unverified third-party API shape).
  2. Implementing an integration with no anchor / relying on an assumed behavior (a default, an ordering guarantee, an idempotency promise) you haven't proven.
  3. Touching a library/API released or materially changed after training cutoff (stale-knowledge risk).

Choice B — the EXACT marker form (from unverified-marker.md, verbatim; sentinel greps the literal token):
```
<comment> SOVEREIGN:UNVERIFIED — <reason> | ref: <url-or-ADR> | <YYYY-MM-DD>
```
Worked examples (token is identical; comment syntax varies by language):
```js
// SOVEREIGN:UNVERIFIED — Paystack webhook signature is HMAC-SHA512? | ref: https://paystack.com/docs/webhooks | 2026-06-09
```
```python
# SOVEREIGN:UNVERIFIED — assumes the cursor is opaque base64, not an offset | ref: ADR-014
```
`reason` required; `ref` and date optional. The literal `SOVEREIGN:UNVERIFIED` (em-dash form) must appear UNMODIFIED so `grep -rn "SOVEREIGN:UNVERIFIED"` finds it.

Choice C — conservative revert (only the unverified hunks; never reset --hard / clean -fd):
```bash
git restore <path>                     # discard unstaged changes to a tracked file
git restore --staged --worktree <path> # discard staged + unstaged
git stash push -- <path>               # non-destructive: stash specific hunks
```
Then hand off to /anchor-docs before re-implementing.

The anchor-docs footer (mirror it back — this is the loop companion):
```
▶ NEXT
  • Unsure about a specific version/endpoint/config as you build? → /verify-self
  Note: anchor-docs records ground truth; it does NOT emit SOVEREIGN:UNVERIFIED
  markers — that's /verify-self, and /sentinel scans for them.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author engine/skills/verify-self/SKILL.md (hard-stop → audit → 3-way choice)</name>

  <read_first>
    @engine/skills/anchor-docs/SKILL.md (mirror its shape beat-for-beat — frontmatter → ## Why this matters → ## When to use this → ## The flow numbered → ## Navigation fenced ▶ NEXT)
    @engine/references/unverified-marker.md (copy the marker form VERBATIM for choice B — do not invent a new shape)
    @engine/references/skill-format.md (the authoring contract: real frontmatter only, no v1 fields, one-call orient, recommendation-first footer)
    The `<interfaces>` block above carries every literal value (frontmatter, orient snippet, boundary commands, marker form + examples, signals, choice C reverts, footer model).
  </read_first>

  <files>engine/skills/verify-self/SKILL.md</files>

  <action>
Create `engine/skills/verify-self/SKILL.md` mirroring `engine/skills/anchor-docs/SKILL.md` beat-for-beat. Use the EXACT literal values from the `<interfaces>` block. Structure:

**Frontmatter** — paste the literal block verbatim (name: verify-self; the ~340-char use-case-first description; `disable-model-invocation: true` on line 4; `argument-hint: "[file or scope to audit]"`). NO v1 fields (`triggers`/`works-best-with`/`min-model`/`tokens`/bare `phase`).

**## Why this matters** — plain language: your training has a cutoff; when you implement against what you *remember* you ship confident, plausible, wrong code, and it surfaces in production, not review. verify-self is the moment-of-doubt forcing function: it stops you before the guess compounds. It's the verify half of the anti-hallucination loop (anchor-docs anchors ground truth; verify-self catches uncertainty; sentinel scans the markers).

**## When to use this** — name the THREE recognizable low-confidence SIGNALS from the interfaces block (about to assert a version/endpoint/config/signature unconfirmed; integration with no anchor / assumed behavior; library/API changed after training cutoff). State plainly: the skill is user-invoked (`/verify-self`), AND the agent should RECOGNIZE these signals and run the flow itself — recognition, not auto-invocation (it sets `disable-model-invocation: true` to hold the listing budget). Add a **Don't**: don't use it as a general linter or to second-guess code you've already verified against a current anchor — it's for the moment you notice you're *guessing about external behavior*.

**## The flow** — open with the "A **thin orchestrator** — ..." intro (engine owns orient + anchor dates + git; this skill owns the hard-stop discipline + the audit judgment + the forcing function). Then numbered, recommendation-first steps:
  - **1 — HARD STOP (first, before anything else).** The literal first move: stop writing new code. State it explicitly as a hard stop — the word "hard stop" / "stop writing" must appear here, ahead of the choices.
  - **2 — Orient (one call).** Paste the orient snippet verbatim (literal `.claude/sovereign-engine/sovereign-tools.cjs init verify-self` + the `@file:` guard line). Parse `paths.unverified_marker_spec` (the marker-form path to read) + `paths.external_docs_dir`. No other orientation reads.
  - **3 — Find the last verified anchor + bound the audit.** Paste the boundary recipe: `anchor list` → max `date_retrieved` = the last-verified-anchor timestamp; then the git quartet (`git status --short`, `git diff`, `git diff --staged`, `git log --since=<date>`, `git diff <ref>..HEAD`) to read what changed since. Document the no-anchors case plainly: boundary = "all un-anchored work in this session/branch" — treat every claim touching an external API/version/config as unverified.
  - **4 — Retroactive audit: surface each unverified claim as `file:line`.** Read the bounded diff with judgment (grep for obvious risk patterns is an aid, not the method). For each claim about external behavior not backed by an anchor, surface it as **`file:line` — what's uncertain**, classified by the three valid-context classes. The literal phrase `file:line` MUST appear.
  - **5 — Present the THREE choices (never auto-resolve).** Recommendation-first, then exactly A/B/C with labels `**(A)**`, `**(B)**`, `**(C)**`:
      - **(A) Provide docs (recommended when the doc is reachable)** → hand off to **`/anchor-docs`** to anchor the authoritative source, then re-check the flagged code against the now-current doc.
      - **(B) Mark `SOVEREIGN:UNVERIFIED` and continue** → write a marker at each unverified claim's `file:line` in the EXACT form from the interfaces block: `<comment> SOVEREIGN:UNVERIFIED — <reason> | ref: <url-or-ADR> | <YYYY-MM-DD>`. Include BOTH worked examples (the `//` JS one and the `#` python one) verbatim. Note: `sentinel` scans this literal token.
      - **(C) Discard and restart with docs** → conservative git revert of ONLY the unverified hunks (`git restore <path>` / `git restore --staged --worktree <path>` / `git stash push -- <path>`; never `reset --hard`/`clean -fd` beyond the unverified changes), then hand off to `/anchor-docs` before re-implementing.
    State explicitly that the skill **never silently continues** — it always surfaces the audit and offers A/B/C. The phrase "never silently continue" (or "always surface" / "always offer") MUST appear.
  - **6 — Persist.** `node ".claude/sovereign-engine/sovereign-tools.cjs" state save`, then commit via `sovereign-tools commit` if `commit_docs` is on. Delegate — never hand-write state or branch on `commit_docs`.

**## Navigation** — a fenced `▶ NEXT` block, recommendation-first. It MUST name `/anchor-docs` (the choice A companion that anchors ground truth) AND note that `/sentinel` scans the `SOVEREIGN:UNVERIFIED` markers choice B writes — making the anchor→verify→sentinel loop legible (mirror the anchor-docs footer back). Echo the loop language: verify-self **writes** the markers; sentinel **reads, never writes** them.

Keep the body model-agnostic ("run `init verify-self`, audit, choose A/B/C"). Ensure `wc -l` ≥ 70 (the sibling skills run 64–84 lines; this one is content-rich so it will clear 70 comfortably). Use the literal engine path everywhere — never `$ENGINE`.
  </action>

  <verify>
    <automated>F=engine/skills/verify-self/SKILL.md; test -f "$F" && [ "$(wc -l < "$F")" -ge 70 ] && grep -q '^disable-model-invocation: true$' "$F" && grep -q '^## Why this matters$' "$F" && grep -q '^## When to use this$' "$F" && grep -q '▶ NEXT' "$F" && grep -q 'init verify-self' "$F" && grep -q '@file:' "$F" && grep -q '\.claude/sovereign-engine/sovereign-tools\.cjs' "$F" && ! grep -q '\$ENGINE' "$F" && ! grep -Eq '^(triggers|works-best-with|min-model|tokens|phase):' "$F" && grep -iq 'hard stop\|stop writing' "$F" && grep -q 'file:line' "$F" && grep -q 'SOVEREIGN:UNVERIFIED — ' "$F" && grep -iq 'discard' "$F" && grep -iq 'anchor-docs' "$F" && grep -iq 'sentinel' "$F" && grep -iq 'never silently\|always surface\|always offer' "$F" && echo ALL_STRUCTURAL_AND_BEHAVIOR_GATES_PASS</automated>
  </verify>

  <acceptance_criteria>
    - S1 (M4-CC): `test -f engine/skills/verify-self/SKILL.md` — file exists.
    - S2/G2 (SC4/M4-CC): `grep -q '^disable-model-invocation: true$'` — present on its own line.
    - S3/G3 (M4-CC): `grep -q '^## Why this matters$'` AND `grep -q '^## When to use this$'`.
    - S5/G3 (M4-CC): `grep -q '▶ NEXT'` — recommendation-first navigation footer.
    - S6/G4 (M4-CC): `[ "$(wc -l < "$F")" -ge 70 ]`.
    - S7/G5 (M4-CC): `! grep -Eq '^(triggers|works-best-with|min-model|tokens|phase):'` — no v1 frontmatter fields.
    - S8/G6 (M4-CC): literal `.claude/sovereign-engine/sovereign-tools.cjs` present AND `! grep -q '\$ENGINE'`.
    - S9/G7 (SC4/M4-CC): `grep -q 'init verify-self'` AND S10 `grep -q '@file:'` — one-call orient + spill guard.
    - B1/G8 (SC1/VERIFY-01): `grep -iq 'hard stop\|stop writing'` — HARD STOP stated, appearing in the flow before the choices.
    - B2/G9 (SC1/VERIFY-01): `grep -q 'file:line'` — retroactive audit surfaces file:line uncertainties.
    - B3/G10 (SC2/VERIFY-02): three choices present — `grep -iq 'discard'` (C) AND `grep -q 'SOVEREIGN:UNVERIFIED'` (B) AND `grep -iq 'anchor-docs'` (A).
    - B4/G12 (SC2/VERIFY-02): `grep -q 'SOVEREIGN:UNVERIFIED — '` — choice B writes the exact em-dash marker form (sentinel-greppable, unmodified token).
    - B5/G13 (VERIFY-02): `grep -iq 'never silently\|always surface\|always offer'` — never silently continues.
    - L1/G11 (ANCHOR-02/SC3): `grep -q 'anchor-docs'` — choice A handoff.
    - L2/G14 (ANCHOR-02/SC3): `grep -q 'SOVEREIGN:UNVERIFIED'` AND `grep -iq 'sentinel'` — loop closed (sentinel scans the markers).
  </acceptance_criteria>

  <done>SKILL.md exists, ≥70 lines, mirrors the anchor-docs shape, encodes hard-stop→audit→3-choice with the exact marker form, closes the loop in the footer, and passes every structural + behavior grep gate (S1–S10, B1–B5, L1–L2).</done>
</task>

<task type="auto">
  <name>Task 2: Run the budget/lint/regression gates from engine/ (doctor 18/5/13, validate, 164 tests)</name>

  <read_first>
    @.planning/phases/16-verify-self-skill/16-VALIDATION.md (gates G15–G17; the CRITICAL "run from engine/" caveat)
  </read_first>

  <files>engine/skills/verify-self/SKILL.md</files>

  <action>
Run the three engine gates. CRITICAL: `doctor` and `validate skills` walk `<cwd>/.claude/skills` + `<cwd>/skills`, NOT `engine/skills/` — they MUST run from `engine/` (or target the explicit `skills/verify-self/SKILL.md` path) or they pass vacuously on zero skills. No engine code is touched in this phase — these gates only confirm the new SKILL.md registers correctly and the engine suite is still green.

1. **validate (G15 / G2 in VALIDATION):**
   `cd engine && node bin/sovereign-tools.cjs validate skills skills/verify-self/SKILL.md`
   Expect `valid: true` (name `verify-self` is 11 chars, lowercase-hyphen, no reserved words; description < 1024).

2. **doctor budget (G16 / G1 in VALIDATION):**
   `cd engine && node bin/sovereign-tools.cjs doctor`
   Expect `total_skills == 18`, `auto_count == 5` (UNCHANGED — verify-self lands in disabled via `disable-model-invocation: true`), `disabled_count == 13`. If `total_skills != 18` → wrong cwd (the Pitfall 1 trap); re-run from `engine/`. If `auto_count == 6` → the frontmatter is missing `disable-model-invocation: true`; fix Task 1.

3. **engine regression (G17 / G3 in VALIDATION):**
   `cd engine && node --test "test/**/*.test.cjs"`
   Expect `pass 164, fail 0` (engine untouched).

If any gate fails, the fix is in Task 1's SKILL.md (or the wrong cwd) — not in the engine. Do NOT modify engine code, ADRs, anchor-docs, sentinel, or the marker spec.
  </action>

  <verify>
    <automated>cd engine && node bin/sovereign-tools.cjs validate skills skills/verify-self/SKILL.md && node bin/sovereign-tools.cjs doctor && node --test "test/**/*.test.cjs"</automated>
  </verify>

  <acceptance_criteria>
    - G15/G2 (SC4/M4-CC): `validate skills skills/verify-self/SKILL.md` (from engine/) → `valid: true`.
    - G16/G1 (SC4/M4-CC): `doctor` (from engine/) → `total_skills == 18` AND `auto_count == 5` AND `disabled_count == 13`.
    - G17/G3 (regression): `node --test "test/**/*.test.cjs"` (from engine/) → `pass 164, fail 0` (engine untouched).
  </acceptance_criteria>

  <done>All three gates pass from `engine/`: validate `valid:true`, doctor reports 18/5/13 (budget held at 5), engine suite still 164 green. No engine/ADR/sibling files modified.</done>
</task>

</tasks>

<verification>
Run from `engine/` (the Pitfall 1 caveat — gates walk `<cwd>/skills`, not `engine/skills/`):
- Structural + behavior + loop greps on `engine/skills/verify-self/SKILL.md` (S1–S10, B1–B5, L1–L2) all pass.
- `cd engine && node bin/sovereign-tools.cjs validate skills skills/verify-self/SKILL.md` → `valid: true`.
- `cd engine && node bin/sovereign-tools.cjs doctor` → `total_skills 18`, `auto_count 5`, `disabled_count 13`.
- `cd engine && node --test "test/**/*.test.cjs"` → 164 pass, 0 fail.

Manual (judgment, per VALIDATION.md): read the SKILL.md — confirm the hard stop is first, the audit names file:line uncertainties, A/B/C are concrete and recommendation-first, choice B's marker matches `unverified-marker.md` verbatim, and the footer makes the anchor→verify→sentinel loop legible.
</verification>

<success_criteria>
All four Phase 16 success criteria are TRUE and grep/command-verifiable:
- SC1 (VERIFY-01): hard stop + retroactive audit since last verified anchor, each claim as `file:line` + what's uncertain — gates B1, B2.
- SC2 (VERIFY-02): three choices (A anchor-docs / B mark SOVEREIGN:UNVERIFIED / C discard+restart); B emits the exact marker form sentinel scans — gates B3, B4, B5, L2.
- SC3 (ANCHOR-02): composes with anchor-docs on choice A; loop closed (sentinel scans markers) — gates L1, L2.
- SC4 (M4-CC): one-call `init verify-self` orient, thin-orchestrator shape, `disable-model-invocation: true` (doctor budget held at 5), validate passes — gates S2–S10, G15, G16.
- Regression: engine untouched, 164 tests green — gate G17.
</success_criteria>

<output>
After completion, create `.planning/phases/16-verify-self-skill/16-01-SUMMARY.md`.
</output>
