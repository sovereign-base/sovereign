---
phase: 09-security-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/skills/deploy-design/SKILL.md
autonomous: true
requirements: [ARCH-07, ARCH-08]
must_haves:
  truths:
    - "Running /deploy-design orients with exactly one `init deploy-design` call (+ @file: guard) and reads nothing else to orient."
    - "The skill ASKS the monthly infra budget explicitly up front, then walks the deploy topics one at a time, recommendation-first: self-hosted vs managed vs hybrid + platform, container strategy, IaC, CI/CD, environments, disaster recovery + backups."
    - "Recommendations are budget + scale + region aware (platform choices keyed to budget), and the plan is recorded to .sovereign/docs/infra/DEPLOY_MODEL.md."
    - "Gate-passing choices (cloud/platform, container strategy, IaC) are OFFERED to /adr-log, not re-implemented; the skill updates DEPLOY_MODEL.md in place on re-run; it stays at the plan level (no actual provisioning)."
    - "The skill sets disable-model-invocation: true, opens with `## Why this matters`, ends with a navigation footer pointing toward Construction (tdd/sentinel), and delegates state save + commit to sovereign-tools."
  artifacts:
    - path: "engine/skills/deploy-design/SKILL.md"
      provides: "Budget-aware deploy-design thin orchestrator over the M1 engine"
      min_lines: 70
      contains: "disable-model-invocation: true"
  key_links:
    - from: "engine/skills/deploy-design/SKILL.md"
      to: "sovereign-tools init deploy-design"
      via: "single orient call"
      pattern: "init deploy-design"
    - from: "engine/skills/deploy-design/SKILL.md"
      to: ".sovereign/docs/infra/DEPLOY_MODEL.md"
      via: "record + in-place update"
      pattern: "DEPLOY_MODEL\\.md"
    - from: "engine/skills/deploy-design/SKILL.md"
      to: "/adr-log"
      via: "offer gate-passing choices"
      pattern: "adr-log"
---

<objective>
Author `engine/skills/deploy-design/SKILL.md` — a hand-authored thin orchestrator over the M1 engine that runs a budget-aware deployment/infrastructure conversation (self-hosted vs managed, platform, container strategy, IaC, CI/CD, environments, disaster recovery) and records it to `.sovereign/docs/infra/DEPLOY_MODEL.md`. Satisfies ARCH-07 and contributes to ARCH-08 (the cross-cutting thin-orchestrator shape). This is the FINAL M2 skill — completing it closes the Architecture milestone.

Purpose: Infrastructure decided ad hoc at deploy time, ignorant of budget, is how a side project racks up a four-figure cloud bill or a startup over-engineers Kubernetes for ten users. This skill makes the deploy plan a budget-first, recommendation-first design — recorded — that Construction can build against, composing with `adr-log` for the consequential choices.

Output: `engine/skills/deploy-design/SKILL.md` (≥70 lines), passing `validate skills`, with `doctor` confirming the final M2 listing budget (5 auto-triggerable + 8 disabled, no warnings).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-security-deploy/09-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Sibling M2 skills to mirror exactly. stack-select is the closest analog — it ALSO
# asks budget explicitly. Mirror its shape, sections, in-place-update + currency-honesty:
@engine/skills/stack-select/SKILL.md
@engine/skills/api-design/SKILL.md

# Authoring standard + the gate this skill offers decisions against:
@engine/references/skill-format.md
@engine/references/adr-format.md

<interfaces>
<!-- `init deploy-design` routes through the `default` (fast-lane stub) case in
     engine/bin/lib/init.cjs — NO engine change needed. Returns the same shape as
     09-01's <interfaces> block: project_root, sovereign_version, models {},
     config {model_profile, commit_docs, council_mode_default, parallelization},
     phase {}, context_injection {manifest_path, glossary_path}, paths {state,
     manifest}, exists {...}, agents_installed, missing_agents. -->

The write target dir `.sovereign/docs/infra/` already ships in the scaffolded
template tree (engine/templates/sovereign/docs/infra). Read prior architecture docs
BY PATH only — .sovereign/docs/STACK.md and SCALE_STRATEGY.md (infra follows the
stack + scale) — and degrade gracefully if absent.

Commit + state are delegated:
  node "$ENGINE/bin/sovereign-tools.cjs" state save
  node "$ENGINE/bin/sovereign-tools.cjs" commit "deploy-design: <scope>" --files .sovereign/docs/infra/DEPLOY_MODEL.md .sovereign/STATE.md .sovereign/MANIFEST.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the deploy-design SKILL.md</name>
  <files>engine/skills/deploy-design/SKILL.md</files>
  <read_first>
    Read engine/skills/stack-select/SKILL.md in full — it is the closest sibling
    (it also asks budget explicitly and flags currency-honesty for fast-moving
    facts like pricing). Read engine/skills/api-design/SKILL.md for the
    `## <ARTIFACT>.md format` block + ▶ NEXT footer idiom. Mirror their exact shape:
    YAML frontmatter (name, description, disable-model-invocation: true,
    argument-hint), `## Why this matters`, `## When to use this` (with anti-uses),
    a numbered `## The flow` whose step 1 is the single `init` orient call with the
    `@file:` spill guard, a `## DEPLOY_MODEL.md format` fenced block, and a
    recommendation-first `## Navigation` footer. Read engine/references/skill-format.md
    and engine/references/adr-format.md (the three-condition gate). Read 09-CONTEXT.md
    §decisions for the exact deploy topics and the budget-first requirement, and
    note §discretion: the nav footer should point toward Construction (M1's tdd +
    sentinel), since this closes Architecture.
  </read_first>
  <action>
    Create engine/skills/deploy-design/SKILL.md as a thin orchestrator mirroring
    stack-select/api-design. Requirements:

    FRONTMATTER (real Agent Skills fields only — no v1 fields):
    - name: deploy-design
    - description: lead with the use-case (design a budget-aware deployment plan —
      self-hosted vs managed, platform, containers, IaC, CI/CD, environments, DR —
      recorded as a living DEPLOY_MODEL.md; use after the stack/scale are shaped and
      before construction).
    - disable-model-invocation: true   (REQUIRED — phase-gated, user-only; keeps the
      doctor auto-trigger budget at 5 — this is ARCH-08).
    - argument-hint: "[system or service to deploy]"

    BODY (in this order, per skill-format.md):
    1. `## Why this matters` — plain-language, first-real-product framing: infra picked
       at deploy time with no budget conversation either over-engineers (k8s for ten
       users) or surprises you with a bill. A budget-first, recorded plan is the cheap
       path.
    2. `## When to use this` — after stack/scale design, before construction; plus
       anti-uses ("Don't use it to actually provision infra or write Terraform — this
       designs and records the plan; construction/ops executes it. Stays at the plan
       level.").
    3. `## The flow` — numbered, thin-orchestrator:
       - **1 — Orient (one call).** Exactly one `init deploy-design` call with the
         `@file:` spill guard (copy the idiom from stack-select verbatim, substituting
         the skill name). Parse state path + glossary from the blob; read
         .sovereign/docs/STACK.md and SCALE_STRATEGY.md BY PATH if present (infra
         follows the stack + scale) and degrade gracefully if absent. No other
         orientation reads.
       - **2 — Ask the budget explicitly.** Before recommending anything, ASK the
         monthly infra spend (and any cost ceiling). It changes every downstream answer.
         (Must contain the literal string "budget".)
       - **3 — Walk the deploy topics, one at a time, recommendation-first.** Each topic:
         recommend the best fit for the budget + scale + region first, say what you're
         NOT picking and why, then wait. The topics from 09-CONTEXT §decisions:
           a. **Self-hosted vs managed cloud vs hybrid**, then **platform** options with
              trade-offs (AWS / Azure / GCP / DigitalOcean / Hetzner / Fly.io)
              recommended by budget + scale + region.
           b. **Container strategy** — Docker / Kubernetes / simpler (Coolify / Dokploy /
              Railway), keyed to budget + team capacity.
           c. **IaC** — Terraform / Pulumi / Ansible / none, with reasoning.
           d. **CI/CD** pipeline.
           e. **Environments** — dev / staging / prod / preview.
           f. **Disaster recovery + backups.**
       - **4 — Be honest about currency.** When a recommendation hinges on fast-moving
         facts (current pricing, a new managed tier), say so and flag the user may want
         to verify against current docs (the future anchor-docs, M3) — mirror
         stack-select's currency-honesty step. Don't assert stale pricing with false
         confidence.
       - **5 — Offer ADRs, don't write them.** Gate-passing choices (cloud/platform,
         container strategy, IaC) that pass the three-condition gate → **offer `/adr-log`**.
         Do not number or write ADRs here.
       - **6 — Record + persist.** Write/update `.sovereign/docs/infra/DEPLOY_MODEL.md`.
         **Update in place** on re-run (match on topic headings — don't duplicate). Then
         `state save` then `commit` via sovereign-tools (use the exact delegated command
         shape from <interfaces> — never hand-write state or branch on commit_docs).
    4. `## DEPLOY_MODEL.md format` — a fenced markdown block showing the recorded shape:
       the budget decided-against line up top, then a per-topic block with the chosen
       option + rationale + what was rejected and why. (Layout is Claude's discretion per
       09-CONTEXT; the per-topic block + chosen option + rationale is the intent.)
    5. `## Navigation` — recommendation-first footer (mirror stack-select's ▶ NEXT block):
       DEPLOY_MODEL.md recorded → path; since this CLOSES Architecture, point toward
       Construction — recommend /tdd (begin building red-green) and /sentinel (review
       gate), plus /adr-log for any gate-passing infra choice.

    Keep the body thin (progressive disclosure — no fat inline detail). Target ≥70 lines.
    Author "deploy-design" never tool/model names in the `name`.
  </action>
  <verify>
    <automated>test -f engine/skills/deploy-design/SKILL.md && \
      [ "$(grep -c . engine/skills/deploy-design/SKILL.md)" -ge 70 ] && \
      grep -q "disable-model-invocation: true" engine/skills/deploy-design/SKILL.md && \
      grep -q "init deploy-design" engine/skills/deploy-design/SKILL.md && \
      grep -q "@file:" engine/skills/deploy-design/SKILL.md && \
      grep -q "DEPLOY_MODEL.md" engine/skills/deploy-design/SKILL.md && \
      grep -q "budget" engine/skills/deploy-design/SKILL.md && \
      grep -q "adr-log" engine/skills/deploy-design/SKILL.md && \
      grep -q "Why this matters" engine/skills/deploy-design/SKILL.md && \
      grep -q "commit" engine/skills/deploy-design/SKILL.md && \
      node engine/bin/sovereign-tools.cjs validate skills engine/skills/deploy-design/SKILL.md</automated>
  </verify>
  <done>
    engine/skills/deploy-design/SKILL.md exists, is ≥70 non-blank lines, contains all
    grep strings (disable-model-invocation: true, init deploy-design, @file:,
    DEPLOY_MODEL.md, budget, adr-log, Why this matters, commit), asks the budget
    explicitly before recommending, walks all deploy topics recommendation-first,
    offers /adr-log, updates in place, stays at plan level, footer points toward
    Construction, delegates state/commit, and passes `validate skills` against its
    explicit path.
  </done>
</task>

</tasks>

<verification>
- `node engine/bin/sovereign-tools.cjs validate skills engine/skills/deploy-design/SKILL.md` exits 0.
- `node engine/bin/sovereign-tools.cjs doctor` reports the FINAL M2 state: 5 auto-triggerable Fast Lane skills + 8 disabled (council + the 7 M2 skills: adr-log, entity-design, api-design, stack-select, scale-design, security-design, deploy-design), no warnings — ARCH-08 listing-budget guarantee held across the whole milestone.
- All grep strings present (see Task 1 verify).
- File is ≥70 non-blank lines.
</verification>

<success_criteria>
- deploy-design SKILL.md authored as a thin orchestrator: single `init deploy-design` orient (+ @file: guard), asks budget explicitly first, recommendation-first one-topic-at-a-time, `## Why this matters`, navigation footer pointing toward Construction, `disable-model-invocation: true`.
- Walks all deploy topics (self-hosted/managed/hybrid + platform, container strategy, IaC, CI/CD, environments, DR), records to DEPLOY_MODEL.md with in-place update, offers /adr-log, stays at plan level.
- `validate skills` passes; `doctor` confirms 5 auto-triggerable + 8 disabled with no warnings (ARCH-08 closes; M2 complete).
- ARCH-07 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-deploy/09-02-SUMMARY.md`
</output>
