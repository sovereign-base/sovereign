---
phase: 09-security-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/skills/security-design/SKILL.md
autonomous: true
requirements: [ARCH-06, ARCH-08]
must_haves:
  truths:
    - "Running /security-design orients with exactly one `init security-design` call (+ @file: guard) and reads nothing else to orient."
    - "The skill walks the user through five layers one at a time, recommendation-first: auth/authz, data classification, application/OWASP, infrastructure, AI-agent/prompt-injection."
    - "Each layer ends with a security classification (low/medium/high/critical) and the chosen control recorded to .sovereign/docs/security/SECURITY_MODEL.md."
    - "Gate-passing choices (auth model, tenancy isolation, encryption/KMS) are OFFERED to /adr-log, not re-implemented; the skill updates SECURITY_MODEL.md in place on re-run."
    - "The skill sets disable-model-invocation: true, opens with `## Why this matters`, ends with a navigation footer, and delegates state save + commit to sovereign-tools."
  artifacts:
    - path: "engine/skills/security-design/SKILL.md"
      provides: "Layered security-design thin orchestrator over the M1 engine"
      min_lines: 70
      contains: "disable-model-invocation: true"
  key_links:
    - from: "engine/skills/security-design/SKILL.md"
      to: "sovereign-tools init security-design"
      via: "single orient call"
      pattern: "init security-design"
    - from: "engine/skills/security-design/SKILL.md"
      to: ".sovereign/docs/security/SECURITY_MODEL.md"
      via: "record + in-place update"
      pattern: "SECURITY_MODEL\\.md"
    - from: "engine/skills/security-design/SKILL.md"
      to: "/adr-log"
      via: "offer gate-passing choices"
      pattern: "adr-log"
---

<objective>
Author `engine/skills/security-design/SKILL.md` — a hand-authored thin orchestrator over the M1 engine that walks a user through a layered, state-of-the-art security model (auth/authz, data classification, application/OWASP, infrastructure, AI-agent/prompt-injection) and records it to `.sovereign/docs/security/SECURITY_MODEL.md`. Satisfies ARCH-06 and contributes to ARCH-08 (the cross-cutting thin-orchestrator shape).

Purpose: Architecture should not enter Construction with security as a checkbox afterthought. This skill makes the security model an explicit, layered, recorded design decision — recommendation-first, one layer at a time — composing with `adr-log` for the consequential choices.

Output: `engine/skills/security-design/SKILL.md` (≥70 lines), passing `validate skills` and leaving the `doctor` auto-trigger budget at 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-security-deploy/09-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Sibling M2 skills to mirror exactly (shape, sections, footer, in-place-update language):
@engine/skills/api-design/SKILL.md
@engine/skills/stack-select/SKILL.md

# Authoring standard + the gate this skill offers decisions against:
@engine/references/skill-format.md
@engine/references/adr-format.md

<interfaces>
<!-- The init orient blob for an arbitrary skill name routes through the `default`
     (fast-lane stub) case in engine/bin/lib/init.cjs — NO engine change is needed.
     `init security-design` returns this shape: -->

```json
{
  "project_root": "<cwd>",
  "sovereign_version": "2.0.0",
  "models": {},
  "config": { "model_profile": "...", "commit_docs": "...", "council_mode_default": "...", "parallelization": "..." },
  "phase": { "current": 0, "name": "...", "gate_status": "...", "active_tracks": [] },
  "context_injection": { "manifest_path": ".sovereign/MANIFEST.md", "glossary_path": ".sovereign/CONTEXT.md" },
  "paths": { "state": ".sovereign/STATE.md", "manifest": ".sovereign/MANIFEST.md" },
  "exists": { "sovereign_dir": true, "manifest": true, "constitution": true, "glossary": true },
  "agents_installed": true,
  "missing_agents": []
}
```

The write target dir `.sovereign/docs/security/` already ships in the scaffolded
template tree (engine/templates/sovereign/docs/security). Read prior architecture
docs BY PATH only (.sovereign/docs/ENTITY_MODEL.md, STACK.md) — data sensitivity is
entity-shaped — and degrade gracefully if absent.

Commit + state are delegated:
  node "$ENGINE/bin/sovereign-tools.cjs" state save
  node "$ENGINE/bin/sovereign-tools.cjs" commit "security-design: <scope>" --files .sovereign/docs/security/SECURITY_MODEL.md .sovereign/STATE.md .sovereign/MANIFEST.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the security-design SKILL.md</name>
  <files>engine/skills/security-design/SKILL.md</files>
  <read_first>
    Read engine/skills/api-design/SKILL.md and engine/skills/stack-select/SKILL.md
    in full — mirror their exact shape: YAML frontmatter (name, description,
    disable-model-invocation: true, argument-hint), `## Why this matters`,
    `## When to use this` (with anti-uses), a numbered `## The flow` whose step 1
    is the single `init` orient call with the `@file:` spill guard, a
    `## <ARTIFACT>.md format` fenced block, and a recommendation-first
    `## Navigation` footer. Read engine/references/skill-format.md (the required
    sections + thin-body/single-init rule) and engine/references/adr-format.md
    (the three-condition gate this skill offers decisions against). Read
    09-CONTEXT.md §decisions for the exact five layers and what each must cover.
  </read_first>
  <action>
    Create engine/skills/security-design/SKILL.md as a thin orchestrator mirroring
    api-design/stack-select. Requirements:

    FRONTMATTER (real Agent Skills fields only — no v1 fields):
    - name: security-design
    - description: lead with the use-case (design a layered security model — auth,
      data, app/OWASP, infra, agent — recorded as a living SECURITY_MODEL.md; use
      after the domain/stack are shaped and before construction).
    - disable-model-invocation: true   (REQUIRED — phase-gated, user-only; keeps the
      doctor auto-trigger budget at 5 — this is ARCH-08).
    - argument-hint: "[system or surface to secure]"

    BODY (in this order, per skill-format.md):
    1. `## Why this matters` — plain-language, first-real-product framing: security
       bolted on after the fact is the expensive kind; a layered model decided up
       front and recorded is the cheap kind. NOT checkbox security.
    2. `## When to use this` — after entity/stack design, before construction; plus
       anti-uses ("Don't use it to actually configure controls or provision infra —
       this designs and records; don't run it before the domain is modeled").
    3. `## The flow` — numbered, thin-orchestrator:
       - **1 — Orient (one call).** Exactly one `init security-design` call with the
         `@file:` spill guard (copy the two-line idiom from api-design verbatim,
         substituting the skill name). Parse glossary path + state path from the blob;
         read .sovereign/docs/ENTITY_MODEL.md and STACK.md BY PATH if present (data
         sensitivity is entity-shaped) and degrade gracefully if absent. No other
         orientation reads.
       - **2 — Walk the five layers, one at a time, recommendation-first.** Each layer:
         give your recommended control first, wait, then assign a security
         classification (low/medium/high/critical). The five layers, each named, with
         the sub-topics from 09-CONTEXT §decisions:
           a. **Auth & authz** — auth strategy (sessions / JWT / OAuth2.1 / passkeys /
              magic links), RBAC vs ABAC vs ReBAC, multi-tenancy isolation, token
              refresh/revocation/rotation.
           b. **Data security** — classification (public/internal/confidential/
              restricted), encryption at rest + in transit, PII handling +
              minimization + erasure, secrets management.
           c. **Application (OWASP)** — input validation, injection prevention,
              XSS/CSRF/CORS, rate limiting + abuse, dependency scanning. (Must contain
              the literal string "OWASP".)
           d. **Infrastructure** — network segmentation, least privilege, audit logging
              (who/what/when/from where), intrusion-detection baseline.
           e. **AI/agent (OWASP Agentic)** — prompt-injection prevention, skill
              supply-chain vetting, agent permission scoping.
       - **3 — Offer ADRs, don't write them.** Gate-passing choices (auth model,
         tenancy isolation, encryption/KMS) that pass the three-condition gate →
         **offer `/adr-log`**. Do not number or write ADRs here.
       - **4 — Record the model.** Write/update `.sovereign/docs/security/SECURITY_MODEL.md`.
         **Update in place** on re-run (match on layer headings — don't duplicate).
       - **5 — Persist.** `state save` then `commit` via sovereign-tools (use the exact
         delegated command shape from the <interfaces> block — never hand-write state
         or branch on commit_docs).
    4. `## SECURITY_MODEL.md format` — a fenced markdown block showing the recorded
       shape: a per-layer block with the chosen control + rationale + the assigned
       classification (low/medium/high/critical). (Layout is Claude's discretion per
       09-CONTEXT; the per-layer block + chosen option + rationale + classification is
       the intent.)
    5. `## Navigation` — recommendation-first footer (mirror api-design's fenced ▶ NEXT
       block): SECURITY_MODEL.md recorded → path; recommend /deploy-design next (the
       infra plan secures against this model) and /adr-log for any gate-passing choice.

    Keep the body thin (progressive disclosure — no fat inline detail). Target ≥70 lines.
    Author "security-design" never tool/model names in the `name`.
  </action>
  <verify>
    <automated>test -f engine/skills/security-design/SKILL.md && \
      [ "$(grep -c . engine/skills/security-design/SKILL.md)" -ge 70 ] && \
      grep -q "disable-model-invocation: true" engine/skills/security-design/SKILL.md && \
      grep -q "init security-design" engine/skills/security-design/SKILL.md && \
      grep -q "@file:" engine/skills/security-design/SKILL.md && \
      grep -q "SECURITY_MODEL.md" engine/skills/security-design/SKILL.md && \
      grep -q "OWASP" engine/skills/security-design/SKILL.md && \
      grep -q "adr-log" engine/skills/security-design/SKILL.md && \
      grep -q "Why this matters" engine/skills/security-design/SKILL.md && \
      grep -q "commit" engine/skills/security-design/SKILL.md && \
      node engine/bin/sovereign-tools.cjs validate skills engine/skills/security-design/SKILL.md</automated>
  </verify>
  <done>
    engine/skills/security-design/SKILL.md exists, is ≥70 non-blank lines, contains
    all grep strings (disable-model-invocation: true, init security-design, @file:,
    SECURITY_MODEL.md, OWASP, adr-log, Why this matters, commit), walks all five named
    layers recommendation-first with a classification, offers /adr-log, updates in
    place, delegates state/commit, and passes `validate skills` against its explicit path.
  </done>
</task>

</tasks>

<verification>
- `node engine/bin/sovereign-tools.cjs validate skills engine/skills/security-design/SKILL.md` exits 0.
- `node engine/bin/sovereign-tools.cjs doctor` reports the auto-trigger listing budget unchanged at the 5 Fast Lane skills (security-design is disable-model-invocation, so not auto-triggerable) with no warnings.
- All grep strings present (see Task 1 verify).
- File is ≥70 non-blank lines.
</verification>

<success_criteria>
- security-design SKILL.md authored as a thin orchestrator: single `init security-design` orient (+ @file: guard), recommendation-first one-layer-at-a-time, `## Why this matters`, navigation footer, `disable-model-invocation: true`.
- Walks all five layers (auth/authz, data classification, app/OWASP, infra, agent/prompt-injection), assigns classifications, records to SECURITY_MODEL.md with in-place update, offers /adr-log.
- `validate skills` passes; `doctor` confirms auto-trigger budget stays at 5 (ARCH-08 holds).
- ARCH-06 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-deploy/09-01-SUMMARY.md`
</output>
