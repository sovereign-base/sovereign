---
phase: 02-bootstrap-subagents
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/agents/sovereign-advisor.md
  - engine/agents/sovereign-chairman.md
  - engine/agents/sovereign-peer-reviewer.md
  - engine/agents/sovereign-sentinel.md
  - engine/package.json
  - engine/test/agents.test.cjs
autonomous: true
requirements: [INIT-01]
must_haves:
  truths:
    - "The 4 M1-dispatched subagents exist as definition files: sovereign-advisor, sovereign-chairman, sovereign-peer-reviewer, sovereign-sentinel"
    - "Each agent file uses real Agent Skills subagent frontmatter (name, description, tools, model) — no v1 fields, no 'claude'/'anthropic' in name"
    - "Each agent body declares it returns validated JSON against a fixed schema (never prose), with the schema documented inline"
    - "The package ships the agents/ dir so the installer can copy them"
  artifacts:
    - path: "engine/agents/sovereign-advisor.md"
      provides: "Parameterized lens-shell advisor (lens injected by Council in Phase 3) + JSON return schema"
      contains: "name: sovereign-advisor"
    - path: "engine/agents/sovereign-chairman.md"
      provides: "Synthesis-of-anonymized-transcripts to verdict agent + JSON return schema"
      contains: "name: sovereign-chairman"
    - path: "engine/agents/sovereign-peer-reviewer.md"
      provides: "Anonymous A-E cross-review agent + JSON return schema"
      contains: "name: sovereign-peer-reviewer"
    - path: "engine/agents/sovereign-sentinel.md"
      provides: "Native-tier post-phase reviewer agent + JSON return schema"
      contains: "name: sovereign-sentinel"
    - path: "engine/package.json"
      provides: "files allowlist including agents"
      contains: "agents"
  key_links:
    - from: "engine/package.json"
      to: "engine/agents/"
      via: "files allowlist entry so npm pack ships the agent defs"
      pattern: 'agents allowlist entry'
    - from: "engine/bin/lib/install.cjs"
      to: "engine/agents/*.md"
      via: "installer copies packaged agents into .claude/agents/ (link realized in 02-01)"
      pattern: "agents"
---

<objective>
Define the four reasoning subagents the M1 SOVEREIGN skills dispatch — `sovereign-advisor`, `sovereign-chairman`, `sovereign-peer-reviewer`, `sovereign-sentinel` — each as a real Agent Skills subagent definition file with a fixed JSON return schema, and add `agents` to the package `files` allowlist so they ship.

Purpose: Layer 3 of the architecture. These are the isolated-context reasoners Council (Phase 3) and Sentinel (Phase 4) dispatch. Defining them now with pinned return schemas lets the engine `agents_installed` guard (plan 02-03) check for them and lets later orchestrators dispatch against a stable contract.
Output: 4 agent definition files at repo `agents/` + `engine/package.json` files-allowlist update + a structural test asserting frontmatter validity + schema presence.

DEVIATION (deliberate, per 02-CONTEXT.md): ROADMAP criterion #3 also lists `planner` and `researcher`. M1 has NO skill that dispatches them — defining callerless agents is the over-scope trap. They are DEFERRED. All four M1-dispatched agents are defined here; INIT/SKL requirements stay satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-bootstrap-subagents/02-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@SOVEREIGN.md

# This plan defines the agent SHELLS + return schemas only. The 5 Council lens
# personas (Skeptic/Architect/Builder/Outsider/Risk Officer) are CONTENT injected
# by the Council orchestrator in Phase 3 — sovereign-advisor is ONE parameterized
# file, not five. Do NOT write the lens content here.

<interfaces>
<!-- Real Agent Skills subagent frontmatter (STACK.md, verified from Claude Code docs
     + ~/.claude/agents/gsd-*.md). Use THESE fields only — NO v1 fields
     (triggers/works-best-with/min-model/tokens/phase). -->

Subagent frontmatter fields:
  name          REQUIRED — lowercase-hyphen, <=64 chars, must NOT contain 'claude'/'anthropic'
  description   REQUIRED — drives delegation (what it does + when to dispatch)
  tools         optional — explicit least-privilege list (e.g. "Read, Grep, Glob")
  model         optional — haiku|sonnet|opus|inherit (use `inherit` — engine resolves the real model via model-profiles)
  color         optional — cosmetic

Reference (gsd-planner.md frontmatter shape — fields between the YAML `---` fences):
  name: gsd-planner
  description: Creates executable phase plans ... Spawned by /gsd:plan-phase orchestrator.
  tools: Read, Write, Bash, Glob, Grep, WebFetch
  color: green

Engine model-profile mapping (engine/bin/lib/model-profiles.cjs — the engine resolves
these; agent files set model: inherit and let resolution happen at dispatch):
  advisor, chairman, peer_reviewer = opus (quality) / sonnet (balanced|budget)
  sentinel = sonnet (quality|balanced) / haiku (budget)

Subagent-return-JSON contract (pinned in Phase 1 ADR): every agent returns validated
JSON, never prose. Minimum shape `{ ok: boolean, ... }` plus agent-specific fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 4 subagent definition files with frontmatter + JSON return schemas</name>
  <read_first>
    - .planning/phases/02-bootstrap-subagents/02-CONTEXT.md (agent roster + scope: shells only, defer planner/researcher)
    - .planning/research/ARCHITECTURE.md (dispatch shapes table; write-then-return discipline; path-passing)
    - .planning/research/STACK.md (subagent frontmatter spec lines on name/description/tools/model/disallowedTools)
    - /Users/wizzie/.claude/agents/gsd-planner.md (real frontmatter + structured-return discipline to mirror)
    - engine/bin/lib/model-profiles.cjs (which agents resolve to which models — informs model: inherit choice)
  </read_first>
  <action>
Create the `engine/agents/` directory (canonical packed location — see Task 2) and author these four files. Each = YAML frontmatter (real Agent Skills subagent fields ONLY) + a markdown body that (a) states the agent's single responsibility, (b) enforces write-then-return + path-passing discipline (reads only files the orchestrator points it at; never invents context), and (c) documents the FIXED JSON return schema it MUST emit — "return validated JSON against this schema; never prose."

For ALL four: `model: inherit` (the engine resolves the real model via model-profiles at dispatch); set least-privilege `tools`. Name MUST be lowercase-hyphen, <=64 chars, contain NO 'claude'/'anthropic'. Every body MUST include a literal line: "Return validated JSON matching the schema above — output JSON only, no prose." Every schema MUST include an `ok: boolean` field.

1) engine/agents/sovereign-advisor.md
   - frontmatter: name: sovereign-advisor; description: "A single Council advisor that evaluates a decision through an injected lens. Dispatched in parallel (one per lens) by the council orchestrator."; tools: Read, Grep, Glob; model: inherit; color: (your choice).
   - body: explain it is a PARAMETERIZED SHELL — the orchestrator injects a `<lens>` (the persona) and `<context>` paths (MANIFEST/CONTEXT/relevant ADRs) in the dispatch prompt. The advisor reads those paths itself, reasons through the lens, returns JSON. Document schema:
     `{ ok: boolean, lens: string, position: "support"|"oppose"|"conditional", confidence: number, key_points: string[], risks: string[], recommendation: string }`
     State explicitly: NO lens content lives in this file (that is Phase 3). Note it must NOT call AskUserQuestion (autonomous reasoning).

2) engine/agents/sovereign-chairman.md
   - frontmatter: name: sovereign-chairman; description: "Synthesizes anonymized advisor transcripts into a single binding Council verdict."; tools: Read, Grep, Glob; model: inherit.
   - body: reads the anonymized advisor outputs (paths passed by orchestrator), resolves minority/dissenting positions, produces a verdict. Document schema:
     `{ ok: boolean, verdict: "PASS"|"CONDITIONAL_PASS"|"BLOCKED", synthesis: string, conditions: string[], dissents_addressed: string[], confidence: number }`

3) engine/agents/sovereign-peer-reviewer.md
   - frontmatter: name: sovereign-peer-reviewer; description: "Cross-reviews anonymized advisor responses (labeled A-E) for blind spots and weak arguments before chairman synthesis."; tools: Read, Grep, Glob; model: inherit.
   - body: receives advisor outputs anonymized as A-E (orchestrator does the anonymization in Phase 3), critiques each for gaps/contradictions, returns per-response critique. Document schema:
     `{ ok: boolean, reviews: [{ label: string, strengths: string[], weaknesses: string[], overlooked: string[] }], cross_cutting_concerns: string[] }`

4) engine/agents/sovereign-sentinel.md
   - frontmatter: name: sovereign-sentinel; description: "Native-tier post-phase reviewer: scans for SOVEREIGN:UNVERIFIED markers and checks spec/ADR/commenting alignment, emitting a structured verdict."; tools: Read, Grep, Glob, Bash; model: inherit.
   - body: reviews changed files (paths from orchestrator), checks the items above, emits a verdict. Note the SOVEREIGN:UNVERIFIED marker SPEC itself is defined in Phase 4 (CONV-03) — this agent declares the consumer shape now. Document schema:
     `{ ok: boolean, verdict: "PASS"|"CONCERNS"|"BLOCKED", unverified_markers: [{ file: string, line: number, text: string }], findings: [{ severity: "info"|"warn"|"block", file: string, message: string }] }`
  </action>
  <verify>
    <automated>cd /Users/wizzie/conductor/workspaces/sovereign/seville && for f in engine/agents/sovereign-advisor.md engine/agents/sovereign-chairman.md engine/agents/sovereign-peer-reviewer.md engine/agents/sovereign-sentinel.md; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done && [ "$(grep -lE '^name:.*(claude|anthropic)' engine/agents/*.md | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -l 'ok:' engine/agents/*.md | wc -l | tr -d ' ')" = "4" ] && echo OK</automated>
  </verify>
  <done>All 4 agent files exist with valid lowercase-hyphen names (no claude/anthropic), real Agent Skills frontmatter (no v1 fields), and an inline JSON return schema containing `ok: boolean`.</done>
</task>

<task type="auto">
  <name>Task 2: Add agents to the package files allowlist + structural test</name>
  <read_first>
    - engine/package.json (the files allowlist: currently ["bin","templates","VERSION"])
    - engine/test/validate.test.cjs (frontmatter-parsing test style to mirror)
    - engine/agents/*.md (the four files created in Task 1)
  </read_first>
  <action>
1) Edit engine/package.json: add "agents" to the `files` array → ["bin","templates","agents","VERSION"]. The npm `files` allowlist is rooted at the package dir (engine/), and the agent files live at engine/agents/ (Task 1) — so this entry packs them directly. No build step, no reaching above the package dir. Invariant to satisfy: `npm pack` from engine/ includes the four engine/agents/*.md files, and plan 02-01's install.cjs copies them from path.join(packageRoot,'agents').
2) Create engine/test/agents.test.cjs: for each of the four agent files under engine/agents/, parse frontmatter (inline a minimal line-based `---`-block `key: value` parser mirroring validate.cjs's parseFrontmatter) and assert: name present, name matches ^[a-z0-9-]+$, name has no 'claude'/'anthropic', description present, body contains 'ok:' (schema marker) and the literal 'JSON only' instruction. Also assert require('../package.json').files includes "agents".
  </action>
  <verify>
    <automated>cd /Users/wizzie/conductor/workspaces/sovereign/seville/engine && node -e "const f=require('./package.json').files; if(!f.includes('agents')) process.exit(1)" && node --test "test/agents.test.cjs"</automated>
  </verify>
  <done>engine/package.json `files` includes "agents"; the four agent files ship from the packaged location; agents.test.cjs passes (frontmatter valid, schema + JSON-only marker present).</done>
</task>

</tasks>

<verification>
- All four agent files exist with valid Agent Skills frontmatter (no v1 fields; no reserved words in name).
- Each declares a fixed JSON return schema with an `ok` field and a "JSON only, no prose" instruction.
- `engine/package.json` `files` includes "agents"; `npm pack` ships them.
- `node --test "test/agents.test.cjs"` passes; full suite stays green.
- planner/researcher are intentionally NOT defined (documented deviation).
</verification>

<success_criteria>
- INIT-01 (agent half): the M1-dispatched subagents the installer ships and later orchestrators dispatch are defined with fixed return schemas.
- Real Agent Skills frontmatter only; zero v1 fields.
- agents ship in the package allowlist.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bootstrap-subagents/02-02-SUMMARY.md`.
</output>
