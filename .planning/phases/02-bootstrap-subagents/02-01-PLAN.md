---
phase: 02-bootstrap-subagents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/bin/sovereign.cjs
  - engine/bin/lib/install.cjs
  - engine/test/install.test.cjs
autonomous: true
requirements: [INIT-01, INIT-02, INIT-03]
must_haves:
  truths:
    - "Running `npx sovereign init` in a fresh project copies SOVEREIGN skills + agents into .claude/ and scaffolds .sovereign/ from templates"
    - "`init --quick` copies only the Fast Lane 5 skill dirs; `init --full` copies every packaged skill dir"
    - "Re-running install on an up-to-date project is non-destructive (does not clobber user-modified .sovereign/ content)"
    - "Re-running install when the packaged VERSION is newer reports the version delta and updates skills/agents"
  artifacts:
    - path: "engine/bin/lib/install.cjs"
      provides: "Install logic: target resolution (.claude vs ~/.claude), copy skills+agents, scaffold .sovereign, version diff"
      min_lines: 80
      exports: ["cmdInstall", "runInstall"]
    - path: "engine/bin/sovereign.cjs"
      provides: "User-facing launcher routing `init [--quick|--full|--global]` to cmdInstall"
      contains: "init"
    - path: "engine/test/install.test.cjs"
      provides: "Idempotency + --quick/--full + version-aware install assertions over tmpdir fixtures"
      contains: "--quick"
  key_links:
    - from: "engine/bin/sovereign.cjs"
      to: "engine/bin/lib/install.cjs"
      via: "require + cmdInstall dispatch on 'init' subcommand"
      pattern: "require.*install\\.cjs"
    - from: "engine/bin/lib/install.cjs"
      to: "engine/templates/sovereign"
      via: "recursive copy of the seed tree into target .sovereign/"
      pattern: "templates.*sovereign"
---

<objective>
Implement the `npx sovereign init` installer — the user front door (INIT-01/02/03). It copies SOVEREIGN's packaged skills + agents into the project's `.claude/` (default) or `~/.claude/` (`--global`), scaffolds `.sovereign/` from the shipped `templates/sovereign/` tree, and is idempotent + version-aware on re-run.

Purpose: Phase 1 built the engine; this is what gets it into a user's project in one command. Without it nothing else is installable.
Output: `engine/bin/sovereign.cjs` (real launcher, replaces the Phase-1 stub) + `engine/bin/lib/install.cjs` (install logic) + `engine/test/install.test.cjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-bootstrap-subagents/02-CONTEXT.md
@.planning/ROADMAP.md
@SOVEREIGN.md

# Decisions this plan implements (from 02-CONTEXT.md):
# - Default target = project .claude/ (copy not symlink); --global → ~/.claude/.
# - Scaffold .sovereign/ from engine/templates/sovereign/ only if absent (non-destructive).
# - --quick = Fast Lane 5 skill dirs; --full = all packaged skill dirs. Logic copies
#   whatever skill dirs are present in the package (skills land in Phases 3-4).
# - Idempotent + version-aware via the VERSION file (currently 2.0.0): on re-run detect
#   existing install, diff versions, update cleanly; never clobber user-modified .sovereign/.

<interfaces>
<!-- Engine helpers the installer reuses — DO NOT reimplement these. -->

From engine/bin/lib/core.cjs:
```js
function safeReadFile(p): string | null            // utf-8 read, null on failure
function output(result, raw, rawValue): void        // JSON to stdout, @file: spill >50KB
function error(message): never                        // stderr + exit(1)
```

From engine/bin/sovereign.cjs (CURRENT Phase-1 STUB — replace main()):
```js
// reads ../VERSION and prints "sovereign v{version} — installer is Phase 2"
// keep the `#!/usr/bin/env node` shebang and `module.exports = { main }`.
```

Packaged seed tree (already shipped, verified present):
```
engine/templates/sovereign/{MANIFEST.md,SOVEREIGN.md,CONTEXT.md,STATE.md,config.json,
  council/.gitkeep, external-docs/.gitkeep, extensions/.gitkeep,
  docs/{adr,api,specs,security,infra,intersections}/.gitkeep}
```

The Fast Lane 5 skill names (for --quick filtering; dirs do not exist yet — copy only those present):
  ubiquitous-language, grill-with-docs, handoff, sentinel, tdd
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: install.cjs — target resolution, recursive copy, scaffold, version diff</name>
  <read_first>
    - engine/bin/lib/install.cjs (the file being created — confirm absent first)
    - engine/bin/lib/core.cjs (safeReadFile, output, error — reuse, do not reimplement)
    - engine/bin/lib/init.cjs (style reference: zero-dep node:fs/node:path, JSDoc // @ts-check header)
    - engine/templates/sovereign (the seed tree to copy)
    - engine/test/init.test.cjs (test-fixture style: mkdtemp, copy template, spawnSync the bin)
  </read_first>
  <behavior>
    - runInstall(opts) with opts {cwd, target:'project'|'global', mode:'quick'|'full', packageRoot}:
      - target 'project' → installRoot = path.join(cwd, '.claude'); 'global' → path.join(os.homedir(), '.claude').
      - Copies packaged skill dirs from <packageRoot>/skills/ into <installRoot>/skills/<name>/ ; mode 'quick'
        filters to the Fast Lane 5 names, 'full' copies all present. (Package has no skills/ yet → 0 skills, clean.)
      - Copies all packaged agent files from <packageRoot>/agents/*.md (i.e. engine/agents/ — the canonical
        packed location set in plan 02-02) into <installRoot>/agents/<name>.md. If <packageRoot>/agents/ is
        absent (02-02 not yet executed) → agents_copied:[] cleanly, no throw.
      - Scaffolds <cwd>/.sovereign/ from <packageRoot>/templates/sovereign/ ONLY when .sovereign/ is absent.
        When present, leaves user content untouched (non-destructive).
      - Version-aware: reads <packageRoot>/VERSION and <installRoot>/.sovereign-version (the stamp the
        installer writes). On re-run with equal versions → status 'up_to_date', no skill/agent overwrite of
        identical files. On newer packaged VERSION → status 'updated', re-copies skills/agents, rewrites stamp.
      - Returns {ok:true, target, mode, installed_version, previous_version|null, status:'installed'|'updated'|'up_to_date',
        skills_copied:[...names], agents_copied:[...names], sovereign_scaffolded:boolean}.
    - Test 1: fresh tmpdir, mode 'full' → status 'installed', .sovereign/STATE.md exists, .claude/agents has the 4 agent files, sovereign_scaffolded true.
    - Test 2: re-run identical → status 'up_to_date', sovereign_scaffolded false, existing .sovereign/ untouched (write a sentinel file into .sovereign/, assert it survives).
    - Test 3: mode 'quick' → skills_copied ⊆ FastLane5 (currently empty since no skill dirs ship yet); agents still copied.
    - Test 4: bump <installRoot>/.sovereign-version to '1.0.0' then re-run → status 'updated', stamp rewritten to packaged VERSION.
  </behavior>
  <action>
Create engine/bin/lib/install.cjs (`// @ts-check` + `'use strict'` header like the other lib modules).

Use ONLY node:fs / node:path / node:os (zero-dependency — ADR-002). Reuse safeReadFile/output/error from core.cjs; do not add dependencies.

Implement a `copyDirRecursive(src, dest)` helper with `fs.cpSync(src, dest, {recursive:true})` (Node >=20 — engines.node>=20 is pinned). Implement `runInstall(opts)` exactly per the behavior block above, and `cmdInstall(cwd, {quick, full, global}, raw)` that:
  - resolves mode: `full` flag → 'full'; `quick` flag → 'quick'; default 'full' (init with no mode = full M1 set per INIT-02).
  - resolves target: `global` flag → 'global' else 'project'.
  - computes packageRoot as path.join(__dirname, '..', '..') (engine/ root, where VERSION + templates/ live).
  - calls runInstall, then output(result, raw, result.status).

Define the Fast Lane 5 list as a module const `FAST_LANE = ['ubiquitous-language','grill-with-docs','handoff','sentinel','tdd']`.

The version stamp lives at `<cwd>/.sovereign/.sovereign-version` (inside .sovereign so it travels with committed state). Read packaged VERSION via safeReadFile(path.join(packageRoot,'VERSION')).trim().

export { runInstall, cmdInstall, FAST_LANE }.

Then create engine/test/install.test.cjs covering Tests 1-4 above using node:test + node:assert/strict, fs.mkdtempSync(os.tmpdir()) fixtures, calling runInstall directly (unit) with packageRoot = engine root. Clean up tmpdirs in each test. Skip-free (no npm needed — pure fs).
  </action>
  <verify>
    <automated>cd /Users/wizzie/conductor/workspaces/sovereign/seville/engine && node --test "test/install.test.cjs"</automated>
  </verify>
  <done>install.test.cjs passes all 4 cases; install.cjs exports runInstall/cmdInstall/FAST_LANE; zero new dependencies (engine/package.json dependencies stays {}).</done>
</task>

<task type="auto">
  <name>Task 2: Wire sovereign.cjs launcher to route `init [--quick|--full|--global]`</name>
  <read_first>
    - engine/bin/sovereign.cjs (the Phase-1 stub being replaced — keep shebang + module.exports)
    - engine/bin/lib/install.cjs (cmdInstall created in Task 1)
    - engine/bin/sovereign-tools.cjs (arg-parsing style reference: parseNamedArgs over process.argv.slice(2))
    - engine/bin/lib/core.cjs (error helper for unknown subcommands)
  </read_first>
  <action>
Replace the body of engine/bin/sovereign.cjs (keep `#!/usr/bin/env node` line 1 and the `if (require.main === module)` + `module.exports` tail).

main() now:
  - reads `args = process.argv.slice(2)`; `command = args[0]`.
  - On `command === 'init'`: parse boolean flags `--quick`, `--full`, `--global` (inline `args.includes('--quick')` etc. — no need to import the engine's parseNamedArgs, but you may require it from sovereign-tools.cjs if cleaner). Resolve cwd = process.cwd(). Call `cmdInstall(cwd, {quick, full, global}, false)` from require('./lib/install.cjs').
  - On no command OR `--version`/`version`: keep the existing VERSION print (read ../VERSION, print `sovereign v{version}`).
  - On unknown command: call error(`Unknown command: ${command} (expected: init [--quick|--full|--global])`) from core.cjs.

Keep zero-dependency (node:fs/node:path + the two lib requires only). Update the file's top doc comment to state it is now the real installer (no longer a stub).

Add to engine/test/install.test.cjs an integration test: spawnSync('node', [path-to-sovereign.cjs,'init','--full'], {cwd: tmpdir}) exits 0 and creates tmpdir/.sovereign/STATE.md + tmpdir/.claude/agents/sovereign-advisor.md. (Guard: skip this case if the agent files do not yet exist on disk — they ship in plan 02-02; assert only .sovereign scaffold in that case. Prefer asserting .sovereign scaffold unconditionally so this test is order-independent.)
  </action>
  <verify>
    <automated>cd /Users/wizzie/conductor/workspaces/sovereign/seville/engine && node bin/sovereign.cjs --version && node --test "test/install.test.cjs"</automated>
  </verify>
  <done>`node bin/sovereign.cjs init --full` in a temp dir scaffolds .sovereign/; `node bin/sovereign.cjs --version` prints the version; unknown commands exit non-zero; install.test.cjs (incl. the spawnSync case) passes.</done>
</task>

</tasks>

<verification>
- `node --test "test/install.test.cjs"` passes (idempotency, --quick/--full, version-aware, launcher integration).
- Full engine suite still green: `node --test "test/**/*.test.cjs"`.
- `engine/package.json` `dependencies` remains `{}` (zero-dep invariant).
- A fresh tmpdir + `node bin/sovereign.cjs init` produces a complete `.sovereign/` tree; second run is non-destructive.
</verification>

<success_criteria>
- INIT-01: `npx sovereign init` (via `node bin/sovereign.cjs init`) installs skills/agents into `.claude/` and scaffolds `.sovereign/`.
- INIT-02: `--quick` filters to Fast Lane 5; `--full` (and bare `init`) installs the full set.
- INIT-03: re-run is idempotent + version-aware (status up_to_date | updated; user `.sovereign/` content preserved).
- Installer stays zero-dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bootstrap-subagents/02-01-SUMMARY.md`.
</output>
