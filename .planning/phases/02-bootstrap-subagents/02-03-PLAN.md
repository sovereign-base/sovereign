---
phase: 02-bootstrap-subagents
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - engine/bin/lib/init.cjs
  - engine/bin/lib/doctor.cjs
  - engine/bin/sovereign-tools.cjs
  - engine/test/init.test.cjs
  - engine/test/doctor.test.cjs
autonomous: true
requirements: [SKL-07, INIT-01]
must_haves:
  truths:
    - "init <workflow> returns a REAL agents_installed/missing_agents result computed from the filesystem, not a hardcoded true/[]"
    - "When a required subagent .md file is absent from the install location, missing_agents lists it and agents_installed is false"
    - "`sovereign-tools doctor` enumerates installed SOVEREIGN skills, counts the auto-triggerable ones (without disable-model-invocation: true), and warns when the auto-triggerable count or description-char budget is exceeded"
    - "With no skills installed, doctor reports a clean budget result (the mechanism exists for Phases 3-4 to satisfy)"
  artifacts:
    - path: "engine/bin/lib/doctor.cjs"
      provides: "Budget-check command: skill enumeration, auto-triggerable count, description-char budget, threshold warnings"
      min_lines: 60
      exports: ["cmdDoctor", "checkBudget"]
    - path: "engine/bin/lib/init.cjs"
      provides: "Real agents_installed/missing_agents check replacing the Phase-1 hardcoded stub"
      contains: "missing_agents"
    - path: "engine/bin/sovereign-tools.cjs"
      provides: "Router case dispatching `doctor` to cmdDoctor"
      contains: "doctor"
  key_links:
    - from: "engine/bin/lib/init.cjs"
      to: "engine/agents/*.md (installed at .claude/agents/ or ~/.claude/agents/)"
      via: "fs.existsSync probe per required agent name in withProjectContext"
      pattern: "agents.*existsSync|existsSync.*agents"
    - from: "engine/bin/sovereign-tools.cjs"
      to: "engine/bin/lib/doctor.cjs"
      via: "require + 'doctor' switch case"
      pattern: "doctor"
---

<objective>
Replace the Phase-1 hardcoded `agents_installed: true` / `missing_agents: []` stub in `init.cjs` with a real filesystem check (per workflow's required agents), and add the `sovereign-tools doctor` budget-check command that enforces the skill-listing budget convention (SKL-07).

Purpose: The architecture's anti-pattern #4 (silent fallback to general-purpose agents) is prevented only if `init` actually reports missing subagents — Council must hard-error before dispatching into a void. And the listing-budget mechanism must exist now (reporting clean with zero skills) so Phases 3-4 satisfy it rather than discover the cap too late.
Output: real agent-presence guard in `init.cjs` + new `engine/bin/lib/doctor.cjs` + router wiring + tests. The `disable-model-invocation` convention is recorded as a short reference (feeds CONV-01 in Phase 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-bootstrap-subagents/02-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md

# Decisions this plan implements (from 02-CONTEXT.md):
# - Real agents_installed check: given the required agent names for the requested
#   workflow, verify <name>.md exists in the install location (.claude/agents/ or
#   ~/.claude/agents/); populate missing_agents, set agents_installed. No silent
#   general-purpose fallback.
# - doctor: enumerate installed skills, count auto-triggerable (WITHOUT
#   disable-model-invocation: true), sum name+description chars; warn if
#   auto-triggerable count > ~7 OR descriptions approach Claude Code's ~1% listing
#   budget. With no skills it reports clean.
# - Convention: orchestrator-only / side-effecting skills set disable-model-invocation: true.

<interfaces>
<!-- CURRENT stub in init.cjs (engine/bin/lib/init.cjs, withProjectContext) — REPLACE: -->
```js
function withProjectContext(cwd, blob) {
  return Object.assign(
    { project_root: cwd, sovereign_version: readVersion() },
    blob,
    { agents_installed: true,  // TODO(Phase 2): real agent presence check
      missing_agents: [] }
  );
}
```

<!-- The 4 required agent names that exist as files after plan 02-02: -->
  sovereign-advisor, sovereign-chairman, sovereign-peer-reviewer, sovereign-sentinel

<!-- Per-workflow required-agents mapping (council dispatches all reasoning agents;
     fast-lane 'sentinel' dispatches sovereign-sentinel; sovereign-init dispatches none): -->
  council        → [sovereign-advisor, sovereign-chairman, sovereign-peer-reviewer]
  sentinel       → [sovereign-sentinel]
  sovereign-init → []
  (other fast-lane stubs) → []

<!-- Install locations to probe (STACK.md): project .claude/agents/ first, then ~/.claude/agents/.
     Subagents are NOT loaded from --add-dir, so these two are the real locations. -->

From engine/bin/lib/core.cjs (reuse): safeReadFile, output, error.
From engine/bin/lib/validate.cjs (reference its parseFrontmatter + walkSkillFiles patterns for doctor's skill enumeration).
From engine/bin/sovereign-tools.cjs runCommand switch: add a `case 'doctor'` alongside the existing cases.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Real agents_installed / missing_agents check in init.cjs</name>
  <read_first>
    - engine/bin/lib/init.cjs (withProjectContext stub + buildInit switch — the file being modified)
    - engine/bin/lib/core.cjs (safeReadFile; no new deps)
    - engine/test/init.test.cjs (existing init assertions — extend, do not break)
    - engine/agents/ (the four installed agent files this check looks for; from plan 02-02)
  </read_first>
  <behavior>
    - Add `requiredAgentsFor(workflow)` returning the per-workflow agent-name list (council → [advisor,chairman,peer_reviewer]; sentinel → [sentinel]; else []).
    - Add `checkAgents(cwd, workflow)` returning {agents_installed:boolean, missing_agents:string[]}:
      probe for `<name>.md` under cwd/.claude/agents/ then ~/.claude/agents/ (found in EITHER = present);
      missing_agents = required names with no file in either location; agents_installed = missing_agents.length===0.
      Empty required list → {agents_installed:true, missing_agents:[]}.
    - withProjectContext takes the workflow (thread it through buildInit) and merges the real check result
      instead of the hardcoded {true, []}.
    - Test A: council workflow in a fixture whose .claude/agents/ has all three required files →
      agents_installed true, missing_agents [].
    - Test B: council fixture missing sovereign-peer-reviewer.md → agents_installed false,
      missing_agents ['sovereign-peer-reviewer'].
    - Test C: sovereign-init workflow (no required agents) → agents_installed true, missing_agents [] even with empty .claude/.
  </behavior>
  <action>
Edit engine/bin/lib/init.cjs (keep `// @ts-check`, zero-dep, node:fs/node:path only):

1) Add `requiredAgentsFor(workflow)` per the interfaces mapping (a small const map keyed by workflow; default []).
2) Add `checkAgents(cwd, workflow)`: for each required name, check fs.existsSync(path.join(cwd,'.claude','agents',name+'.md')) OR fs.existsSync(path.join(os.homedir(),'.claude','agents',name+'.md')). Build missing_agents + agents_installed. (require node:os.)
3) Change `withProjectContext(cwd, blob)` → `withProjectContext(cwd, workflow, blob)`; replace the hardcoded `{agents_installed:true, missing_agents:[]}` with `checkAgents(cwd, workflow)`. Update the single call site in buildInit to pass `workflow`.
4) Export checkAgents + requiredAgentsFor for direct unit testing.

Then EXTEND engine/test/init.test.cjs with Tests A/B/C above (seed .claude/agents/ fixtures via mkdtemp + fs.writeFileSync of minimal `<name>.md` files). Do NOT break existing init assertions (the existing greenfield tests run with no .claude/agents and use workflows with no required agents — confirm they still pass).
  </action>
  <verify>
    <automated>cd /Users/wizzie/conductor/workspaces/sovereign/seville/engine && node --test "test/init.test.cjs"</automated>
  </verify>
  <done>init.cjs no longer contains the hardcoded `agents_installed: true` stub; checkAgents/requiredAgentsFor exported; Tests A/B/C pass; existing init tests still green; the `// TODO(Phase 2)` comment is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: doctor.cjs budget-check command + router wiring + convention reference</name>
  <read_first>
    - engine/bin/lib/validate.cjs (walkSkillFiles + parseFrontmatter — reuse the patterns for skill enumeration)
    - engine/bin/lib/core.cjs (output, safeReadFile)
    - engine/bin/sovereign-tools.cjs (runCommand switch — where the `doctor` case is added)
    - engine/templates/sovereign (no skills yet — confirms doctor must report clean on zero skills)
  </read_first>
  <behavior>
    - checkBudget(cwd) enumerates installed SOVEREIGN skills (SKILL.md files under cwd/.claude/skills/ and cwd/skills/),
      parses each frontmatter, and computes:
        - auto_triggerable: skills WITHOUT `disable-model-invocation: true`
        - auto_count = auto_triggerable.length
        - desc_chars = sum of (name.length + description.length) over auto_triggerable
        - listing_token_estimate = ceil(desc_chars / 4)
      Thresholds (Claude's discretion per CONTEXT — use these defaults): AUTO_MAX = 7; TOKEN_BUDGET = 2000
      (≈1% of a 200k window). warnings[] gets an entry when auto_count > AUTO_MAX or listing_token_estimate > TOKEN_BUDGET.
      Returns {ok:boolean (no warnings), total_skills, auto_count, disabled_count, desc_chars,
        listing_token_estimate, auto_max:AUTO_MAX, token_budget:TOKEN_BUDGET, warnings:string[]}.
    - Zero skills → {ok:true, total_skills:0, auto_count:0, warnings:[]} (clean).
    - cmdDoctor(cwd, raw) = output(checkBudget(cwd), raw, result.ok ? 'ok' : 'warn'); exit(1) only if warnings present
      (so CI/install can gate). NOTE: zero-skill clean state must exit 0.
    - Test 1: empty fixture → ok true, total_skills 0, exit-0 semantics (call checkBudget directly).
    - Test 2: fixture with 8 auto-triggerable SKILL.md (no disable-model-invocation) → warnings includes the count breach, ok false.
    - Test 3: fixture with skills that all set `disable-model-invocation: true` → auto_count 0, ok true (orchestrator-only skills don't count).
  </behavior>
  <action>
1) Create engine/bin/lib/doctor.cjs (`// @ts-check` + `'use strict'`). Reuse a frontmatter parser + a skill-file walk (either require the relevant helpers from validate.cjs if exported, or inline minimal copies — keep zero-dep, node:fs/node:path only). Implement checkBudget(cwd) and cmdDoctor(cwd, raw) exactly per the behavior block. Define AUTO_MAX=7, TOKEN_BUDGET=2000 as module consts. export { checkBudget, cmdDoctor, AUTO_MAX, TOKEN_BUDGET }.
2) Wire engine/bin/sovereign-tools.cjs: require cmdDoctor from './lib/doctor.cjs' at the top, add `case 'doctor': cmdDoctor(cwd, raw); break;` to runCommand's switch, and add 'doctor' to the usage string. (Note: the existing --pick interceptor wraps runCommand — doctor output flows through output() so --pick works for free.)
3) Create engine/test/doctor.test.cjs covering Tests 1-3 (mkdtemp fixtures writing minimal SKILL.md files with/without `disable-model-invocation: true`). Clean up tmpdirs.
4) Add a short convention reference at engine/agents/CONVENTIONS-listing-budget.md OR (preferred) a `references/listing-budget.md` at repo root documenting: orchestrator-only/side-effecting skills MUST set `disable-model-invocation: true` to stay out of the listing budget; only genuinely auto-triggerable skills remain invocable; the AUTO_MAX≈7 + ~1% token budget enforced by `sovereign-tools doctor`. Keep it under ~30 lines (it feeds CONV-01 in Phase 5).
  </action>
  <verify>
    <automated>cd /Users/wizzie/conductor/workspaces/sovereign/seville/engine && node bin/sovereign-tools.cjs doctor && node --test "test/doctor.test.cjs"</automated>
  </verify>
  <done>`sovereign-tools doctor` runs and reports clean on the current zero-skill repo (exit 0); doctor.test.cjs passes all 3 cases; the router has a `doctor` case; the listing-budget convention reference exists.</done>
</task>

</tasks>

<verification>
- `node --test "test/init.test.cjs"` and `node --test "test/doctor.test.cjs"` pass.
- Full engine suite green: `node --test "test/**/*.test.cjs"`.
- `node bin/sovereign-tools.cjs init council` against a project missing a required agent reports `agents_installed: false` with the missing name (no silent true).
- `node bin/sovereign-tools.cjs doctor` exits 0 with zero skills installed.
- Engine stays zero-dependency.
</verification>

<success_criteria>
- INIT-01 (guard half): the agents_installed/missing_agents hard-error path is real (filesystem check), replacing the Phase-1 stub — no silent general-purpose fallback.
- SKL-07: a `/doctor`-style budget check exists, counts auto-triggerable skills, enforces the ~7 / ~1% listing budget, and reports clean at zero skills; the disable-model-invocation convention is documented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bootstrap-subagents/02-03-SUMMARY.md`.
</output>
