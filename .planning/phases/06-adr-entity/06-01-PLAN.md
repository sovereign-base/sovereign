---
phase: 06-adr-entity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [engine/skills/adr-log/SKILL.md]
autonomous: true
requirements: [ARCH-01, ARCH-08]
must_haves:
  truths:
    - "Running adr-log records a decision to .sovereign/docs/adr/NNNN-slug.md, sequentially numbered (highest existing NNNN + 1)"
    - "The ADR is written in the minimal adr-format.md form (context + decision + why in 1-3 sentences; Status/Options/Consequences only when they add value)"
    - "The skill applies the three-condition gate (hard-to-reverse + surprising-without-context + a real trade-off) and DECLINES decisions that fail it, advising CONTEXT.md/the plan instead"
    - "The skill is a thin orchestrator: orients via one `init adr-log` call, opens with `## Why this matters`, asks recommendation-first, ends with a navigation footer, and sets disable-model-invocation: true"
    - "adr-log owns ADR numbering + gate + writing in one place — the skill other skills point users to for ADR-worthy choices"
  artifacts:
    - path: "engine/skills/adr-log/SKILL.md"
      provides: "The canonical gated ADR recording skill implementing adr-format.md (numbering + 3-condition gate + minimal form + docs/adr write)"
      min_lines: 70
      contains: "disable-model-invocation: true"
  key_links:
    - from: "engine/skills/adr-log/SKILL.md"
      to: "sovereign-tools init adr-log"
      via: "single init orient call with @file: spill guard"
      pattern: "init adr-log"
    - from: "engine/skills/adr-log/SKILL.md"
      to: ".sovereign/docs/adr/"
      via: "writes NNNN-slug.md after the gate passes"
      pattern: "docs/adr"
    - from: "engine/skills/adr-log/SKILL.md"
      to: "sovereign-tools commit"
      via: "delegates persistence (state save + commit) to the engine"
      pattern: "commit"
---

<objective>
Hand-author `engine/skills/adr-log/SKILL.md` — the canonical, gated way to record an architectural decision. It implements `engine/references/adr-format.md` verbatim: scan `.sovereign/docs/adr/` for the next sequential number, apply the three-condition gate (declining decisions that fail), write the minimal ADR form to `.sovereign/docs/adr/NNNN-slug.md`, then delegate state save + commit to the engine. It is the single owner of ADR numbering + gate + writing — the skill every other M2 skill (entity-design, grill-with-docs, api/stack/scale/security/deploy) points users to for ADR-worthy choices.

Purpose: Establish the ADR-recording artifact every later M2 skill composes with. Closes ARCH-01 and (for this skill) ARCH-08.
Output: One shipped skill (`engine/skills/adr-log/SKILL.md`; `engine/skills/` ships via the package `files` allowlist; `--full` install only, NOT in FAST_LANE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-adr-entity/06-CONTEXT.md

# The spec this skill implements verbatim:
@engine/references/adr-format.md
# The authoring standard both M2 skills follow:
@engine/references/skill-format.md
# Patterns to MIRROR (shape, not content):
@engine/skills/grill-with-docs/SKILL.md
@engine/skills/council/SKILL.md
# The init <skill> default blob this skill consumes:
@engine/bin/lib/init.cjs

<facts>
<!-- Verified against the codebase — author against these, do not re-derive. -->

THE `init adr-log` BLOB (default case in engine/bin/lib/init.cjs):
- `adr-log` is NOT 'council' or 'sovereign-init', so it hits the DEFAULT case in buildInit().
- It returns: `models: {}`, `config` (orientation subset), `phase`, and
  `context_injection: { manifest_path: ".sovereign/MANIFEST.md", glossary_path: ".sovereign/CONTEXT.md" }`,
  `paths: { state: ".sovereign/STATE.md", manifest: ".sovereign/MANIFEST.md" }`, `exists`.
- NOTE: the default case does NOT include `relevant_adrs`. adr-log therefore SCANS
  `.sovereign/docs/adr/` ITSELF (readdir → highest NNNN + 1) to find the next number — this is
  exactly the numbering rule adr-format.md states ("Scan the directory for the highest number and increment").
- The skill performs ZERO other orientation reads beyond this one `init` call.

THE @file: SPILL GUARD (every skill uses it verbatim):
  INIT=$(node "$ENGINE/bin/sovereign-tools.cjs" init adr-log)
  if [[ "$INIT" == @file:* ]]; then INIT=$(cat "${INIT#@file:}"); fi

THE adr-format.md RULES TO IMPLEMENT (read engine/references/adr-format.md):
- Location & numbering: `.sovereign/docs/adr/`, sequential `0001-slug.md`, `0002-slug.md`, …
  (four-digit zero-padded NNNN). Scan for highest + 1. Create the dir lazily (first ADR only).
- The minimal form: one-to-three sentences — context + decision + why. The value is recording
  THAT a decision was made and WHY, not filling a template. Optional Status / Considered options /
  Consequences sections ONLY when they genuinely add value (most ADRs need none).
- The three-condition gate — offer/record an ADR ONLY when ALL THREE are true:
  (1) hard to reverse, (2) surprising without context, (3) the result of a real trade-off.
  If any condition is missing → DECLINE: advise recording it in CONTEXT.md or the plan instead
  (don't pad the ADR log — "a directory full of trivial ADRs is as useless as none").
- What qualifies (cite a couple): architectural shape, integration patterns between contexts,
  technology choices that carry lock-in (not every library), boundary/scope decisions incl. explicit
  no's, deliberate deviations from the obvious path, constraints not visible in the code.

ARCH-08 THIN-ORCHESTRATOR SHAPE (per skill-format.md; both M2 skills):
- Real Agent Skills frontmatter ONLY: `name`, `description` (lead with the use-case), `disable-model-invocation: true`.
- NO v1 fields: triggers, works-best-with, min-model, tokens, bare phase.
- `disable-model-invocation: true` → user-only (/adr-log), side-effecting, OFF the auto-trigger
  listing budget so `doctor` stays at the 5 Fast Lane skills.
- One `init adr-log` orient call (+ @file: spill); reads file content only from init-handed paths.
- Required body sections in order: `## Why this matters` (plain language) → `## When to use this`
  (with anti-uses) → the numbered flow → a recommendation-first navigation footer.
- All state/commit delegated to `sovereign-tools` (state save, commit) — never hand-write state
  or branch on commit_docs.

INSTALL / BUDGET:
- adr-log is `--full` only, NOT added to install.cjs FAST_LANE — it must NOT become auto-triggerable.
- After this phase, `doctor` must still report the auto-trigger budget at the 5 Fast Lane skills.

VERIFY (the M1 verifiers flagged it): `validate skills` is a NO-OP without an explicit path.
The automated verify MUST pass the explicit file path:
  node engine/bin/sovereign-tools.cjs validate skills engine/skills/adr-log/SKILL.md
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author engine/skills/adr-log/SKILL.md</name>
  <files>engine/skills/adr-log/SKILL.md</files>
  <read_first>
    - engine/references/adr-format.md — the location/numbering/minimal-form/three-condition-gate spec this skill implements VERBATIM (the source of truth for the flow).
    - engine/references/skill-format.md — the thin-orchestrator authoring standard (real frontmatter, single-init-load rule, required body sections, dropped v1 fields, listing budget).
    - engine/skills/council/SKILL.md — mirror the `disable-model-invocation: true` frontmatter + the explanatory HTML comment + the `init` orient step + the `▶ NEXT` recommendation-first nav footer.
    - engine/skills/grill-with-docs/SKILL.md — mirror the conversational/recommendation-first body shape, the "offer ADRs only when all three hold" gate phrasing, and the delegate-writes-to-the-engine pattern.
    - engine/bin/lib/init.cjs (default case + contextInjection) — confirm what `init adr-log` actually returns (models:{}, context_injection.{manifest_path,glossary_path}, paths.state) and that adr-log must scan `.sovereign/docs/adr/` itself for numbering (relevant_adrs is NOT in the default blob).
  </read_first>
  <action>
    Hand-author `engine/skills/adr-log/SKILL.md`. Use the `<facts>` block as the source of truth; mirror council/grill-with-docs shape exactly. Sections in order:

    FRONTMATTER (real Agent Skills fields only):
    ```yaml
    ---
    name: adr-log
    description: Record an architectural decision that's hard to reverse, surprising without context, and the result of a real trade-off. Gates out trivial decisions, numbers and writes the ADR in minimal form to .sovereign/docs/adr/. Use when a consequential, non-obvious choice gets locked.
    disable-model-invocation: true
    argument-hint: "\"<decision>\""
    ---
    ```
    Add an HTML comment below it (mirroring council's) explaining: disable-model-invocation: true keeps adr-log user-invoked (/adr-log) and side-effecting (writes ADRs + commits), OFF the listing auto-trigger budget — it is `--full` only, not a Fast Lane skill.

    `## Why this matters` — plain language (for someone building their first real product): the decisions that bite hardest are the ones nobody wrote down — six months later a reader stares at the code and asks "why on earth did they do it this way?" An ADR is one-to-three sentences that answer that, forever. But a log full of trivial ADRs is as useless as none — so this skill GATES: it only records the decisions that are hard to reverse, surprising, and a real trade-off, and politely declines the rest.

    `## When to use this` — Use it when a consequential architectural choice is locked: architectural shape, integration patterns between contexts, technology choices that carry lock-in (a database/message bus/auth provider — not every library), boundary/scope decisions including explicit no's, a deliberate deviation from the obvious path, a constraint not visible in the code. Anti-uses ("Don't use it for…"): easily-reversed choices (you'll just reverse them), unsurprising defaults, choices with no real alternative, or routine library picks — record those in CONTEXT.md or the plan instead.

    `## The flow` — a thin orchestrator: orient once, gate, then write; delegate persistence to `sovereign-tools`. Numbered steps:
      1 — Orient (one call). Show the exact bash with the @file: spill guard for `init adr-log`. State: parse `context_injection.glossary_path` and `paths.state` from the blob; the skill performs no other orientation reads.
      2 — Apply the three-condition gate. State the decision in neutral terms, then check ALL THREE: (1) hard to reverse, (2) surprising without context, (3) the result of a real trade-off. If ANY is missing → DECLINE: tell the user this isn't ADR-worthy and advise recording it in CONTEXT.md (a term) or the plan instead — do NOT write an ADR. Only continue when all three hold.
      3 — Determine the next number. Scan `.sovereign/docs/adr/` for existing `NNNN-*.md`, take the highest NNNN and add 1 (start at 0001 if empty); zero-pad to four digits. Derive a short kebab-case slug from the decision. Create the `adr/` directory lazily if it doesn't exist (first ADR only).
      4 — Write the ADR in minimal form. Write `.sovereign/docs/adr/NNNN-slug.md`: a `# NNNN-slug` heading + one-to-three sentences (context + decision + why). Add optional `Status` / `Considered options` / `Consequences` sections ONLY when they genuinely add value (most need none) — do not pad a template.
      5 — Persist (delegate to the engine). `node "$ENGINE/bin/sovereign-tools.cjs" state save` (regenerates MANIFEST), then `node "$ENGINE/bin/sovereign-tools.cjs" commit "adr: NNNN-slug — <short summary>" --files .sovereign/docs/adr/NNNN-slug.md <paths.state> <paths.manifest>`. Note commit is gated by the engine on commit_docs + gitignore and sanitizes the message — the skill does NOT branch on those itself.

    Include a short minimal-ADR example block mirroring adr-format.md's (e.g. a `# 0007-paystack-over-flutterwave` two-sentence ADR) so the executor sees the target form.

    `## Navigation` — a recommendation-first `▶ NEXT` footer: after a recorded ADR, recommend continuing the design (e.g. `/entity-design` or `/grill-with-docs`); after a DECLINED decision, recommend `/ubiquitous-language` to capture the term in CONTEXT.md instead. Always show where the ADR landed (the `.sovereign/docs/adr/NNNN-slug.md` path).

    Keep it tight (~70-130 lines), prose tone matching council/grill-with-docs. This is authored prose verified by grep + `validate skills`.
  </action>
  <verify>
    <automated>test -f engine/skills/adr-log/SKILL.md && grep -q "disable-model-invocation: true" engine/skills/adr-log/SKILL.md && grep -q "init adr-log" engine/skills/adr-log/SKILL.md && grep -q "docs/adr" engine/skills/adr-log/SKILL.md && grep -qi "three" engine/skills/adr-log/SKILL.md && grep -qi "gate" engine/skills/adr-log/SKILL.md && grep -qi "decline" engine/skills/adr-log/SKILL.md && grep -q "@file:" engine/skills/adr-log/SKILL.md && grep -q "Why this matters" engine/skills/adr-log/SKILL.md && grep -q "commit" engine/skills/adr-log/SKILL.md && grep -qiv "triggers:" engine/skills/adr-log/SKILL.md && node engine/bin/sovereign-tools.cjs validate skills engine/skills/adr-log/SKILL.md && echo PASS</automated>
  </verify>
  <done>
    `engine/skills/adr-log/SKILL.md` exists (≥70 lines) with real Agent Skills frontmatter (`name: adr-log`, `description`, `disable-model-invocation: true`, no v1 fields), and contains: a single `init adr-log` orient call with the `@file:` spill guard; the three-condition gate with an explicit DECLINE path; sequential `NNNN` numbering by scanning `.sovereign/docs/adr/`; the minimal-form ADR write to `.sovereign/docs/adr/NNNN-slug.md`; engine-delegated `state save` + `commit`; `## Why this matters`; and a recommendation-first navigation footer. `node engine/bin/sovereign-tools.cjs validate skills engine/skills/adr-log/SKILL.md` passes.
  </done>
</task>

</tasks>

<verification>
- `engine/skills/adr-log/SKILL.md` exists and ships (under `engine/skills/`, in the package `files` allowlist) and is NOT added to install.cjs FAST_LANE (`--full` only).
- `validate skills <explicit path>` passes (the path is mandatory — the check is a no-op without it).
- Grep proves: `disable-model-invocation: true`; one `init adr-log` orient + `@file:` spill; the three-condition gate + DECLINE path; `docs/adr` write with `NNNN` numbering; engine-delegated commit; `## Why this matters`; nav footer; no v1 frontmatter.
- Spot-check the flow against adr-format.md — numbering, minimal form, and the gate must match the spec verbatim.
- `doctor` still reports the auto-trigger budget at the 5 Fast Lane skills (adr-log is disable-model-invocation, so off the count).
</verification>

<success_criteria>
ARCH-01 satisfied: running `adr-log` records a decision to `.sovereign/docs/adr/NNNN-slug.md` — sequentially numbered, in the minimal `adr-format.md` form — and the skill applies the three-condition gate, declining decisions that fail it. ARCH-08 satisfied for this skill: thin orchestrator (single `init` orient, recommendation-first, `## Why this matters`, nav footer), `disable-model-invocation: true`, `validate skills` passes, doctor budget held at 5.
</success_criteria>

<output>
After completion, create `.planning/phases/06-adr-entity/06-01-SUMMARY.md`
</output>
