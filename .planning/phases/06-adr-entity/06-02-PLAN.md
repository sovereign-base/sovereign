---
phase: 06-adr-entity
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [engine/skills/entity-design/SKILL.md]
autonomous: true
requirements: [ARCH-02, ARCH-08]
must_haves:
  truths:
    - "Running entity-design walks the user through entities, relationships, and bounded contexts one piece at a time, recommendation-first"
    - "The skill draws its entity vocabulary from the CONTEXT.md glossary and flags an undefined-but-needed term (suggesting /ubiquitous-language)"
    - "The domain model is recorded to .sovereign/docs/ENTITY_MODEL.md (entities + attributes, relationships/cardinality, bounded-context grouping)"
    - "For hard-to-reverse modeling choices the skill OFFERS adr-log rather than re-implementing ADR numbering/writing — the two compose"
    - "The skill is a thin orchestrator: orients via one `init entity-design` call, opens with `## Why this matters`, recommendation-first one-piece-at-a-time, ends with a navigation footer, and sets disable-model-invocation: true"
  artifacts:
    - path: "engine/skills/entity-design/SKILL.md"
      provides: "The glossary-driven, one-piece-at-a-time domain-modeling skill that writes ENTITY_MODEL.md and composes with adr-log"
      min_lines: 70
      contains: "disable-model-invocation: true"
  key_links:
    - from: "engine/skills/entity-design/SKILL.md"
      to: "sovereign-tools init entity-design"
      via: "single init orient call with @file: spill guard"
      pattern: "init entity-design"
    - from: "engine/skills/entity-design/SKILL.md"
      to: ".sovereign/CONTEXT.md glossary"
      via: "reads context_injection.glossary_path for the entity vocabulary"
      pattern: "glossary"
    - from: "engine/skills/entity-design/SKILL.md"
      to: ".sovereign/docs/ENTITY_MODEL.md"
      via: "records the domain model (entities, relationships, bounded contexts)"
      pattern: "ENTITY_MODEL.md"
    - from: "engine/skills/entity-design/SKILL.md"
      to: "adr-log"
      via: "offers ADR-worthy modeling choices to adr-log instead of writing ADRs itself (compose, don't duplicate)"
      pattern: "adr-log"
---

<objective>
Hand-author `engine/skills/entity-design/SKILL.md` — the domain-modeling skill that walks a user through entities, relationships, and bounded contexts one piece at a time, recommendation-first, DDD-aligned. It reads the `CONTEXT.md` glossary for its entity vocabulary (flagging undefined-but-needed terms → `/ubiquitous-language`), records the model to `.sovereign/docs/ENTITY_MODEL.md`, and for hard-to-reverse modeling choices OFFERS `adr-log` rather than re-implementing ADR recording (compose, don't duplicate). It stays at the model level — no schema/migration/code.

Purpose: Establish the domain-model artifact every later M2 skill (api/stack/scale/security/deploy) references, and prove the adr-log composition. Closes ARCH-02 and (for this skill) ARCH-08.
Output: One shipped skill (`engine/skills/entity-design/SKILL.md`; `--full` install only, NOT in FAST_LANE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-adr-entity/06-CONTEXT.md

# The skill this one COMPOSES WITH (offers ADR-worthy choices to it) — authored in 06-01:
@engine/skills/adr-log/SKILL.md
# The spec adr-log implements (entity-design references the same 3-condition gate when deciding what to offer):
@engine/references/adr-format.md
# The authoring standard both M2 skills follow:
@engine/references/skill-format.md
# Patterns to MIRROR (shape, not content): grill-with-docs reads CONTEXT.md + offers ADRs sparingly; council is the disable-model-invocation/init/nav-footer reference:
@engine/skills/grill-with-docs/SKILL.md
@engine/skills/council/SKILL.md
# The init <skill> default blob this skill consumes:
@engine/bin/lib/init.cjs

<facts>
<!-- Verified against the codebase — author against these, do not re-derive. -->

THE `init entity-design` BLOB (default case in engine/bin/lib/init.cjs):
- `entity-design` is NOT 'council'/'sovereign-init', so it hits the DEFAULT case in buildInit().
- It returns: `models: {}`, `config` (orientation subset), `phase`, and
  `context_injection: { manifest_path: ".sovereign/MANIFEST.md", glossary_path: ".sovereign/CONTEXT.md" }`,
  `paths: { state: ".sovereign/STATE.md", manifest: ".sovereign/MANIFEST.md" }`, `exists`.
- The glossary IS the entity vocabulary: read CONTENT of `context_injection.glossary_path`
  (.sovereign/CONTEXT.md) AFTER the init call — the terms there are the candidate entities/value
  objects. `exists.glossary` tells you if it's there.
- The skill performs ZERO other orientation reads beyond this one `init` call.

THE @file: SPILL GUARD (verbatim):
  INIT=$(node "$ENGINE/bin/sovereign-tools.cjs" init entity-design)
  if [[ "$INIT" == @file:* ]]; then INIT=$(cat "${INIT#@file:}"); fi

THE FLOW (per 06-CONTEXT decisions, DDD-aligned):
- Read the CONTEXT.md glossary → terms are the entity vocabulary. If a NEEDED term is undefined,
  flag it and suggest `/ubiquitous-language` to define it first (don't invent vocabulary silently).
- Walk the model ONE entity/relationship at a time, recommendation-first: propose the entity, its
  key attributes, its relationships + cardinality, and its bounded context; the user confirms/corrects.
  Ask ONE thing, wait, then continue — never batch (mirror grill-with-docs).
- Record the domain model to `.sovereign/docs/ENTITY_MODEL.md`: per-entity blocks (name + key
  attributes), a relationships section (with cardinality), and bounded-context grouping. Update the
  file in place on re-run (don't duplicate). The `.sovereign/docs/` root ships in the scaffold.
- COMPOSE, DON'T DUPLICATE: for hard-to-reverse modeling choices — aggregate boundaries,
  event-sourced vs CRUD, shared vs owned data — OFFER `adr-log` (point the user to `/adr-log`, or
  describe the decision for it). entity-design does NOT number or write ADRs itself; adr-log is the
  single owner of ADR recording (the 3-condition gate lives there).
- Stay at the MODEL level — no schema, migrations, or code (that's Construction).

ENTITY_MODEL.md LAYOUT (Claude's discretion per 06-CONTEXT; intended shape):
- a per-entity block (entity name + key attributes), a relationships section (cardinality), and a
  bounded-context grouping. Exact formatting is the executor's call — these three parts are the intent.

ARCH-08 THIN-ORCHESTRATOR SHAPE (per skill-format.md; identical to 06-01):
- Real Agent Skills frontmatter ONLY: `name`, `description` (lead with the use-case),
  `disable-model-invocation: true`. NO v1 fields (triggers/works-best-with/min-model/tokens/bare phase).
- disable-model-invocation: true → user-only (/entity-design), side-effecting, OFF the auto-trigger budget.
- One `init entity-design` orient call (+ @file: spill); reads content only from init-handed paths.
- Required body sections in order: `## Why this matters` → `## When to use this` (with anti-uses) →
  the numbered flow → a recommendation-first navigation footer.
- All state/commit delegated to `sovereign-tools` (state save, commit) — never hand-write state.

DEPENDENCY: this plan depends_on 06-01 (adr-log must exist so entity-design can OFFER it).
entity-design references `/adr-log` by name — it does not re-implement ADR numbering/writing.

INSTALL / BUDGET: entity-design is `--full` only, NOT in install.cjs FAST_LANE — must NOT be
auto-triggerable. After this phase, `doctor` must still report the auto-trigger budget at the 5
Fast Lane skills.

VERIFY: `validate skills` is a NO-OP without an explicit path. The automated verify MUST pass:
  node engine/bin/sovereign-tools.cjs validate skills engine/skills/entity-design/SKILL.md
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author engine/skills/entity-design/SKILL.md</name>
  <files>engine/skills/entity-design/SKILL.md</files>
  <read_first>
    - engine/skills/adr-log/SKILL.md (authored in 06-01) — the skill entity-design OFFERS for ADR-worthy choices; reference it by name (`/adr-log`), do NOT re-implement its numbering/gate/write.
    - engine/references/adr-format.md — the same three-condition gate entity-design uses to decide WHICH modeling choices are worth offering to adr-log (hard-to-reverse + surprising + real trade-off).
    - engine/references/skill-format.md — the thin-orchestrator standard (real frontmatter, single-init-load, required body sections, dropped v1 fields, listing budget).
    - engine/skills/grill-with-docs/SKILL.md — mirror the conversational one-question-at-a-time, recommendation-first shape; the read-CONTEXT.md-glossary pattern; the offer-ADRs-sparingly composition; delegate writes to the engine.
    - engine/skills/council/SKILL.md — the `disable-model-invocation: true` frontmatter + HTML comment + init orient + `▶ NEXT` nav-footer pattern.
    - engine/bin/lib/init.cjs (default case + contextInjection) — confirm `init entity-design` returns context_injection.glossary_path (.sovereign/CONTEXT.md) and paths.state; the glossary is the entity vocabulary.
  </read_first>
  <action>
    Hand-author `engine/skills/entity-design/SKILL.md`. Use the `<facts>` block as the source of truth; mirror grill-with-docs (conversational + glossary + offer-ADRs) and council (frontmatter + nav). Sections in order:

    FRONTMATTER (real Agent Skills fields only):
    ```yaml
    ---
    name: entity-design
    description: Model your domain one entity at a time — entities, attributes, relationships, and bounded contexts — drawing vocabulary from your CONTEXT.md glossary, recommendation-first. Records ENTITY_MODEL.md and offers hard-to-reverse modeling choices to /adr-log. Use before designing the API or the data layer.
    disable-model-invocation: true
    argument-hint: "\"<domain or feature>\""
    ---
    ```
    Add an HTML comment below it (mirroring council's): disable-model-invocation: true keeps entity-design user-invoked (/entity-design) and side-effecting (writes ENTITY_MODEL.md + commits), OFF the listing auto-trigger budget — it is `--full` only, not a Fast Lane skill.

    `## Why this matters` — plain language: a domain model built from fuzzy, shifting vocabulary produces an API and a database that argue with each other forever. Naming the entities, their relationships, and their bounded contexts BEFORE any schema is the cheapest place to be right. This skill walks the model one piece at a time, grounded in the words you already agreed on (the glossary), and records it so the API and data layer build on the same map.

    `## When to use this` — Use it before designing the API or the data layer, when a feature introduces new domain concepts, or when the model has drifted from the glossary. Anti-uses ("Don't use it for…"): writing schemas/migrations/code (that's Construction — this stays at the model level); naming individual terms (use `/ubiquitous-language` for the glossary first); recording a single locked decision (use `/adr-log`).

    `## The flow` — a thin orchestrator: orient once, read the glossary, walk the model one piece at a time, offer ADRs sparingly, delegate writes. Numbered steps:
      1 — Orient (one call). Show the exact bash with the @file: spill guard for `init entity-design`. Parse `context_injection.glossary_path` (.sovereign/CONTEXT.md) and `paths.state`; check `exists.glossary`. The skill performs no other orientation reads.
      2 — Read the glossary as the entity vocabulary. Read the CONTENT of the glossary path (only now). The defined terms are your candidate entities/value objects. If a term you NEED is undefined, STOP and flag it: recommend `/ubiquitous-language` to define it first — don't invent vocabulary silently.
      3 — Walk the model one piece at a time, recommendation-first. For each entity: propose the entity, its key attributes, its relationships + cardinality, and its bounded context — your recommendation FIRST with reasoning, then the user confirms or corrects. Ask ONE thing, wait, continue. Never batch. Keep walking until entities, relationships, and bounded contexts are all settled.
      4 — Offer ADRs sparingly (compose, don't duplicate). When a modeling choice is hard to reverse (aggregate boundaries, event-sourced vs CRUD, shared vs owned data) AND meets the three-condition gate (hard-to-reverse + surprising + real trade-off), OFFER `/adr-log` — point the user to it or describe the decision for it. Do NOT number or write ADRs here; adr-log owns that.
      5 — Record the model. Write/update `.sovereign/docs/ENTITY_MODEL.md`: a per-entity block (name + key attributes), a relationships section (with cardinality), and a bounded-context grouping. Update in place on re-run — don't duplicate.
      6 — Persist (delegate to the engine). `node "$ENGINE/bin/sovereign-tools.cjs" state save`, then `node "$ENGINE/bin/sovereign-tools.cjs" commit "entity-design: <short summary>" --files .sovereign/docs/ENTITY_MODEL.md <paths.state> <paths.manifest>`. commit is gated by the engine on commit_docs + gitignore and sanitizes the message — the skill does NOT branch on those itself.

    Optionally include a short recommendation-first example line (mirroring grill-with-docs' example) showing one entity proposed with its attributes/relationships/bounded context and an ADR offer.

    `## Navigation` — a recommendation-first `▶ NEXT` footer: after the model is recorded, recommend `/api-design` (design the contract over these entities) as the primary next step, with `/adr-log` (record an outstanding modeling decision) and `/ubiquitous-language` (define a missing term) as alternatives. Show where the model landed (`.sovereign/docs/ENTITY_MODEL.md`).

    Keep it tight (~70-140 lines), prose tone matching grill-with-docs/council. Authored prose verified by grep + `validate skills`.
  </action>
  <verify>
    <automated>test -f engine/skills/entity-design/SKILL.md && grep -q "disable-model-invocation: true" engine/skills/entity-design/SKILL.md && grep -q "init entity-design" engine/skills/entity-design/SKILL.md && grep -q "ENTITY_MODEL.md" engine/skills/entity-design/SKILL.md && grep -qi "glossary" engine/skills/entity-design/SKILL.md && grep -q "adr-log" engine/skills/entity-design/SKILL.md && grep -qi "bounded context" engine/skills/entity-design/SKILL.md && grep -q "@file:" engine/skills/entity-design/SKILL.md && grep -q "Why this matters" engine/skills/entity-design/SKILL.md && grep -q "commit" engine/skills/entity-design/SKILL.md && grep -qiv "min-model:" engine/skills/entity-design/SKILL.md && node engine/bin/sovereign-tools.cjs validate skills engine/skills/entity-design/SKILL.md && echo PASS</automated>
  </verify>
  <done>
    `engine/skills/entity-design/SKILL.md` exists (≥70 lines) with real Agent Skills frontmatter (`name: entity-design`, `description`, `disable-model-invocation: true`, no v1 fields), and contains: a single `init entity-design` orient call with the `@file:` spill guard; reading the CONTEXT.md glossary as the entity vocabulary (+ flag undefined terms → `/ubiquitous-language`); one-piece-at-a-time recommendation-first walking of entities/relationships/bounded contexts; OFFERING `/adr-log` for hard-to-reverse choices (no re-implemented ADR numbering); writing the model to `.sovereign/docs/ENTITY_MODEL.md`; engine-delegated `state save` + `commit`; `## Why this matters`; and a recommendation-first navigation footer. `node engine/bin/sovereign-tools.cjs validate skills engine/skills/entity-design/SKILL.md` passes.
  </done>
</task>

</tasks>

<verification>
- `engine/skills/entity-design/SKILL.md` exists and ships (under `engine/skills/`) and is NOT added to install.cjs FAST_LANE (`--full` only).
- `validate skills <explicit path>` passes (path is mandatory — no-op without it).
- Grep proves: `disable-model-invocation: true`; one `init entity-design` orient + `@file:` spill; glossary-driven vocabulary; `ENTITY_MODEL.md` write; bounded contexts; OFFERS `adr-log` (composition, not re-implementation); engine-delegated commit; `## Why this matters`; nav footer; no v1 frontmatter.
- Composition check: entity-design references `/adr-log` by name and does NOT contain its own `NNNN`-numbering/ADR-write logic (adr-log is the single owner — authored in 06-01).
- `doctor` still reports the auto-trigger budget at the 5 Fast Lane skills (entity-design is disable-model-invocation, so off the count).
</verification>

<success_criteria>
ARCH-02 satisfied: running `entity-design` walks the user through entities, relationships, and bounded contexts one piece at a time, drawing terms from the `CONTEXT.md` glossary and recording the model to `.sovereign/docs/ENTITY_MODEL.md`; it offers ADR-worthy modeling choices to `adr-log` rather than re-implementing decision recording (the two compose). ARCH-08 satisfied for this skill: thin orchestrator (single `init` orient, recommendation-first one-piece-at-a-time, `## Why this matters`, nav footer), `disable-model-invocation: true`, `validate skills` passes, doctor budget held at 5.
</success_criteria>

<output>
After completion, create `.planning/phases/06-adr-entity/06-02-SUMMARY.md`
</output>
