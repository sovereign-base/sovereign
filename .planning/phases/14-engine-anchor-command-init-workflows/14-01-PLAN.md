---
phase: 14-engine-anchor-command-init-workflows
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - engine/test/anchor.test.cjs
  - engine/bin/lib/anchor.cjs
  - engine/bin/sovereign-tools.cjs
  - engine/bin/lib/init.cjs
  - engine/test/init.test.cjs
autonomous: true
requirements: [ENG-09]

must_haves:
  truths:
    - "SC1: `sovereign-tools anchor add` writes `.sovereign/external-docs/<slug>.md` carrying source, version, date-retrieved, re-verify-by headers — URL-by-default, content stored only on opt-in."
    - "SC2: `anchor list` returns header-parsed anchors and `anchor check` flags every anchor with `re-verify-by < today` as stale (deterministic from stored dates); both return clean empty results on greenfield (no external-docs/ dir)."
    - "SC3: `init anchor-docs` and `init verify-self` each return one orient JSON blob (paths + config + exists) via core.cjs output() with @file: spill; verify-self additionally surfaces references/unverified-marker.md."
    - "SC4: `node --test` covers add/list/check (stale, opt-in, greenfield, slug) and the two init blobs; engine package.json dependencies and devDependencies stay {}."
  artifacts:
    - path: "engine/bin/lib/anchor.cjs"
      provides: "anchor add|list|check module mirroring bridge.cjs"
      contains: "function cmdAnchorAdd"
    - path: "engine/test/anchor.test.cjs"
      provides: "TDD suite for add/list/check/stale/opt-in/greenfield/slug + deps-{} guard"
      contains: "node:assert/strict"
    - path: "engine/bin/sovereign-tools.cjs"
      provides: "router case 'anchor': dispatching add|list|check"
      contains: "case 'anchor'"
    - path: "engine/bin/lib/init.cjs"
      provides: "init anchor-docs + verify-self workflow arms"
      contains: "case 'verify-self'"
    - path: "engine/test/init.test.cjs"
      provides: "buildInit anchor-docs + verify-self blob assertions"
      contains: "anchor-docs"
  key_links:
    - from: "engine/bin/lib/anchor.cjs"
      to: "engine/bin/lib/core.cjs"
      via: "require('./core.cjs') output/error/safeReadFile"
      pattern: "require\\('\\./core\\.cjs'\\)"
    - from: "engine/bin/lib/anchor.cjs"
      to: "engine/bin/lib/state.cjs"
      via: "require('./state.cjs') readField for **key:** header parse"
      pattern: "readField"
    - from: "engine/bin/sovereign-tools.cjs"
      to: "engine/bin/lib/anchor.cjs"
      via: "require + case 'anchor': dispatch on args[1]"
      pattern: "require\\('\\./lib/anchor\\.cjs'\\)"
    - from: "engine/bin/lib/init.cjs"
      to: "init anchor-docs / verify-self blobs"
      via: "switch(workflow) case arms returning blob via withProjectContext"
      pattern: "case 'anchor-docs'"
---

<objective>
Ship the zero-dependency ENGINE surface that backs M4 anchoring: a new `anchor add|list|check` module over `.sovereign/external-docs/`, router wiring, and two `init` orient workflows (`anchor-docs`, `verify-self`). Engine code + tests ONLY — no skills, no HTTP/fetch client, no copyright-warning logic, no deploy-gate blocking. The engine is content-agnostic: it stores the source URL + metadata always, and full content only on explicit opt-in.

Purpose: Phases 15 (`anchor-docs` skill) and 16 (`verify-self` skill) are thin orchestrators that wrap this surface. The engine must own deterministic storage/list/staleness so the skills stay prose-thin (R-002, engine-first).

Output: `engine/bin/lib/anchor.cjs` (new), `engine/test/anchor.test.cjs` (new), edits to `engine/bin/sovereign-tools.cjs` (router) + `engine/bin/lib/init.cjs` (two arms) + `engine/test/init.test.cjs` (two blob tests). Full suite green; `dependencies` and `devDependencies` stay `{}`.

This is a near-line-for-line mirror of the shipped `bridge.cjs` skeleton with a different domain. A reviewer should be able to diff `anchor.cjs` against `bridge.cjs` and see the same shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-engine-anchor-command-init-workflows/14-CONTEXT.md
@.planning/phases/14-engine-anchor-command-init-workflows/14-RESEARCH.md
@.planning/phases/14-engine-anchor-command-init-workflows/14-VALIDATION.md

# The diff targets — read these; the answer to "how" is already in the repo
@engine/bin/lib/bridge.cjs
@engine/bin/lib/core.cjs
@engine/bin/lib/init.cjs
@engine/bin/sovereign-tools.cjs
@engine/bin/lib/state.cjs
@engine/test/bridge.test.cjs
@engine/test/adopt.test.cjs
@engine/test/init.test.cjs
@engine/references/unverified-marker.md
@engine/package.json

<interfaces>
<!-- Exact contracts to reuse verbatim. Do NOT reinvent any of these. -->

From engine/bin/lib/core.cjs (use exactly):
- `output(result, raw, rawValue)` — JSON-stringifies result; spills to `@file:<tmp>` if >50KB; else writes to fd 1. Inherit the spill for free.
- `error(message)` → never returns (writes `Error: <msg>` to fd 2, exit 1). Use for missing `--source`, unknown subcommand.
- `safeReadFile(p)` → file contents or `null` on any throw.
- `loadConfig(cwd)` → merged config object (used by init arms via orientationConfig).

From engine/bin/lib/state.cjs (use exactly — this is the header parser, do NOT write a new one):
- `readField(content, fieldName)` → trimmed value of `**fieldName:** value` (bold) or `fieldName: value` (plain), else `null`. Already tested.
  Export line: `module.exports = { ..., readField, ... }` — import via `const { readField } = require('./state.cjs');`

From engine/bin/sovereign-tools.cjs (use exactly):
- `parseNamedArgs(args, valueFlags, booleanFlags)` → `{flag: value|null}` for value flags (captures next token unless it starts with `--`), `{flag: true|false}` for booleans.
- Router strips `--raw`/`--pick`/`--cwd` BEFORE dispatch, so by the `case` only command-specific flags remain in `args`.

bridge skeleton to mirror (engine/bin/lib/bridge.cjs):
- `// @ts-check` + `'use strict';` + header doc-comment naming the requirement + "Zero runtime deps — node: built-ins only."
- `const { output, safeReadFile } = require('./core.cjs');`
- module-level rel-path const: `const REGISTRY_REL = path.join('.sovereign', 'bridges', 'registry.json');`
- pure helpers first, `cmd*(cwd, ...args, raw)` after (each ends in `output(result, raw);`), `module.exports = {...}` last.
- greenfield short-circuit: `safeReadFile`/readdir-in-try → clean empty result, never throw.

init arm to mirror (engine/bin/lib/init.cjs `case 'bridge':` @ ~299):
- `blob = { models: {}, config: orientationConfig, phase, context_injection: contextInjection(cwd), paths: {...}, exists };`
- returned via `withProjectContext(cwd, workflow, blob)` (wrapper adds project_root, sovereign_version, agents_installed:true, missing_agents:[] for workflows not in REQUIRED_AGENTS — neither new workflow needs an agent, so NO change to REQUIRED_AGENTS).

test capture helper to mirror (engine/test/adopt.test.cjs:31-51):
- monkeypatch `fs.writeSync` fd-1 to capture `output()`, resolve `@file:` prefix by reading the tmpfile back, then `JSON.parse`.
- `mkProject()` = `fs.mkdtempSync(path.join(os.tmpdir(), 'sov-anchor-'))`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write engine/test/anchor.test.cjs — failing suite for add/list/check + deps-{} guard</name>
  <read_first>engine/test/adopt.test.cjs (capture/mkProject idiom), engine/test/bridge.test.cjs (fixed-fixture + idempotency idiom), 14-VALIDATION.md (T1–T9, T12), 14-RESEARCH.md "Code Examples" + "Common Pitfalls"</read_first>
  <files>engine/test/anchor.test.cjs</files>
  <behavior>
    Author these tests RED (they must fail because anchor.cjs does not exist yet). Mirror adopt.test.cjs's `capture(fn)` (fd-1 monkeypatch + `@file:` resolution) and `mkProject()` (tmpdir). Import `{ cmdAnchorAdd, cmdAnchorList, cmdAnchorCheck, addDays, slugify, isStale }` from `../bin/lib/anchor.cjs`. Use `node:test` + `node:assert/strict` only.

    - T1 (SC1): `cmdAnchorAdd(cwd, { id:'stripe', source:'https://stripe.com/docs', version:'2024-06', 'date-retrieved':'2026-01-01', 're-verify-by':'2026-04-01', content:null, 'store-content':false }, true)` writes `.sovereign/external-docs/stripe.md`; the file contains all five headers `**source:**`, `**version:**`, `**date-retrieved:**`, `**re-verify-by:**`, `**content-stored:**`.
    - T2 (SC1): no `--content` ⇒ body absent (file is header-only) AND `**content-stored:** false`.
    - T3 (SC1): `content: '@<tmpfile>'` (write a tmp file with body text) stores the body after the header AND `**content-stored:** true`; also assert a literal-string content (no `@`/`-`) is stored verbatim.
    - T4 (SC1): re-adding the same `--id` with a different source overwrites the file (idempotent update — assert new source present, exactly one file in dir).
    - T5 (SC1): `addDays('2026-01-01', 90)` === '2026-04-01' (pure); and `cmdAnchorAdd` WITHOUT `--re-verify-by` sets `re-verify-by` = `addDays(date-retrieved, 90)`; WITH `--re-verify-by` honors the explicit value.
    - SC1 required-arg: calling `cmdAnchorAdd` with `source: null` calls `error()` (exit 1) — assert via `spawnSync(process.execPath, [BIN,'anchor','add','--id','x','--cwd',dir])` exits non-zero (use the BIN-integration style, since `error()` calls process.exit).
    - SC1 slug: `slugify('../../etc/foo')` contains no `/` or `.` and matches `/^[a-z0-9-]+$/`; an `add` with `--id ../x` writes a file that stays inside `.sovereign/external-docs/` (assert no file outside the dir).
    - T6 (SC2): seed two `<slug>.md` files with known headers, `capture(() => cmdAnchorList(cwd, true))` returns an array of `{id, source, version, date_retrieved, re_verify_by, content_stored, stale}` parsed from headers (assert field names + values; `content_stored` is a boolean).
    - T7 (SC2): `capture(() => cmdAnchorList(cwd, true))` on a fresh `mkProject()` (no external-docs/) === `[]`, no throw.
    - T8 (SC2): seed one anchor with `re-verify-by` in the past (e.g. '2000-01-01') and one in the future (e.g. '2999-01-01'); `capture(() => cmdAnchorCheck(cwd, true))` returns `{ anchors:[...], stale_count: 1 }` with the past one `stale:true` and the future one `stale:false`. Also assert `isStale('2000-01-01','2026-06-09')===true` and `isStale('2026-06-09','2026-06-09')===false` (equal-to-today NOT stale).
    - T9 (SC2): `capture(() => cmdAnchorCheck(cwd, true))` on greenfield === `{ anchors:[], stale_count:0 }`, no throw.
    - T12 (SC4): read `../package.json`; `assert.deepEqual(pkg.dependencies, {})` and `assert.deepEqual(pkg.devDependencies, {})`.
  </behavior>
  <action>
    Create `engine/test/anchor.test.cjs`. Copy the `capture(fn)` helper VERBATIM from `engine/test/adopt.test.cjs:31-51` (fd-1 monkeypatch + `@file:` resolve + JSON.parse). Define `mkProject()` as `fs.mkdtempSync(path.join(os.tmpdir(), 'sov-anchor-'))`. Define `ENGINE_ROOT = path.join(__dirname, '..')` and `BIN = path.join(ENGINE_ROOT,'bin','sovereign-tools.cjs')` (for the error/router spawnSync cases). Import the module under test from `path.join(ENGINE_ROOT,'bin','lib','anchor.cjs')`.

    Always pass EXPLICIT `date-retrieved` and `re-verify-by` in stored fixtures and `add` calls so assertions are deterministic across runs (per RESEARCH Pitfall 2 — never assert a literal "today"). For staleness, seed FIXED past/future dates. For the +90d default test, assert the relationship via `addDays`, not a literal.

    Run `cd engine && node --test test/anchor.test.cjs` — it MUST fail (module not found / undefined functions). Commit RED.
  </action>
  <verify>
    <automated>cd /Users/wizzie/conductor/workspaces/sovereign/seville/engine && node --test test/anchor.test.cjs; test $? -ne 0 && echo "RED-as-expected"</automated>
  </verify>
  <done>engine/test/anchor.test.cjs exists, imports cmdAnchorAdd/cmdAnchorList/cmdAnchorCheck/addDays/slugify/isStale from ../bin/lib/anchor.cjs, covers T1–T9 + T12 + required-source + slug-traversal, and the run FAILS (RED) because anchor.cjs does not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement engine/bin/lib/anchor.cjs mirroring bridge.cjs until Task 1 passes</name>
  <read_first>engine/bin/lib/bridge.cjs (skeleton to diff against), engine/bin/lib/core.cjs (output/error/safeReadFile), engine/bin/lib/state.cjs:72 (readField), 14-RESEARCH.md "Code Examples"</read_first>
  <files>engine/bin/lib/anchor.cjs</files>
  <action>
    Create `engine/bin/lib/anchor.cjs` as a near-line-for-line analog of `bridge.cjs`:
    - Open with `// @ts-check` + `'use strict';` + a header doc-comment naming ENG-09 / the `anchor add|list|check` scope + "Zero runtime deps — node: built-ins only."
    - Imports: `const fs = require('node:fs');` `const path = require('node:path');` `const { output, error, safeReadFile } = require('./core.cjs');` `const { readField } = require('./state.cjs');`
    - Module-level const: `const EXTERNAL_DOCS_REL = path.join('.sovereign', 'external-docs');`

    Pure helpers (export them — Task 1 imports `addDays`, `slugify`, `isStale`):
    - `slugify(id)` → lowercase, replace every char NOT in `[a-z0-9-]` with `-`, collapse repeat `-`, strip leading/trailing `-`. Result can never contain `/` or `.`, so `path.join(dir, slug + '.md')` cannot escape the dir.
    - `todayISO()` → `new Date()` → `getUTCFullYear/Month/Date`, zero-padded `YYYY-MM-DD` (mirror init.cjs dateStamp shape; `new Date()` IS allowed in the engine).
    - `addDays(iso, n)` → `const [y,m,d]=iso.split('-').map(Number); const dt=new Date(Date.UTC(y,m-1,d)); dt.setUTCDate(dt.getUTCDate()+n);` → re-format `YYYY-MM-DD`.
    - `isStale(reVerifyBy, today)` → `typeof reVerifyBy === 'string' && reVerifyBy < today` (lexicographic ISO compare — strict past; equal-to-today NOT stale).
    - `buildHeader({source, version, dateRetrieved, reVerifyBy, contentStored})` → joined bold-markdown lines `**source:** ${source}`, `**version:** ${version}`, `**date-retrieved:** ${dateRetrieved}`, `**re-verify-by:** ${reVerifyBy}`, `**content-stored:** ${contentStored}`, trailing blank line.
    - `parseAnchor(id, content)` → `{ id, source: readField(content,'source'), version: readField(content,'version'), date_retrieved: readField(content,'date-retrieved'), re_verify_by: readField(content,'re-verify-by'), content_stored: readField(content,'content-stored') === 'true' }`.
    - `listAnchors(cwd)` → `dir = path.join(cwd, EXTERNAL_DOCS_REL); let names; try { names = fs.readdirSync(dir); } catch { return []; }` then filter `.md`, sort, map to `parseAnchor(name.replace(/\.md$/,''), safeReadFile(path.join(dir,f))||'')` (greenfield-safe — mirror init.cjs:99-111 readdir-in-try).

    cmd functions (each ends in `output(...)` or `error(...)`):
    - `cmdAnchorAdd(cwd, flags, raw)`: read `flags.id`, `flags.source`, `flags.version`, `flags['date-retrieved']`, `flags['re-verify-by']`, `flags.content`. If `!flags.source` → `error('anchor add: --source is required');`. `const slug = slugify(flags.id || '');` (if empty after slugify → `error('anchor add: --id is required')`). `const dateRetrieved = flags['date-retrieved'] || todayISO();` `const reVerifyBy = flags['re-verify-by'] || addDays(dateRetrieved, 90);` `const version = flags.version || 'unknown';`. Resolve content opt-in: if `flags.content` is non-null → `contentStored = true`; resolve the body: `@<file>` → `safeReadFile(path.join(cwd, rest)) ?? safeReadFile(rest) ?? ''`; `-` → `fs.readFileSync(0, 'utf-8')` (stdin); otherwise the literal string. Else `contentStored = false`, body `''`. `const dir = path.join(cwd, EXTERNAL_DOCS_REL); fs.mkdirSync(dir, { recursive: true });` (mirror manifest.cjs/state.cjs write idiom). Write `path.join(dir, slug + '.md')` = `buildHeader({source: flags.source, version, dateRetrieved, reVerifyBy, contentStored}) + (contentStored ? body : '')` via `fs.writeFileSync(..., 'utf-8')`. Then `output({ id: slug, source: flags.source, version, date_retrieved: dateRetrieved, re_verify_by: reVerifyBy, content_stored: contentStored, path: path.join(EXTERNAL_DOCS_REL, slug + '.md') }, raw);`
    - `cmdAnchorList(cwd, raw)`: `output(listAnchors(cwd).map(a => ({ ...a, stale: isStale(a.re_verify_by, todayISO()) })), raw);` (array top-level — per RESEARCH Pitfall 1; `stale` is cheap to compute so include it).
    - `cmdAnchorCheck(cwd, raw)`: `const today = todayISO(); const anchors = listAnchors(cwd).map(a => ({ ...a, stale: isStale(a.re_verify_by, today) })); const stale_count = anchors.filter(a => a.stale).length; output({ anchors, stale_count }, raw);` (object top-level WITH summary count — mirror bridge check returning an object).

    `module.exports = { cmdAnchorAdd, cmdAnchorList, cmdAnchorCheck, slugify, addDays, isStale, todayISO, buildHeader, parseAnchor, listAnchors };`

    Do NOT add any require beyond `node:fs`, `node:path`, `./core.cjs`, `./state.cjs`. Do NOT fetch any URL. Do NOT add copyright-warning logic.

    Run `cd engine && node --test test/anchor.test.cjs` until GREEN. Commit GREEN.
  </action>
  <verify>
    <automated>cd /Users/wizzie/conductor/workspaces/sovereign/seville/engine && node --test test/anchor.test.cjs</automated>
  </verify>
  <done>engine/bin/lib/anchor.cjs exists with `function cmdAnchorAdd`/`cmdAnchorList`/`cmdAnchorCheck` + the pure helpers, requires only node:fs/node:path/./core.cjs/./state.cjs, and `node --test test/anchor.test.cjs` exits 0 (all T1–T9, T12, slug, required-source GREEN).</done>
</task>

<task type="auto">
  <name>Task 3: Wire the router case 'anchor': in sovereign-tools.cjs + integration coverage</name>
  <read_first>engine/bin/sovereign-tools.cjs (case 'bridge': @357, parseNamedArgs @57, require block @32-39, usage string @217-221), 14-RESEARCH.md "Router arm"</read_first>
  <files>engine/bin/sovereign-tools.cjs, engine/test/anchor.test.cjs</files>
  <action>
    Edit `engine/bin/sovereign-tools.cjs`:
    1. After the bridge require (line ~32), add: `const { cmdAnchorAdd, cmdAnchorList, cmdAnchorCheck } = require('./lib/anchor.cjs');`
    2. Add `case 'anchor':` to the `switch (command)` router (place it next to `case 'bridge':`), mirroring the bridge arm:
       ```
       case 'anchor': {
         const sub = args[1];
         if (sub === 'add') {
           const f = parseNamedArgs(args.slice(2),
             ['id', 'source', 'version', 'date-retrieved', 're-verify-by', 'content'],
             ['store-content']);
           cmdAnchorAdd(cwd, f, raw);
         } else if (sub === 'list') {
           cmdAnchorList(cwd, raw);
         } else if (sub === 'check') {
           cmdAnchorCheck(cwd, raw);
         } else {
           error(`Unknown anchor subcommand: ${sub || '(none)'} (expected add|list|check)`);
         }
         break;
       }
       ```
    3. Update the usage string at line ~217-221: append `anchor (add|list|check)` to the Commands list (after `adopt (scan)`).

    Then ADD router-integration cases to `engine/test/anchor.test.cjs` (spawnSync the BIN, mirror adopt.test/init.test integration style):
    - `anchor add` via BIN with `--id`, `--source`, `--date-retrieved`, `--re-verify-by`, `--cwd <tmp>` exits 0 and writes the file (resolve `@file:` if present, assert JSON `id`/`source`).
    - `anchor list --cwd <tmp> --raw` (or default) exits 0 and parses (array).
    - `anchor check --cwd <tmp>` exits 0 and parses (`{anchors, stale_count}`).
    - `anchor add --id x --cwd <tmp>` (no `--source`) exits NON-zero (required-arg path through `error()`).
    - `anchor bogus --cwd <tmp>` exits NON-zero (unknown subcommand).
    - SC1 `--content @<tmpfile>` round-trips through the BIN: stored file body matches the tmpfile contents and `content-stored: true`.
  </action>
  <verify>
    <automated>cd /Users/wizzie/conductor/workspaces/sovereign/seville/engine && node --test test/anchor.test.cjs</automated>
  </verify>
  <done>sovereign-tools.cjs requires anchor.cjs and contains `case 'anchor'` dispatching add/list/check with an unknown-subcommand `error()`; usage string lists `anchor`; anchor.test.cjs's BIN-integration cases (incl. unknown-sub + missing-source non-zero exits, and `--content @file` round-trip) pass; `node --test test/anchor.test.cjs` exits 0.</done>
</task>

<task type="auto">
  <name>Task 4: Add init anchor-docs + verify-self arms in init.cjs + extend init.test.cjs</name>
  <read_first>engine/bin/lib/init.cjs (buildInit switch @255, case 'bridge': @299, case 'adopt': @323, withProjectContext, REQUIRED_AGENTS), engine/test/init.test.cjs (M3 bridge/adopt/extension blob tests @177-212), 14-RESEARCH.md "Init arm" + Open Q4 (marker path)</read_first>
  <files>engine/bin/lib/init.cjs, engine/test/init.test.cjs</files>
  <action>
    Edit `engine/bin/lib/init.cjs` — add two arms to the `switch (workflow)` in `buildInit`, modeled on `case 'bridge':`/`case 'adopt':`. Do NOT touch `REQUIRED_AGENTS` (neither workflow dispatches a subagent → `withProjectContext` yields `agents_installed:true, missing_agents:[]` for free).

    ```
    case 'anchor-docs': {
      blob = {
        models: {},
        config: orientationConfig,
        phase,
        context_injection: contextInjection(cwd),
        paths: {
          external_docs_dir: '.sovereign/external-docs',
          state: '.sovereign/STATE.md',
          manifest: '.sovereign/MANIFEST.md',
        },
        exists,
      };
      break;
    }
    case 'verify-self': {
      blob = {
        models: {},
        config: orientationConfig,
        phase,
        context_injection: contextInjection(cwd),
        paths: {
          external_docs_dir: '.sovereign/external-docs',
          unverified_marker_spec: 'references/unverified-marker.md',
          state: '.sovereign/STATE.md',
          manifest: '.sovereign/MANIFEST.md',
        },
        exists,
      };
      break;
    }
    ```

    DECISION (locked, per RESEARCH Open Q4): the marker-spec path is the SHIPPED-package-relative string `references/unverified-marker.md`. Rationale: `engine/package.json` `files` ships the `references/` dir at the package root, so the installed Phase-16 skill resolves it as `references/unverified-marker.md` (NOT the repo-relative `engine/references/...`). The file exists today at `engine/references/unverified-marker.md` (confirmed). No other init blob currently expresses a shipped-asset path, so this sets the convention: package-root-relative, no `engine/` prefix, forward slashes.

    Then extend `engine/test/init.test.cjs` (mirror the M3 bridge/adopt blob tests @177-212, reuse the existing `mkSeededProject`/`mkEmptyProject` helpers + EXPECTED_VERSION):
    - T10 (SC3): `buildInit(dir, 'anchor-docs')` → `paths.external_docs_dir === '.sovereign/external-docs'`, `models` deepEqual `{}`, `config`/`phase`/`exists` are objects, `project_root === dir`, `sovereign_version === EXPECTED_VERSION`, `agents_installed === true`.
    - T11 (SC3): `buildInit(dir, 'verify-self')` → `paths.external_docs_dir === '.sovereign/external-docs'` AND `paths.unverified_marker_spec === 'references/unverified-marker.md'`.
    - SC3 greenfield: both workflows in `mkEmptyProject()` do not throw and `exists.sovereign_dir === false` (extend the existing greenfield loop or add a parallel one for `['anchor-docs','verify-self']`).
  </action>
  <verify>
    <automated>cd /Users/wizzie/conductor/workspaces/sovereign/seville/engine && node --test test/init.test.cjs</automated>
  </verify>
  <done>init.cjs contains `case 'anchor-docs'` and `case 'verify-self'` arms (verify-self surfaces `unverified_marker_spec: 'references/unverified-marker.md'`); init.test.cjs asserts both blobs (T10/T11) + greenfield safety; `node --test test/init.test.cjs` exits 0.</done>
</task>

<task type="auto">
  <name>Task 5: Full-suite green gate + deps-{} regression confirmation</name>
  <read_first>engine/package.json (scripts.test, dependencies, devDependencies), 14-VALIDATION.md "Sampling Rate"</read_first>
  <files>engine/test/anchor.test.cjs</files>
  <action>
    Run the FULL engine suite and confirm no regressions across the existing 129-test baseline plus the new anchor + init cases. Run `cd engine && node --test "test/**/*.test.cjs"`.

    If the deps-{} guard (T12) is not already in `anchor.test.cjs` from Task 1, confirm it is present and asserts BOTH `pkg.dependencies` and `pkg.devDependencies` deepEqual `{}` (reading `../package.json`). Do NOT bump VERSION or package.json version in this phase (2.2.0 already shipped; versioning is handled at milestone completion).

    This task is the wave/phase gate — it adds NO new production behavior, only confirms the whole surface is green and zero-dep before `/gsd:verify-work`.
  </action>
  <verify>
    <automated>cd /Users/wizzie/conductor/workspaces/sovereign/seville/engine && node --test "test/**/*.test.cjs" && node -e "const p=require('./package.json'); const a=require('node:assert'); a.deepEqual(p.dependencies,{}); a.deepEqual(p.devDependencies,{}); console.log('deps-clean')"</automated>
  </verify>
  <done>`node --test "test/**/*.test.cjs"` exits 0 (full suite green, no regressions); `engine/package.json` `dependencies` and `devDependencies` are both `{}`; VERSION/package.json version unchanged.</done>
</task>

</tasks>

<verification>
- `cd engine && node --test test/anchor.test.cjs` exits 0 (SC1 add/headers/opt-in/idempotent/slug/required-source; SC2 list/check/stale/greenfield).
- `cd engine && node --test test/init.test.cjs` exits 0 (SC3 both init blobs + greenfield + marker-spec path).
- `cd engine && node --test "test/**/*.test.cjs"` exits 0 (SC4 full suite green, no regressions).
- `engine/bin/lib/anchor.cjs` requires only node:fs/node:path/./core.cjs/./state.cjs (no new deps; no fetch).
- `engine/package.json` `dependencies` and `devDependencies` both `{}` (SC4).
- Router: `sovereign-tools anchor add|list|check` dispatches; unknown subcommand and missing `--source` exit non-zero.
</verification>

<success_criteria>
- SC1: `anchor add` writes `.sovereign/external-docs/<slug>.md` with `source`/`version`/`date-retrieved`/`re-verify-by`/`content-stored` headers; URL-by-default; content stored only on `--content` opt-in; slug sanitized; re-add overwrites; `--re-verify-by` defaults to date-retrieved+90d.
- SC2: `anchor list` returns the header-parsed array; `anchor check` returns `{anchors, stale_count}` flagging `re-verify-by < today` (lexicographic, deterministic); both greenfield-safe.
- SC3: `init anchor-docs` and `init verify-self` each return one orient blob via `output()` (with `@file:` spill inherited); `verify-self` surfaces `references/unverified-marker.md`.
- SC4: `node --test` covers add/list/check (stale, opt-in, greenfield, slug) + both init blobs and passes; engine deps stay `{}`.
</success_criteria>

<output>
After completion, create `.planning/phases/14-engine-anchor-command-init-workflows/14-01-SUMMARY.md`
</output>
