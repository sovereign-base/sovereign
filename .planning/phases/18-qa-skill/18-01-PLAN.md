---
phase: 18-qa-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/skills/qa/SKILL.md
autonomous: true
requirements: [QA-01, M5-CC]

must_haves:
  truths:
    - "A user can run /qa to sweep each workspace/module using its OWN toolchain across all 5 categories (static correctness, tests, dependency & wiring integrity, navigation/routing, cross-workspace consistency incl. API contract vs .sovereign/docs/api/API_SPEC.md)."
    - "qa reports check/cross/warn glyphs grouped by module then category, failures with exact error + file:line, ending in a one-line verdict (pass / fail with N blocking); it drives the project's own qa command when present, else per-module equivalents — stack-agnostic (no hardcoded default runner)."
    - "qa is a core-tier thin orchestrator: one `init qa` orient call, Why-this-matters, recommendation-first flow, navigation footer; disable-model-invocation true holds the doctor auto-budget at 5 (total 20 / disabled 15); validate skills passes; engine suite stays 164 green; composes with /diagnose (failures to debug) and complements /sentinel (mechanical correctness vs standards)."
  artifacts:
    - path: "engine/skills/qa/SKILL.md"
      provides: "The hand-authored qa thin-orchestrator skill"
      min_lines: 70
      contains:
        - "name: qa"
        - "disable-model-invocation: true"
        - "## Why this matters"
        - "## When to use this"
        - "## The flow"
        - "init qa"
        - "@file:"
        - ".claude/sovereign-engine/sovereign-tools.cjs"
        - "static correctness"
        - "dependency"
        - "wiring"
        - "import"
        - "version"
        - "config"
        - "routing"
        - "cross-workspace"
        - ".sovereign/docs/api/API_SPEC.md"
        - "file:line"
        - "verdict"
        - "per-module"
        - "diagnose"
        - "sentinel"
  key_links:
    - from: "engine/skills/qa/SKILL.md"
      to: ".claude/sovereign-engine/sovereign-tools.cjs init qa"
      via: "one-call orient with @file: guard"
      pattern: "init qa"
    - from: "engine/skills/qa/SKILL.md"
      to: "/diagnose and /sentinel"
      via: "composition (flow handoff + navigation footer)"
      pattern: "diagnose|sentinel"
    - from: "engine/skills/qa/SKILL.md"
      to: ".sovereign/docs/api/API_SPEC.md"
      via: "cross-workspace API-contract check target (literal path)"
      pattern: "API_SPEC\\.md"
---

<objective>
Author the hand-authored `qa` skill — a relentless, stack-agnostic, repo-wide correctness sweep that catches errors, type mismatches, broken imports/wiring, missing config, dead routes, and cross-workspace/contract drift BEFORE they hit a running build. A core-tier thin orchestrator over the project's OWN toolchain.

Purpose: Closes QA-01 + the M5-CC cross-cutting gate. Gives the construction phase its correctness sweep, composing with the shipped `/diagnose` (Phase 17) and complementing `/sentinel`.
Output: ONE file — `engine/skills/qa/SKILL.md`. NOTHING ELSE. NO engine change (research verified `init qa` works via the `init` default case), NO new ADR/reference, NO change to diagnose/sentinel/tdd.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-qa-skill/18-CONTEXT.md
@.planning/phases/18-qa-skill/18-RESEARCH.md
@.planning/phases/18-qa-skill/18-VALIDATION.md
@.planning/research/qa-skill-source-spec.md
@CLAUDE.md

# The pattern to mirror (shape, flow, one-call orient, footer, composition):
@engine/skills/diagnose/SKILL.md
# The report-block + verdict-style idea (do NOT copy its frontmatter — sentinel is Fast Lane, NO disable flag):
@engine/skills/sentinel/SKILL.md
# The authoring contract (frontmatter, required sections, one-init rule, line gate, footer):
@engine/references/skill-format.md

<interfaces>
<!-- The exact contracts the executor needs. Use these directly — no codebase exploration required. -->

FRONTMATTER (literal, copy verbatim — mirrors diagnose; description ~330 chars, well under cap).
NOTE: the two `===` lines below STAND IN for the SKILL.md's real `---` YAML delimiters — they are
written as `===` here ONLY so they don't collide with THIS plan's own frontmatter parser. In the
actual engine/skills/qa/SKILL.md, use real `---` delimiter lines.

```yaml
===
name: qa
description: Sweep the whole repo for correctness before it fails in a running build — typecheck/compile, schema, lint, tests, broken imports and wiring, missing config, dead routes, and cross-workspace/contract drift — using the project's own toolchain, reporting check/cross/warn glyphs with file:line. Use before a commit, PR, or build, after a big change, or when wiring feels shaky.
disable-model-invocation: true
argument-hint: "[module or path to scope the sweep]"
===
```
(The `description` MUST literally contain the three glyphs check (U+2705), cross (U+274C), warn (U+26A0 U+FE0F) — shown as words here only to keep this plan's YAML clean. Lead with the use-case; stay under the 1024-char cap.)

ONE-CALL ORIENT (literal installed engine path + @file: guard — copy verbatim, only the workflow name differs from diagnose):
```bash
INIT=$(node ".claude/sovereign-engine/sovereign-tools.cjs" init qa)
if [[ "$INIT" == @file:* ]]; then INIT=$(cat "${INIT#@file:}"); fi
```
NO dedicated `init.cjs` case — the default case returns a usable blob (verified live, exit 0). The API_SPEC path is stated LITERALLY in the body (the default blob does not surface it), exactly as verify-self hardcodes references/unverified-marker.md.

THE 5 CHECK CATEGORIES (agnostic — describe each as a CLASS, never a tool; from the source spec + CONTEXT lock):
  1. Static correctness — compiler/type checker in check/no-emit mode; validate schema definitions (DB, serialization, config) against their own tooling; run the configured linter and surface errors.
  2. Tests — run the module's unit and integration/component test suites with its own runner.
  3. Dependency & wiring integrity — DI/service/provider wiring registered+imported+exported where the framework requires; every import resolves to a real installed module (no "module not found"); matched-version requirements agree exactly; example/template config (.env.example, sample config) cross-checked against what the code actually reads at runtime.
  4. Navigation / routing — every route/link/navigation target referenced in code maps to a real, registered destination.
  5. Cross-workspace consistency — a shared runtime resolves to exactly ONE version across modules; shared/contract types match producers↔consumers; schema fields match their type/interface definitions; endpoint request/response shapes match the documented API contract at `.sovereign/docs/api/API_SPEC.md`.

PROJECT'S-OWN-QA-COMMAND PHRASING (agnostic — name the idea, not a tool):
  "Run the project's own `qa` command if it has one. Detect it from the project's task-runner/manifest (a configured qa/check/verify script or task target) and run it from the repo root for a full sweep. SOVEREIGN imposes no runner — use whatever this project already uses. If there is no project-level `qa` command, run the per-module equivalents below, one workspace/module at a time, using each module's own toolchain."

REPORT SKELETON (embed verbatim in the body — module → category → glyphs with file:line → one-line verdict). Use the REAL glyphs check (U+2705) / cross (U+274C) / warn (U+26A0 U+FE0F) in the actual SKILL.md (shown here exactly):
```
QA SWEEP — <repo or scope>
Toolchain: <project qa command | per-module equivalents>

── <workspace/module A> ───────────────────────────────
  Static correctness   ✅ typecheck · ✅ schema · ❌ lint
     ❌ src/api/user.ts:42 — 'unused import "Foo"' (no-unused-vars)
  Tests                ✅ 128 passed
  Dependency & wiring  ❌ src/db/client.ts:7 — cannot resolve 'pg-native' (module not found)
                       ⚠️ .env.example missing STRIPE_KEY read at src/pay.ts:15
  Navigation/routing   ✅ all routes resolve
  Cross-workspace      ✅ (covered in the cross-workspace section)

── <workspace/module B> ───────────────────────────────
  Static correctness   ✅ · ✅ · ✅
  Tests                ⚠️ 2 skipped
  Dependency & wiring  ✅
  Navigation/routing   ✅
  Cross-workspace      ✅

── CROSS-WORKSPACE CONSISTENCY ────────────────────────
  Shared-runtime version  ❌ react 18.2.0 (web) vs 18.3.1 (admin) — must match
  Shared/contract types   ✅
  Schema ↔ types          ✅
  API contract            ❌ POST /users response missing `createdAt`
                          (src/api/user.ts:55 vs .sovereign/docs/api/API_SPEC.md:120)

── VERDICT ────────────────────────────────────────────
  FAIL — 4 blocking issues (hand each ❌ to /diagnose)
```

NAVIGATION FOOTER (recommendation-first; mirrors diagnose's footer + composition — embed verbatim, with `▶ NEXT` as the header):
```
▶ NEXT
  Verdict: <PASS | FAIL with N blocking>
  PASS:  continue — /sentinel for the standards pass, or proceed to commit/PR.
  FAIL:  take each ❌ to /diagnose (reproduce → isolate → fix → verify) — qa finds
         breakage broadly; diagnose fixes one root cause at a time.

  qa is the mechanical-correctness sweep. Its complements:
  • /diagnose — debug a specific failure qa surfaced.
  • /sentinel — judge the result against your standards (qa = does it work;
    sentinel = is it right).
  • /tdd      — when a fix needs new behavior, drive it test-first.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author engine/skills/qa/SKILL.md</name>
  <files>engine/skills/qa/SKILL.md</files>
  <read_first>
    - engine/skills/diagnose/SKILL.md (the 1:1 template — frontmatter shape, bold-numbered flow `**1 — …**`, one-call orient, `▶ NEXT` footer, composition). MIRROR this shape, NOT sentinel's (sentinel is Fast Lane with no disable flag).
    - engine/skills/sentinel/SKILL.md (copy the IDEA of an embedded report block + a structured verdict; do NOT copy its frontmatter).
    - engine/references/skill-format.md (required sections in order, one-init rule, line gate >= 70, recommendation-first footer, dropped v1 fields).
    - .planning/research/qa-skill-source-spec.md (the authoritative content source — adapt verbatim to thin-orchestrator shape).
    - The `<interfaces>` block in this plan's context (all literal values: frontmatter, orient snippet, 5 categories, report skeleton, footer). REMEMBER: in the interfaces block the SKILL.md frontmatter delimiters are shown as `===` and the glyphs are spelled out — in the ACTUAL SKILL.md use real `---` delimiter lines and the real glyphs (check U+2705, cross U+274C, warn U+26A0 U+FE0F).
  </read_first>
  <action>
    Create `engine/skills/qa/SKILL.md` as a core-tier thin orchestrator, mirroring `engine/skills/diagnose/SKILL.md`'s exact shape. Implements QA-01 and M5-CC.

    1. FRONTMATTER — use the literal block from `<interfaces>`, with REAL `---` YAML delimiter lines (the `===` in the interfaces block is only a parser-collision placeholder). Fields: `name: qa`, the use-case-first `description` (which MUST literally contain the three glyphs check/cross/warn — see the interfaces note), `disable-model-invocation: true` (MANDATORY per M5-CC — keeps doctor auto-budget at 5; do NOT omit it as sentinel does), `argument-hint`. Use ONLY real Agent-Skills fields — NO v1 fields (`triggers`, `works-best-with`, `min-model`, `tokens`, bare `phase`).

    2. `## Why this matters` — plain language: skipping a correctness sweep ships plausible-but-broken code (an import that doesn't resolve, a type that drifted from its contract, a dead route) and the failure surfaces in a RUNNING build, not in review, where it's far more expensive. State that qa is a thin orchestrator over the project's OWN toolchain — it never bundles a linter/test runner (the engine stays zero-dep); its value is COVERAGE + DISCIPLINE + a consistent report.

    3. `## When to use this` — the trigger signals: before a commit/PR/build, after a big change, when wiring feels shaky. Include a `**Don't**` line drawing the sibling boundaries: don't use it for standards/judgment (that's `/sentinel`); don't use it to debug ONE failure (that's `/diagnose`); don't use it to write new tests/behavior (that's `/tdd`). qa finds breakage broadly; the siblings each do their specific part.

    4. `## The flow` — a thin orchestrator, recommendation-first, bold-numbered steps (`**1 — …**` like diagnose):
       - `**1 — Orient.**` ONE call — paste the orient snippet from `<interfaces>` verbatim (literal `.claude/sovereign-engine/sovereign-tools.cjs init qa` + the `@file:` guard line). Then read only what it points to (glossary/state) and the LITERAL `.sovereign/docs/api/API_SPEC.md` for the contract check. NO `$ENGINE`, NO `engine/bin/...`, NO second orientation read.
       - `**2 — Detect the project's own qa command.**` Use the agnostic phrasing from `<interfaces>`: detect a configured qa/check/verify script or task target and run it from the repo root for a full sweep; SOVEREIGN imposes no runner. If there's none, run the per-module equivalents (below), one workspace/module at a time, using each module's own toolchain. Detect repo structure (monorepo packages/workspaces vs single module) from the project's own manifests — do NOT assume npm workspaces.
       - `**3 — Sweep each module across the 5 categories.**` Name ALL FIVE with the class-level (agnostic) phrasing from `<interfaces>`: (1) static correctness (compiler/type checker in check/no-emit mode + schema validation + configured linter), (2) tests (unit + integration/component with the module's own runner), (3) dependency & wiring integrity (DI/wiring registered+imported+exported + import resolution "no module not found" + matched-version alignment + example/template config like `.env.example` cross-checked against what the code reads at runtime), (4) navigation/routing (every route/link/target maps to a real registered destination), (5) cross-workspace consistency (single shared-runtime version + shared/contract types match producers↔consumers + schema↔types + endpoint shapes match `.sovereign/docs/api/API_SPEC.md`). NEVER hardcode `npm test`/`pytest`/`jest`/`go test`/`cargo test`/`vitest`/`mvn`/`gradle` as THE default runner — if you mention any, scope it to a parenthetical `e.g.` EXAMPLE only, never as the command.
       - `**4 — Report.**` Embed the report skeleton from `<interfaces>` VERBATIM in a fenced block (with the REAL glyphs): grouped by module → category, check pass / cross failure with the exact error + `file:line` / warn non-blocking warning, ending in a one-line VERDICT (pass / fail with N blocking). The three glyphs (check U+2705, cross U+274C, warn U+26A0 U+FE0F), the literal token `file:line`, and `verdict` MUST all appear in the body.
       - `**5 — Hand off / persist.**` Each failure goes to `/diagnose` (reproduce → isolate → fix → verify); qa finds breakage broadly, diagnose fixes one root cause at a time. Persist via `node ".claude/sovereign-engine/sovereign-tools.cjs" state save`; commit via `sovereign-tools commit` if `commit_docs` is on. Delegate ALL bookkeeping to the engine — never hand-write state.

    5. `## Navigation` footer — paste the `▶ NEXT` block from `<interfaces>` verbatim (recommendation-first verdict + PASS/FAIL paths + the /diagnose · /sentinel · /tdd composition note).

    Target >= 70 lines (diagnose is 82, sentinel 64). Keep it tight — progressive disclosure, no fat body. DO NOT touch init.cjs, diagnose, sentinel, tdd, any ADR/reference, or ROADMAP.
  </action>
  <acceptance_criteria>
    All run from the repo root (paths relative to it). Each is presence-based (no line-ordering); a flat `&&` chain ending in `echo OK`.

    Structural (G1–G7, SC3/M5-CC):
    ```bash
    S=engine/skills/qa/SKILL.md
    test -f "$S" \
      && grep -qE '^name:[[:space:]]*qa[[:space:]]*$' "$S" \
      && grep -qE '^disable-model-invocation:[[:space:]]*true' "$S" \
      && grep -qF '## Why this matters' "$S" \
      && grep -qF '## When to use this' "$S" \
      && grep -qF '## The flow' "$S" \
      && grep -qF '▶ NEXT' "$S" \
      && [ "$(wc -l < "$S")" -ge 70 ] \
      && ! grep -qE '^(triggers|works-best-with|min-model|tokens|phase):' "$S" \
      && grep -qF '.claude/sovereign-engine/sovereign-tools.cjs' "$S" \
      && ! grep -qF '$ENGINE' "$S" \
      && grep -qF 'init qa' "$S" \
      && grep -qF '@file:' "$S" \
      && echo OK
    ```

    Coverage-in-prose (G8–G13, SC1/SC2 — case-insensitive, tolerant of prose; each glyph grepped literally):
    ```bash
    S=engine/skills/qa/SKILL.md
    grep -iq 'static correctness' "$S" \
      && grep -iq 'tests' "$S" \
      && grep -iqE 'wiring|dependency' "$S" \
      && grep -iqE 'rout' "$S" \
      && grep -iq 'cross-workspace' "$S" \
      && grep -iq 'import' "$S" \
      && grep -iq 'version' "$S" \
      && grep -iq 'config' "$S" \
      && grep -qF '.sovereign/docs/api/API_SPEC.md' "$S" \
      && grep -qF '✅' "$S" \
      && grep -qF '❌' "$S" \
      && grep -qF '⚠️' "$S" \
      && grep -iqF 'file:line' "$S" \
      && grep -iq 'verdict' "$S" \
      && grep -iqE "project.?s own" "$S" \
      && grep -iq 'per-module' "$S" \
      && echo OK
    ```

    Stack-agnostic (G13 — NO hardcoded default runner; examples behind `e.g.` are allowed). Manual eyeball is the source of truth; this grep is a tripwire that flags any hardcoded-runner line that is NOT an `e.g.` example:
    ```bash
    S=engine/skills/qa/SKILL.md
    ! grep -iE '\b(npm test|pytest|jest|go test|cargo test|vitest|mvn|gradle)\b' "$S" | grep -ivq 'e\.g\.' \
      && echo OK
    ```

    Composition (G14, SC2/SC3):
    ```bash
    S=engine/skills/qa/SKILL.md
    grep -iqF 'diagnose' "$S" && grep -iqF 'sentinel' "$S" && echo OK
    ```
  </acceptance_criteria>
  <verify>
    <automated>S=engine/skills/qa/SKILL.md; test -f "$S" && grep -qE '^name:[[:space:]]*qa[[:space:]]*$' "$S" && grep -qE '^disable-model-invocation:[[:space:]]*true' "$S" && grep -qF '## Why this matters' "$S" && grep -qF '## When to use this' "$S" && grep -qF '## The flow' "$S" && grep -qF '▶ NEXT' "$S" && [ "$(wc -l < "$S")" -ge 70 ] && ! grep -qE '^(triggers|works-best-with|min-model|tokens|phase):' "$S" && grep -qF '.claude/sovereign-engine/sovereign-tools.cjs' "$S" && ! grep -qF '$ENGINE' "$S" && grep -qF 'init qa' "$S" && grep -qF '@file:' "$S" && grep -iq 'static correctness' "$S" && grep -iqE 'wiring|dependency' "$S" && grep -iqE 'rout' "$S" && grep -iq 'cross-workspace' "$S" && grep -iq 'import' "$S" && grep -iq 'version' "$S" && grep -iq 'config' "$S" && grep -qF '.sovereign/docs/api/API_SPEC.md' "$S" && grep -qF '✅' "$S" && grep -qF '❌' "$S" && grep -qF '⚠️' "$S" && grep -iqF 'file:line' "$S" && grep -iq 'verdict' "$S" && grep -iqE "project.?s own" "$S" && grep -iq 'per-module' "$S" && grep -iqF 'diagnose' "$S" && grep -iqF 'sentinel' "$S" && echo OK</automated>
  </verify>
  <done>
    `engine/skills/qa/SKILL.md` exists, >= 70 lines, mirrors diagnose's shape. Frontmatter has `name: qa` + the use-case-first description (with the three glyphs) + `disable-model-invocation: true` + `argument-hint`, no v1 fields. Body has `## Why this matters`, `## When to use this` (with a Don't), a bold-numbered `## The flow` (one-call `init qa` orient with `@file:` guard via the literal engine path, project's-own-command detection + per-module fallback, all 5 categories agnostically, the embedded report skeleton with the three glyphs + file:line + verdict, /diagnose handoff), and a `▶ NEXT` footer referencing /diagnose + /sentinel. The cross-workspace check names `.sovereign/docs/api/API_SPEC.md`. No hardcoded default runner. The full verify chain prints OK.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify the engine gates hold (budget · lint · suite)</name>
  <files>engine/skills/qa/SKILL.md</files>
  <read_first>
    - .planning/phases/18-qa-skill/18-VALIDATION.md (gates G15–G17 — the budget/lint/suite gates, all RUN FROM engine/).
    - 18-RESEARCH.md Pitfall 1 (gates walk `<cwd>/skills`, NOT `engine/skills/` → run from engine/ or pass vacuously) and Pitfall 2 (`doctor` exits 0 regardless → ASSERT the JSON field).
  </read_first>
  <action>
    No file edits — this task ASSERTS the verification gates after Task 1 authored the skill. Run ALL gates from `engine/` (the gates walk `<cwd>/skills`, not `engine/skills/`; from the repo root they pass vacuously with `total_skills: 0`). `doctor` exits 0 regardless of warnings, so ASSERT the JSON values — do NOT trust the exit code.

    G16 — doctor budget held at 5 (qa is user-invoked → disabled tier). Expected: total 20, auto 5, disabled 15. Assert all three JSON fields:
    ```bash
    cd engine && node bin/sovereign-tools.cjs doctor \
      | grep -E '"(total_skills|auto_count|disabled_count)"'
    # MUST show: "total_skills": 20, "auto_count": 5, "disabled_count": 15
    ```

    G15 — validate skills passes for qa:
    ```bash
    cd engine && node bin/sovereign-tools.cjs validate skills skills/qa/SKILL.md \
      | grep -F '"valid": true'
    ```

    G17 — engine suite still 164 green (NO engine change was made, so the count must be unchanged). The pass line uses the U+2139 info char `ℹ`, NOT `#`:
    ```bash
    cd engine && node --test "test/**/*.test.cjs" 2>&1 | grep -E 'ℹ (pass|fail) '
    # MUST show: ℹ pass 164  AND  ℹ fail 0
    ```

    If doctor shows `total_skills: 19` you ran from the wrong directory (or qa wasn't created); if `auto_count: 6` the `disable-model-invocation: true` flag is missing/malformed — fix Task 1, do NOT touch init.cjs. If the suite is not 164, an unintended engine change crept in — revert it (this phase makes NO engine change).
  </action>
  <acceptance_criteria>
    - G16: `cd engine && node bin/sovereign-tools.cjs doctor` JSON has `"total_skills": 20`, `"auto_count": 5`, `"disabled_count": 15`.
    - G15: `cd engine && node bin/sovereign-tools.cjs validate skills skills/qa/SKILL.md` JSON has `"valid": true` (exit 0).
    - G17: `cd engine && node --test "test/**/*.test.cjs"` reports `ℹ pass 164` and `ℹ fail 0`.
  </acceptance_criteria>
  <verify>
    <automated>cd engine && node bin/sovereign-tools.cjs doctor | grep -qE '"auto_count":[[:space:]]*5' && node bin/sovereign-tools.cjs doctor | grep -qE '"total_skills":[[:space:]]*20' && node bin/sovereign-tools.cjs doctor | grep -qE '"disabled_count":[[:space:]]*15' && node bin/sovereign-tools.cjs validate skills skills/qa/SKILL.md | grep -qF '"valid": true' && node --test "test/**/*.test.cjs" 2>&1 | grep -qE 'ℹ pass 164' && node --test "test/**/*.test.cjs" 2>&1 | grep -qE 'ℹ fail 0' && echo OK</automated>
  </verify>
  <done>
    From `engine/`: doctor reports total_skills 20 / auto_count 5 / disabled_count 15; `validate skills skills/qa/SKILL.md` returns `"valid": true`; the engine suite reports `ℹ pass 164` / `ℹ fail 0`. No engine source changed. The verify chain prints OK.
  </done>
</task>

</tasks>

<verification>
Phase 18 is complete when every VALIDATION gate (G1–G17) passes:
- G1–G14 (structural + coverage-in-prose + composition): Task 1's verify chain prints OK.
- G15 (validate skills `"valid": true`), G16 (doctor 20/5/15), G17 (suite 164 green): Task 2's verify chain prints OK, run from `engine/`.

CRITICAL: doctor/validate gates MUST run from `engine/` (they walk `<cwd>/skills`, not `engine/skills/`) — from the repo root they pass vacuously. `doctor` exits 0 regardless of warnings, so the verify ASSERTS the JSON field values (auto_count 5, total 20, disabled 15), not the exit code. The suite pass line uses the U+2139 `ℹ` info char (`ℹ pass 164`), not `#`.

Manual eyeball (judgment, from 18-VALIDATION.md): the sweep reads as genuinely relentless + agnostic (every category actionable per the project's own tools), the report skeleton is concrete and copyable, and the /diagnose + /sentinel hand-offs are real composition (not name-drops).
</verification>

<success_criteria>
SC1 — `qa` sweeps each workspace/module using its OWN toolchain across all 5 categories (static correctness, tests, dependency & wiring integrity, navigation/routing, cross-workspace consistency incl. `.sovereign/docs/api/API_SPEC.md`). [G8–G11]
SC2 — reports check/cross/warn glyphs grouped module→category, failure with exact error + `file:line`, one-line verdict; drives the project's own `qa` command when present else per-module equivalents — stack-agnostic, no hardcoded default runner. [G11–G13, G6–G7]
SC3 — core-tier thin-orchestrator shape (one `init qa` orient, `## Why this matters`, recommendation-first, navigation footer); `disable-model-invocation: true` (doctor auto-budget held at 5); `validate skills` passes; composes with `/diagnose` and complements `/sentinel`; engine suite stays 164 green. [G1–G5, G14, G15–G17]
</success_criteria>

<output>
After completion, create `.planning/phases/18-qa-skill/18-01-SUMMARY.md`.
</output>
