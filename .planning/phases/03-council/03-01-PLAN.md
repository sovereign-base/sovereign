---
phase: 03-council
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/skills/council/lenses.md
autonomous: true
requirements: [CNL-01]
must_haves:
  truths:
    - "Five distinct advisor lenses (Skeptic, Architect, Builder, Outsider, Risk Officer) exist as injectable verbatim prompt text"
    - "An executor reading lenses.md knows exactly what JSON each subagent (advisor, peer-reviewer, chairman) must return"
  artifacts:
    - path: "engine/skills/council/lenses.md"
      provides: "The 5 locked lens prompts + the three subagent JSON return contracts"
      contains: "The Skeptic"
      min_lines: 60
  key_links:
    - from: "engine/skills/council/lenses.md"
      to: "engine/agents/sovereign-advisor.md"
      via: "lens text injected as the advisor's <lens> at dispatch"
      pattern: "Skeptic|Architect|Builder|Outsider|Risk Officer"
---

<objective>
Author `engine/skills/council/lenses.md` — the progressive-disclosure content file the Council orchestrator loads at runtime to inject one verbatim lens per advisor, plus the pinned subagent JSON return contracts the orchestrator validates at its boundary.

This is the CONTENT layer of the Council (CNL-01: five distinct advisor perspectives). It lives in a separate file (not the SKILL.md header) so it is loaded only when Council actually runs, keeping the skill body thin and off the listing budget. Per R-004 this is HAND-AUTHORED by the orchestrator, not generated by an executor.

Purpose: Give Plan 03-02's SKILL.md a single file to read for (a) the five locked lens texts to fan out, and (b) the exact JSON shapes the advisor/peer-reviewer/chairman return — so the orchestrator never improvises personas or contracts.
Output: `engine/skills/council/lenses.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-council/03-CONTEXT.md

# Source content to mine (locked v1 lens text + tensions + report layout)
@archive/v1/skills/council/SKILL.md

# The agent shells these contracts refine (advisor is a parameterized lens-shell;
# peer-reviewer reads anonymized A-E; chairman is the single fan-in)
@engine/agents/sovereign-advisor.md
@engine/agents/sovereign-peer-reviewer.md
@engine/agents/sovereign-chairman.md

<interfaces>
<!-- The advisor shell is PARAMETERIZED: it has NO baked persona. lenses.md owns the persona text.
     The advisor returns { ok, lens, position, confidence, key_points[], risks[], recommendation }.
     The peer-reviewer returns { ok, reviews[ {label, strengths[], weaknesses[], overlooked[]} ], cross_cutting_concerns[] }.
     The chairman returns { ok, verdict: PASS|CONDITIONAL_PASS|BLOCKED, synthesis, conditions[], dissents_addressed[], confidence }.
     These three schemas are ALREADY pinned in the agent shells above — lenses.md must restate them verbatim
     (field names exactly as in the shells) so the orchestrator validates returns against one reference. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the five locked advisor lenses</name>
  <files>engine/skills/council/lenses.md</files>
  <read_first>
    - archive/v1/skills/council/SKILL.md (lines 77-95: "The Five Advisors" table + natural tensions; lines 160-167 advisor output framing — "leans fully into lens, no hedging")
    - .planning/phases/03-council/03-CONTEXT.md (lines 39-46: the LOCKED lens text to inject verbatim + the natural tensions to preserve)
    - engine/agents/sovereign-advisor.md (the shell consuming this lens as its <lens>)
  </read_first>
  <action>
    Create `engine/skills/council/lenses.md`. Open with a one-paragraph header explaining this file is loaded at runtime by the council orchestrator (progressive disclosure — NOT in the SKILL.md frontmatter) and that each block below is injected verbatim as one advisor's `<lens>` at dispatch.

    Author the five lenses, each as its own `##` section, copying the LOCKED text from 03-CONTEXT.md lines 39-46 VERBATIM (these are authorial — do not paraphrase). Each section names the advisor, its emoji, and its directive prose:
      - "## 🔴 The Skeptic (Risk Hunter)" — "Assume this has a fatal flaw and find it. Attack assumptions, question the problem statement, identify what will fail and why. Do not hedge. Do not balance. Your job is to break this."
      - "## 🔵 The Architect (Systems Thinker)" — "Probe technical feasibility and hidden complexity. Identify what hasn't been thought through — scaling cliffs, integration nightmares, data-model problems. Think three steps ahead."
      - "## 🟡 The Builder (Action-First)" — "Only care about what gets built Monday morning. If it sounds brilliant but has no clear first step or is too vague to implement, say so. Turn everything into concrete, executable decisions."
      - "## ⚪ The Outsider (Fresh Eyes)" — "You have zero context about this project, domain, or history. Catch the curse of knowledge — things obvious to the builder but confusing to everyone else. Question jargon. Question assumptions treated as fact."
      - "## 🟣 The Risk Officer (Compliance & Safety)" — "Focus on legal, regulatory, security, ethical, and operational risk. Surface what could go catastrophically wrong — compliance, data privacy, failure modes, reputational risk."

    After the five lenses, add a "## Natural tensions" subsection preserving the locked tensions: Skeptic vs Builder (stop vs go); Architect vs Builder (rethink vs ship); Outsider vs everyone (assumptions vs reality). Note these tensions are intentional — advisors must NOT converge.

    Each lens block must instruct the advisor to lean FULLY into its lens — no hedging, no balance, no pre-emptive synthesis (synthesis is the chairman's job).
  </action>
  <verify>
    <automated>grep -q "The Skeptic" engine/skills/council/lenses.md && grep -q "The Architect" engine/skills/council/lenses.md && grep -q "The Builder" engine/skills/council/lenses.md && grep -q "The Outsider" engine/skills/council/lenses.md && grep -q "The Risk Officer" engine/skills/council/lenses.md && grep -q "Natural tensions" engine/skills/council/lenses.md && echo PASS</automated>
  </verify>
  <done>lenses.md exists and names all five locked advisors with their verbatim directive text plus the natural-tensions note. grep confirms all five names + "Natural tensions".</done>
</task>

<task type="auto">
  <name>Task 2: Pin the three subagent JSON return contracts</name>
  <files>engine/skills/council/lenses.md</files>
  <read_first>
    - engine/agents/sovereign-advisor.md (return_schema: ok, lens, position, confidence, key_points[], risks[], recommendation)
    - engine/agents/sovereign-peer-reviewer.md (return_schema: ok, reviews[ {label, strengths[], weaknesses[], overlooked[]} ], cross_cutting_concerns[])
    - engine/agents/sovereign-chairman.md (return_schema: ok, verdict PASS|CONDITIONAL_PASS|BLOCKED, synthesis, conditions[], dissents_addressed[], confidence)
    - .planning/phases/03-council/03-CONTEXT.md (lines 48-52: the contracts "enforced at the orchestrator boundary")
  </read_first>
  <action>
    Append a "## Subagent return contracts" section to `engine/skills/council/lenses.md`. This is the single reference the orchestrator validates returns against — so the field names MUST match the agent shells exactly (do not invent new field names; the shells are the source of truth).

    Three fenced ```json blocks, one per agent, each preceded by a one-line "the orchestrator MUST treat ok:false as a dispatch failure" note:

    1. "### Advisor returns" — `{ "ok": true, "lens": "...", "position": "support|oppose|conditional", "confidence": 0.0, "key_points": [], "risks": [], "recommendation": "..." }` (matches sovereign-advisor.md).
    2. "### Peer reviewer returns" — `{ "ok": true, "reviews": [ { "label": "A", "strengths": [], "weaknesses": [], "overlooked": [] } ], "cross_cutting_concerns": [] }` (matches sovereign-peer-reviewer.md; note `label` echoes the orchestrator's A–E labels and the reviewer must NOT re-identify the lens).
    3. "### Chairman returns" — `{ "ok": true, "verdict": "PASS|CONDITIONAL_PASS|BLOCKED", "synthesis": "...", "conditions": [], "dissents_addressed": [], "confidence": 0.0 }` (matches sovereign-chairman.md; `conditions` non-empty when verdict is CONDITIONAL_PASS).

    Add a closing note: all three agents output JSON ONLY (no prose), and the orchestrator — never the agents — is the only writer to disk (file-race safety, CNL-04). State explicitly that the verdict enum is the engine/agent form `CONDITIONAL_PASS` (underscore), and the SKILL's human-facing report renders it as "CONDITIONAL PASS".
  </action>
  <verify>
    <automated>grep -q "Subagent return contracts" engine/skills/council/lenses.md && grep -q "CONDITIONAL_PASS" engine/skills/council/lenses.md && grep -q "cross_cutting_concerns" engine/skills/council/lenses.md && grep -q "dissents_addressed" engine/skills/council/lenses.md && grep -Eq "position.*support|support.*oppose" engine/skills/council/lenses.md && echo PASS</automated>
  </verify>
  <done>lenses.md contains the three pinned JSON contracts with field names matching the agent shells (advisor position enum, peer-reviewer cross_cutting_concerns, chairman verdict + dissents_addressed), plus the JSON-only + orchestrator-only-write notes.</done>
</task>

</tasks>

<verification>
- `engine/skills/council/lenses.md` exists.
- All five locked lenses present by name with verbatim directive text (CNL-01).
- Natural-tensions note present.
- Three subagent JSON contracts present with field names matching `engine/agents/sovereign-*.md`.
- Combined grep gate:
  `grep -q "The Skeptic" engine/skills/council/lenses.md && grep -q "The Risk Officer" engine/skills/council/lenses.md && grep -q "Subagent return contracts" engine/skills/council/lenses.md && grep -q "CONDITIONAL_PASS" engine/skills/council/lenses.md`
</verification>

<success_criteria>
- Five distinct lenses authored verbatim from the locked CONTEXT text (CNL-01 content basis).
- The orchestrator (03-02) has one file to load for both the lens prompts and the return contracts.
- No invented field names — all three contracts mirror the Phase-2 agent shells exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-council/03-01-SUMMARY.md`.
</output>
