---
phase: 03-council
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - engine/skills/council/SKILL.md
autonomous: true
requirements: [CNL-01, CNL-02, CNL-03, CNL-04]
must_haves:
  truths:
    - "A user runs /council --standard \"<decision>\" and the skill orients with one `init council` call, hard-erroring if advisor agents are missing"
    - "Five advisors are dispatched IN PARALLEL, one per lens, each injected with the project context paths + the neutralized question"
    - "Advisor responses are shuffled to anonymized A–E and a single peer-reviewer is dispatched over the anonymized set BEFORE synthesis"
    - "The chairman produces a binding verdict (PASS / CONDITIONAL PASS / BLOCKED) which the ORCHESTRATOR ALONE writes to .sovereign/council/, then runs state save + gate pass + commit and prints a navigation footer"
  artifacts:
    - path: "engine/skills/council/SKILL.md"
      provides: "The thin Council orchestrator: frontmatter, Why-this-matters, the 7-dispatch --standard flow, report format, nav footer"
      contains: "disable-model-invocation: true"
      min_lines: 120
  key_links:
    - from: "engine/skills/council/SKILL.md"
      to: "sovereign-tools init council"
      via: "the single orientation call (with @file: spill handling)"
      pattern: "init council"
    - from: "engine/skills/council/SKILL.md"
      to: "engine/skills/council/lenses.md"
      via: "the flow loads lenses.md to inject one verbatim lens per advisor"
      pattern: "lenses\\.md"
    - from: "engine/skills/council/SKILL.md"
      to: ".sovereign/council/"
      via: "orchestrator-only transcript write to paths.transcript"
      pattern: "\\.sovereign/council"
    - from: "engine/skills/council/SKILL.md"
      to: "sovereign-tools gate pass / state save / commit"
      via: "the side-effecting cycle after the verdict"
      pattern: "gate pass|state save|commit"
---

<objective>
Author `engine/skills/council/SKILL.md` — the thin Council orchestrator for `--standard`. This is the flagship reasoning skill and the integration proof: when it runs end-to-end it exercises every engine primitive (init → context injection → 5 parallel advisors → anonymized peer review → chairman verdict → orchestrator-only transcript write → state save → gate pass → commit → footer).

This is HAND-AUTHORED prose (R-004), not executor-generated code. It orients with ONE `init council` call and reads `lenses.md` (from Plan 03-01) for the five verbatim lenses and the subagent return contracts.

Purpose: Make `/council --standard "<decision>"` real — satisfying CNL-01 (5 parallel advisors), CNL-02 (anonymized A–E peer review before synthesis), CNL-03 (project-context injection via init paths), and CNL-04 (chairman verdict + orchestrator-only timestamped transcript in `.sovereign/council/` + gate-log reference).
Output: `engine/skills/council/SKILL.md`.

Scope discipline: `--standard` ONLY. `--express` / `--deep` / multi-model / every-advisor-re-reviews are DEFERRED (M4) — the skill merely notices the flag and prints a "deferred — running --standard" notice. Do NOT author those flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-council/03-CONTEXT.md

# The content file this skill loads at runtime (authored in Plan 03-01)
@engine/skills/council/lenses.md

# Locked v1 content to mine: Why-this-matters, report layout, when-to-use, nav
@archive/v1/skills/council/SKILL.md

# The one blob this skill consumes — exact field names live here
@engine/bin/lib/init.cjs

# The agents this skill dispatches (names + return schemas)
@engine/agents/sovereign-advisor.md
@engine/agents/sovereign-peer-reviewer.md
@engine/agents/sovereign-chairman.md

# Dispatch + side-effecting mechanics already proven
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-engine-foundation/01-05-SUMMARY.md

# The disable-model-invocation / listing-budget convention
@engine/references/listing-budget.md

<interfaces>
<!-- `init council` returns this nested blob (from engine/bin/lib/init.cjs buildInit, case 'council'):
{
  project_root, sovereign_version,
  models: { advisor, chairman, peer_reviewer },     // resolved model aliases — inject as Task model=
  config: { model_profile, commit_docs, council_mode_default, parallelization },
  phase: { current, name, gate_status, active_tracks },
  context_injection: { manifest_path, constitution_path, glossary_path, relevant_adrs[] },
  paths: { council_dir: ".sovereign/council", transcript: ".sovereign/council/council-<YYYYMMDD>-001.md", state, manifest },
  exists: { sovereign_dir, manifest, constitution, glossary },
  agents_installed: boolean, missing_agents: []      // HARD ERROR if false
}
The skill reads context_injection paths' CONTENT (MANIFEST/CONTEXT/relevant ADRs) only to inject into advisors.
It passes PATHS to advisors (path-passing, not content-passing) per ARCHITECTURE anti-pattern #3.
@file: spill: `INIT=$(node ... init council); if [[ "$INIT" == @file:* ]]; then INIT=$(cat "${INIT#@file:}"); fi`

Agent dispatch names (subagent_type): sovereign-advisor (x5, parallel, one <lens> each),
sovereign-peer-reviewer (x1, over anonymized A–E), sovereign-chairman (x1, fan-in).
Return contracts are pinned in lenses.md (authored 03-01). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the frontmatter, Why-this-matters, and When-to-use header</name>
  <files>engine/skills/council/SKILL.md</files>
  <read_first>
    - engine/references/listing-budget.md (the disable-model-invocation convention + why orchestrator-only skills set it)
    - archive/v1/skills/council/SKILL.md (lines 20-59: "Why This Matters" + "When to Use" — mine the prose; do NOT copy the v1 non-standard frontmatter at lines 1-18)
    - .planning/phases/03-council/03-CONTEXT.md (lines 24-27: the skill shape — thin orchestrator, disable-model-invocation rationale, Why-this-matters requirement)
    - CLAUDE.md (Skill frontmatter spec: name ≤64 lowercase-hyphen no claude/anthropic; description + when_to_use within cap)
  </read_first>
  <action>
    Create `engine/skills/council/SKILL.md`. Author REAL Agent Skills frontmatter (NOT v1's `triggers`/`works-best-with`/`min-model`/`tokens`/bare `phase` — those are dropped per ADR-(d)). The frontmatter block (YAML) MUST include:
      - `name: council`
      - `description:` one line — "Five advisor personas argue your decision in parallel, an anonymous peer-review round catches what each missed, and a chairman returns a binding verdict. Run before any decision that is expensive to undo." (keep within the description cap; lead with the key use case).
      - `disable-model-invocation: true` — Council is user-invoked via `/council` and side-effecting, and this keeps it off the auto-trigger listing budget (cite the listing-budget convention in a comment or the body, not just here).
      - `argument-hint: "[--standard] \"<decision>\""` (optional but helpful).

    Then author two body sections mining the v1 prose verbatim-in-spirit:
      1. "## Why this matters" — the plain-language framing: AI is sycophantic; "should I?" vs "is this a bad idea?" flips the answer on the same idea; the Council forces structured disagreement across five lenses + blind peer review + a chairman verdict you can trust; run before any expensive-to-undo decision. (Mine v1 lines 20-38.)
      2. "## When to use this" — bullet the genuine use cases (build-or-not, architecture choice, significant tech choice, pivot, irreversible decisions) and the anti-uses (settled questions / validation-seeking, trivial details, quick facts). (Mine v1 lines 42-58.)

    Add a one-line scope note under When-to-use: "`--express` and `--deep` are deferred to a later milestone; this skill runs `--standard`. If invoked with `--express` or `--deep`, it prints a one-line notice and proceeds as `--standard`."
  </action>
  <verify>
    <automated>grep -q "^name: council" engine/skills/council/SKILL.md && grep -q "disable-model-invocation: true" engine/skills/council/SKILL.md && grep -qi "Why this matters" engine/skills/council/SKILL.md && grep -qi "When to use" engine/skills/council/SKILL.md && node engine/bin/sovereign-tools.cjs validate skills --cwd "$(node -e 'const fs=require("fs"),os=require("os"),p=require("path");const d=fs.mkdtempSync(p.join(os.tmpdir(),"cval-"));fs.mkdirSync(p.join(d,"skills","council"),{recursive:true});fs.copyFileSync("engine/skills/council/SKILL.md",p.join(d,"skills","council","SKILL.md"));process.stdout.write(d)')" && echo PASS</automated>
  </verify>
  <done>SKILL.md has valid Agent Skills frontmatter (name council, disable-model-invocation true), a Why-this-matters section, a When-to-use section with the deferred-flags note, and passes `sovereign-tools validate skills` (name ≤64 lowercase-hyphen no claude/anthropic, description within cap).</done>
</task>

<task type="auto">
  <name>Task 2: Author the 7-dispatch --standard flow (init → 5 advisors → peer review → chairman → write → side effects → footer)</name>
  <files>engine/skills/council/SKILL.md</files>
  <read_first>
    - engine/bin/lib/init.cjs (buildInit case 'council': the exact field names — models{advisor,chairman,peer_reviewer}, context_injection{manifest_path,constitution_path,glossary_path,relevant_adrs}, paths{council_dir,transcript,state,manifest}, agents_installed/missing_agents)
    - engine/skills/council/lenses.md (the five lenses to inject + the three return contracts — authored in 03-01)
    - .planning/research/ARCHITECTURE.md (lines 261-303: Pattern 2 fan-out/fan-in, path-passing not content-passing, the canonical command flow; anti-pattern #3 + #4)
    - .planning/phases/03-council/03-CONTEXT.md (lines 29-37: the exact 8-step flow + orchestrator-only write)
    - .planning/phases/01-engine-foundation/01-05-SUMMARY.md (the @file: spill convention + engine command surface)
  </read_first>
  <action>
    Append a "## The --standard flow" section to `engine/skills/council/SKILL.md` authored as numbered orchestrator steps a Claude can execute without interpretation. Bake in the orchestrator-only-write rule explicitly (CNL-04). The steps:

    1. **Orient (one call).** Run `INIT=$(node <path>/sovereign-tools.cjs init council)` then the `@file:` spill guard (`if [[ "$INIT" == @file:* ]]; then INIT=$(cat "${INIT#@file:}"); fi`). Parse: `models.advisor/chairman/peer_reviewer`, `context_injection.*` paths, `paths.transcript`/`paths.council_dir`/`paths.state`/`paths.manifest`, `config.commit_docs`, `phase.current`/`phase.gate_status`, `agents_installed`/`missing_agents`. State explicitly: the skill performs ZERO other orientation file reads.
    2. **Agents guard (hard error, no fallback).** If `agents_installed` is false, STOP and print the missing advisor names from `missing_agents` with a fix hint (`npx sovereign init --full`). NEVER silently fall back to a general-purpose agent (ARCHITECTURE anti-pattern #4).
    3. **Inject context + neutralize the question.** Read the CONTENT of `context_injection.manifest_path`, `context_injection.glossary_path`, and any `context_injection.relevant_adrs` (only now, only because the advisors need it). Strip loaded language from the user's question → a neutral framing. Print the "COUNCIL CONVENING" header (neutral question + injected phase/decisions/glossary terms), mining v1's Step-2 layout (archive lines 140-151).
    4. **Round 1 — 5 advisors IN PARALLEL.** Load `lenses.md`. Dispatch FIVE `Task(subagent_type="sovereign-advisor", model=<models.advisor>)` calls in a single parallel batch — one per lens — each prompt carrying (a) that lens's verbatim text from lenses.md as `<lens>`, (b) the `context_injection` PATHS (path-passing, not content-passing — the advisor reads them itself), and (c) the neutral question. Each advisor returns the pinned advisor JSON. Treat any `ok:false` as a failed advisor (note it, continue if ≥? quorum — or surface and stop; pick the simpler: surface the failure).
    5. **Round 2 — anonymized peer review (the resolved spike).** The orchestrator SHUFFLES the five advisor responses into anonymized labels A–E (randomize the lens→label mapping; keep a private mapping the orchestrator alone holds). Dispatch ONE `Task(subagent_type="sovereign-peer-reviewer", model=<models.peer_reviewer>)` over the anonymized A–E set. It returns the pinned peer-reviewer JSON (per-label strengths/weaknesses/overlooked + cross_cutting_concerns). The reviewer never learns which lens is which (CNL-02). Do NOT run the M4 "every advisor re-reviews" variant.
    6. **Round 3 — chairman verdict.** Dispatch ONE `Task(subagent_type="sovereign-chairman", model=<models.chairman>)` with the five (de-anonymized for the chairman) advisor positions + the peer review. It returns the pinned chairman JSON: `verdict` PASS|CONDITIONAL_PASS|BLOCKED, synthesis (agreements / genuine clashes / blind spots caught), conditions[], dissents_addressed[], a recommendation, and one first action.
    7. **Persist — ORCHESTRATOR-ONLY write.** The orchestrator — and ONLY the orchestrator — writes the full transcript to `paths.transcript` (`.sovereign/council/council-<stamp>-001.md`). Advisors/reviewer/chairman NEVER write files (file-race safety, CNL-04). State this prohibition in-line.
    8. **Side effects + footer.** Then, in order: `node <path>/sovereign-tools.cjs state save` (regenerates MANIFEST); `node <path>/sovereign-tools.cjs gate pass <phase.current>` IF the council was convened at a phase gate (reference the transcript path in the gate log); `node <path>/sovereign-tools.cjs commit "council: <sanitized verdict summary>" --files <paths.transcript> <paths.state> <paths.manifest>` (commit honors commit_docs + gitignore — the engine gates it). Finish with the navigation footer (Task 3).

    Throughout: pass PATHS to subagents, never file contents (anti-pattern #3). Use the resolved `models.*` aliases as the `Task` model. Reference `lenses.md` by name in step 4 and `init council` by name in step 1 (these are the verifiable links).
  </action>
  <verify>
    <automated>grep -q "init council" engine/skills/council/SKILL.md && grep -q "@file:" engine/skills/council/SKILL.md && grep -q "agents_installed" engine/skills/council/SKILL.md && grep -q "lenses.md" engine/skills/council/SKILL.md && grep -q "sovereign-advisor" engine/skills/council/SKILL.md && grep -q "sovereign-peer-reviewer" engine/skills/council/SKILL.md && grep -q "sovereign-chairman" engine/skills/council/SKILL.md && grep -Eq "A.?.?E|A–E|A-E" engine/skills/council/SKILL.md && grep -q "paths.transcript\|.sovereign/council" engine/skills/council/SKILL.md && grep -q "gate pass" engine/skills/council/SKILL.md && grep -q "state save" engine/skills/council/SKILL.md && grep -q "commit" engine/skills/council/SKILL.md && echo PASS</automated>
  </verify>
  <done>The flow section specifies all 8 ordered steps: one `init council` call + @file: spill, agents_installed hard error, context injection from init paths (CNL-03), 5 parallel sovereign-advisor dispatches one per lens (CNL-01), anonymized A–E single sovereign-peer-reviewer before synthesis (CNL-02), sovereign-chairman verdict, orchestrator-only transcript write to .sovereign/council/ + gate pass reference (CNL-04), and state save + commit. grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 3: Author the report/transcript format and navigation footer</name>
  <files>engine/skills/council/SKILL.md</files>
  <read_first>
    - archive/v1/skills/council/SKILL.md (lines 160-229: advisor/peer-review/chairman/verdict report blocks; lines 290-307: Output table + Navigation; lines 310-417: the worked example to mirror the layout)
    - engine/skills/council/lenses.md (verdict enum note: engine form CONDITIONAL_PASS rendered as "CONDITIONAL PASS")
    - .planning/phases/03-council/03-CONTEXT.md (lines 54-58: report format to mine; nav footer requirement)
  </read_first>
  <action>
    Append two sections to `engine/skills/council/SKILL.md`:

    1. "## Report format" — the human-facing SOVEREIGN COUNCIL REPORT layout the orchestrator renders AND writes to the transcript, mining v1 lines 160-229: a header (mode `--standard` / phase / neutral question), the five advisor blocks each with its emoji + lens name + position, a "PEER REVIEW (anonymous)" block (strongest / biggest blind spot / missed-by-everyone, referencing A–E labels), a "CHAIRMAN SYNTHESIS" block (where the council agrees / where it genuinely clashes / blind spots caught / recommendation / one thing to do first), and a "COUNCIL VERDICT" line rendering PASS / CONDITIONAL PASS / BLOCKED (note: map the agent enum `CONDITIONAL_PASS` → display "CONDITIONAL PASS"). End the transcript with a "Saved to: .sovereign/council/council-<stamp>-001.md" footer line.

    2. "## Navigation" footer — a recommendation-first navigation block: the recommended next action (derived from the chairman's "first action") + the transcript path + the copy-paste command(s) to act on it. Mine v1 lines 300-307 for the before/after navigation idea, retargeted to v2 skills (e.g. after a PASS: `/grill-with-docs` to interrogate the spec; after BLOCKED: address conditions first). Keep it short.

    Confirm the file as a whole stays a THIN orchestrator: flow + format + footer, with NO engine/bookkeeping logic inlined (all state/gate/commit work is delegated to `sovereign-tools` per step 8). Re-run the skills linter at the end.
  </action>
  <verify>
    <automated>grep -qi "COUNCIL VERDICT\|Report format" engine/skills/council/SKILL.md && grep -q "PEER REVIEW" engine/skills/council/SKILL.md && grep -q "CHAIRMAN" engine/skills/council/SKILL.md && grep -qi "CONDITIONAL PASS" engine/skills/council/SKILL.md && grep -q "BLOCKED" engine/skills/council/SKILL.md && grep -qi "Navigation" engine/skills/council/SKILL.md && grep -q "Saved to:\|.sovereign/council" engine/skills/council/SKILL.md && node engine/bin/sovereign-tools.cjs validate skills --cwd "$(node -e 'const fs=require("fs"),os=require("os"),p=require("path");const d=fs.mkdtempSync(p.join(os.tmpdir(),"cval2-"));fs.mkdirSync(p.join(d,"skills","council"),{recursive:true});fs.copyFileSync("engine/skills/council/SKILL.md",p.join(d,"skills","council","SKILL.md"));process.stdout.write(d)')" && echo PASS</automated>
  </verify>
  <done>SKILL.md has a Report-format section (advisor blocks + PEER REVIEW + CHAIRMAN SYNTHESIS + COUNCIL VERDICT with PASS/CONDITIONAL PASS/BLOCKED + Saved-to footer) and a recommendation-first Navigation footer; the skill remains thin and passes `validate skills`.</done>
</task>

</tasks>

<verification>
- `engine/skills/council/SKILL.md` exists, is a thin orchestrator, passes `sovereign-tools validate skills`.
- Frontmatter: `name: council`, `disable-model-invocation: true`, valid description (CNL-04 footer convention, SKL-07 convention).
- Flow covers: one `init council` orientation + @file: spill, `agents_installed` hard error, context injection from init paths (CNL-03), 5 parallel `sovereign-advisor` dispatches one per lens (CNL-01), anonymized A–E single `sovereign-peer-reviewer` before synthesis (CNL-02), `sovereign-chairman` verdict, orchestrator-only transcript write to `.sovereign/council/` + `gate pass` reference + `state save` + `commit` (CNL-04).
- Report format + navigation footer present.
- Combined grep gate (run all three task <automated> blocks; all print PASS).
</verification>

<success_criteria>
- `/council --standard "<decision>"` is fully specified end-to-end: a Claude executing this SKILL.md needs no further interpretation.
- All four phase requirements covered: CNL-01 (5 parallel advisors), CNL-02 (anonymized peer review pre-synthesis), CNL-03 (context injection via init paths), CNL-04 (chairman verdict + orchestrator-only transcript + gate ref).
- Thin orchestrator discipline held: every state/gate/commit/model action is delegated to `sovereign-tools`; the skill holds flow + content only.
- `--express`/`--deep`/multi-model NOT authored — only the deferred notice.
</success_criteria>

<output>
After completion, create `.planning/phases/03-council/03-02-SUMMARY.md`.
</output>
