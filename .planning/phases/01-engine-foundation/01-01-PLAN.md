---
phase: 01-engine-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/ADR-002-zero-dep-cjs-engine.md
  - docs/adr/ADR-009-cjs-packaging.md
  - docs/adr/ADR-010-commands-as-skill-directories.md
  - docs/adr/ADR-011-drop-v1-frontmatter.md
  - docs/adr/ADR-012-manifest-engine-derived.md
  - docs/adr/ADR-013-subagent-return-json-schema.md
  - engine/package.json
  - engine/.gitignore
  - engine/bin/sovereign-tools.cjs
  - engine/bin/sovereign.cjs
  - engine/VERSION
  - engine/test/smoke.test.cjs
  - templates/sovereign/MANIFEST.md
  - templates/sovereign/SOVEREIGN.md
  - templates/sovereign/CONTEXT.md
  - templates/sovereign/STATE.md
  - templates/sovereign/config.json
autonomous: true
requirements: [STATE-01, ENG-06]

must_haves:
  truths:
    - "The six locked Phase-1 ADRs exist as durable files under docs/adr/, each stating decision + rationale."
    - "An engine/ directory exists with a CJS package.json (no \"type\":\"module\"), engines.node >= 20, and two bin entries with the node shebang."
    - "node --test runs against engine/ and the placeholder smoke test passes (proves the harness + shebang + CJS load)."
    - "templates/sovereign/ holds the seed .sovereign/ tree files (MANIFEST, SOVEREIGN, CONTEXT, STATE, config.json) using **Field:** value lines."
  artifacts:
    - path: "engine/package.json"
      provides: "CJS package manifest, bin entries, engines.node, files allowlist, test script"
      contains: "\"sovereign-tools\""
    - path: "engine/bin/sovereign-tools.cjs"
      provides: "Engine entry point with shebang (stub router in this plan)"
      contains: "#!/usr/bin/env node"
    - path: "templates/sovereign/STATE.md"
      provides: "Field-patchable state template"
      contains: "**Phase:**"
    - path: "docs/adr/ADR-012-manifest-engine-derived.md"
      provides: "Locks MANIFEST-is-derived decision before code"
      contains: "Status:"
  key_links:
    - from: "engine/package.json"
      to: "engine/bin/sovereign-tools.cjs"
      via: "bin map"
      pattern: "sovereign-tools.*bin/sovereign-tools.cjs"
---

<objective>
Record the six "ADRs before code" as durable files, then scaffold the `engine/` package skeleton and the `templates/sovereign/` seed tree. This is the foundation everything else in Phase 1 builds on: no engine command can be written until the package, bin entries, test harness, and state templates exist.

Purpose: Make the locked architectural decisions durable (so later phases cannot silently relitigate them) and stand up the zero-dependency CJS package shell that plans 02-05 fill in.
Output: `docs/adr/ADR-002,009-013`, `engine/package.json` + bin stubs + VERSION + placeholder test, and `templates/sovereign/` (MANIFEST/SOVEREIGN/CONTEXT/STATE/config.json).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-engine-foundation/01-CONTEXT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@SOVEREIGN.md

<!-- Reference shapes to lift (read, do not copy wholesale): -->
<!-- ~/.claude/get-shit-done/bin/gsd-tools.cjs — shebang + main() shape -->
<!-- ~/.claude/get-shit-done/VERSION — VERSION file convention -->
<!-- archive/v1/templates/{MANIFEST,SOVEREIGN,CONTEXT}.md — template content to mine -->

<interfaces>
<!-- package.json bin contract that plans 02-05 depend on -->
bin: {
  "sovereign":       "bin/sovereign.cjs",        // user-facing launcher (stubbed this phase)
  "sovereign-tools": "bin/sovereign-tools.cjs"   // the engine (filled by plans 02-04)
}
engines: { "node": ">=20" }
// NO "type":"module" — CommonJS. files allowlist: ["bin","templates","VERSION"]
// test script: "node --test test/"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the six locked ADRs under docs/adr/</name>
  <files>docs/adr/ADR-002-zero-dep-cjs-engine.md, docs/adr/ADR-009-cjs-packaging.md, docs/adr/ADR-010-commands-as-skill-directories.md, docs/adr/ADR-011-drop-v1-frontmatter.md, docs/adr/ADR-012-manifest-engine-derived.md, docs/adr/ADR-013-subagent-return-json-schema.md</files>
  <read_first>
    - SOVEREIGN.md (§9 ADRs — existing ADR-001..008 numbering and one-line style)
    - .planning/phases/01-engine-foundation/01-CONTEXT.md (the "ADRs to write BEFORE code" list, verbatim)
    - .planning/ROADMAP.md (Phase 1 "ADRs locked in this phase" line)
  </read_first>
  <action>
    Create `docs/adr/` and write six ADR markdown files. Each file uses this exact section structure (header line, then `**Status:** Accepted`, `**Date:** 2026-06-08`, `## Context`, `## Decision`, `## Consequences`):

    - `docs/adr/ADR-002-zero-dep-cjs-engine.md` — Decision: engine is zero-dependency Node `.cjs`, mirroring GSD's `gsd-tools.cjs`; the source IS the artifact (no compiled TS, no `tsx`, no `bun`, no `commander`/`yargs`); native `process.argv` parsing. Rationale: instant `npx`, offline determinism, no source/artifact gap (cite SOVEREIGN ADR-002, R-001).
    - `docs/adr/ADR-009-cjs-packaging.md` — Decision: `package.json` WITHOUT `"type":"module"`; `engines.node >= 20`; bin entries carry shebang `#!/usr/bin/env node`. Rationale: resolves the ESM-vs-CJS packaging pitfall.
    - `docs/adr/ADR-010-commands-as-skill-directories.md` — Decision: every SOVEREIGN command is authored as a skill DIRECTORY (`skills/<name>/SKILL.md`), never a bare `.claude/commands/*.md`; on name clash the skill wins. Rationale: supporting-files dir + auto-load. (Relevant later, recorded now.)
    - `docs/adr/ADR-011-drop-v1-frontmatter.md` — Decision: drop v1's non-standard SKILL.md frontmatter (`triggers`, `works-best-with`, `min-model`, `tokens`, bare `phase`) in favour of the real Agent Skills spec; `validate skills` enforces it. Rationale: portability, spec-clean surface.
    - `docs/adr/ADR-012-manifest-engine-derived.md` — Decision: MANIFEST.md is an engine-derived view, regenerated on every `state save`, never hand-edited; token budget enforced in code (~500 tokens). Rationale: hand-maintained MANIFEST goes stale (Pitfall 2).
    - `docs/adr/ADR-013-subagent-return-json-schema.md` — Decision: reasoning subagents return validated JSON, never prose; pins the return-shape Phase 2/3 consume. Include a minimal schema sketch: `{ "agent": string, "verdict"?: string, "findings": [...], "confidence"?: string }`. Rationale: avoids heuristic prose-parsing (Pitfall 8).
  </action>
  <acceptance_criteria>
    - `ls docs/adr/` lists exactly these six files: ADR-002-zero-dep-cjs-engine.md, ADR-009-cjs-packaging.md, ADR-010-commands-as-skill-directories.md, ADR-011-drop-v1-frontmatter.md, ADR-012-manifest-engine-derived.md, ADR-013-subagent-return-json-schema.md
    - `grep -l "## Decision" docs/adr/ADR-0*.md | wc -l` returns 6
    - `grep -rl "Status:" docs/adr/` matches all six files
    - `grep -q "type.*module" docs/adr/ADR-009-cjs-packaging.md` (the ADR mentions the no-`type:module` decision)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -l '## Decision' docs/adr/ADR-0*.md | wc -l | tr -d ' ')" = "6"</automated>
  </verify>
  <done>Six ADR files exist under docs/adr/, each with Status/Context/Decision/Consequences sections capturing the locked decision verbatim from CONTEXT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold engine/ package (CJS package.json, bin stubs, VERSION, test harness)</name>
  <files>engine/package.json, engine/.gitignore, engine/VERSION, engine/bin/sovereign-tools.cjs, engine/bin/sovereign.cjs, engine/test/smoke.test.cjs</files>
  <read_first>
    - .planning/research/STACK.md (the `npm publish` / `npm pack` row: bin map, files allowlist, shebang; engines.node >=20)
    - ~/.claude/get-shit-done/bin/gsd-tools.cjs (lines 1-20 + main() at ~198 — shebang + entry shape to mirror, do not copy)
    - ~/.claude/get-shit-done/VERSION (single-line version convention)
  </read_first>
  <action>
    Create the `engine/` directory and these files:

    - `engine/package.json` — `name: "sovereign"`, `version: "2.0.0"`, NO `"type"` key (CommonJS), `"engines": { "node": ">=20" }`, `"bin": { "sovereign": "bin/sovereign.cjs", "sovereign-tools": "bin/sovereign-tools.cjs" }`, `"files": ["bin","templates","VERSION"]`, `"scripts": { "test": "node --test test/" }`, `"license": "MIT"`. No `dependencies`. `devDependencies` may be empty `{}`.
    - `engine/VERSION` — single line `2.0.0` (sovereign_version starts at 2.0.0 per CONTEXT.md specifics).
    - `engine/bin/sovereign-tools.cjs` — first line `#!/usr/bin/env node`. A minimal stub `main()`: parse `process.argv.slice(2)`, if first arg is `version` print the contents of `../VERSION`, otherwise write `{"error":"not implemented","command":<cmd>}` to stdout via `fs.writeSync(1, ...)` and exit 0. (Plans 02-04 replace the body with the real router.) Add a `module.exports = { main }` guard and an `if (require.main === module) main()` invocation.
    - `engine/bin/sovereign.cjs` — first line `#!/usr/bin/env node`. Stub user-facing launcher: print `sovereign v<VERSION> — installer is Phase 2` and exit 0. (Bin wiring only; the `init` SKILL is Phase 2.)
    - `engine/.gitignore` — ignore `node_modules/`.
    - `engine/test/smoke.test.cjs` — a `node:test` placeholder using `node:assert/strict`: one test that spawns `node bin/sovereign-tools.cjs version` via `child_process.execFileSync` and asserts stdout includes `2.0.0`. This proves the shebang/CJS/bin load before any real command exists.

    Make both bin files executable conceptually (shebang is sufficient for `node` invocation; do not rely on chmod in tests — invoke via `node <path>`).
  </action>
  <acceptance_criteria>
    - `node --version` is >= 20 AND `cd engine && node --test test/` exits 0
    - `head -1 engine/bin/sovereign-tools.cjs` equals `#!/usr/bin/env node`
    - `head -1 engine/bin/sovereign.cjs` equals `#!/usr/bin/env node`
    - `node engine/bin/sovereign-tools.cjs version` prints a line containing `2.0.0`
    - `grep -q '"type"' engine/package.json` returns NON-zero (no type:module)
    - `grep -q '"node": ">=20"' engine/package.json` returns zero
    - `node -e "const p=require('./engine/package.json'); process.exit(p.bin['sovereign-tools']==='bin/sovereign-tools.cjs'?0:1)"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd engine && node --test test/ && node bin/sovereign-tools.cjs version | grep -q 2.0.0</automated>
  </verify>
  <done>`engine/` is a zero-dependency CJS package: package.json with bin map + engines + files + test script, two shebang'd bin stubs, VERSION=2.0.0, and a passing node:test smoke test that loads the bin.</done>
</task>

<task type="auto">
  <name>Task 3: Seed templates/sovereign/ state-tree templates</name>
  <files>templates/sovereign/MANIFEST.md, templates/sovereign/SOVEREIGN.md, templates/sovereign/CONTEXT.md, templates/sovereign/STATE.md, templates/sovereign/config.json</files>
  <read_first>
    - archive/v1/templates/MANIFEST.md (field structure + <500-token mandate to mine)
    - archive/v1/templates/SOVEREIGN.md (constitution + append-only Phase Gate Log shape)
    - archive/v1/templates/CONTEXT.md (glossary format)
    - .planning/research/ARCHITECTURE.md (the `.sovereign/` layout block + "**Field:** value" patch convention)
  </read_first>
  <action>
    Create `templates/sovereign/` with the seed files the engine copies into a project's `.sovereign/` (Phase 2 wires the copy; this plan only authors the templates). Mine v1 content but modernize to v2:

    - `templates/sovereign/MANIFEST.md` — DERIVED-view header comment ("regenerated by the engine on every state save; <500 tokens; never hand-edit"), sections: `## Current Status` (with `**Phase:** [n]`, `**Gate Status:** [status]`), `## Next Recommended Action`, `## Current Blockers`, `## Key Decisions (Quick Reference)` table, `## Stack (Quick Reference)` table. Bump footer to `sovereign-base/sovereign v2.0.0`. Drop v1's Bridge/UNVERIFIED sections (M3/M4 scope).
    - `templates/sovereign/SOVEREIGN.md` — constitution with `**Sovereign Version:** 2.0.0`, an `## Active Tracks` section, and an `## Phase Gate Log` section marked `<!-- APPEND ONLY — gate open/pass writes here, never edit existing entries -->` with no pre-baked v1 phase entries (gates are appended by the engine).
    - `templates/sovereign/CONTEXT.md` — glossary template (term format block from v1), `**Term Count:** 0`, footer `v2.0.0`.
    - `templates/sovereign/STATE.md` — field-patchable using `**Field:** value` lines so `stateReplaceField` works: must include at minimum `**Phase:** 0`, `**Plan:** 0`, `**Status:** Not started`, `**Blockers:** None`, `**Next Action:** —`, `**Last Updated:** —`.
    - `templates/sovereign/config.json` — JSON with keys: `model_profile: "balanced"`, `commit_docs: true`, `parallelization: true`, `council_mode_default: "standard"`, `resolve_model_ids: false`, `context_window: 200000`. (These are the keys `loadConfig` in plan 02 will deep-merge.)
  </action>
  <acceptance_criteria>
    - `ls templates/sovereign/` lists exactly: CONTEXT.md, MANIFEST.md, SOVEREIGN.md, STATE.md, config.json
    - `grep -q '\*\*Phase:\*\*' templates/sovereign/STATE.md` returns zero
    - `grep -q '\*\*Blockers:\*\*' templates/sovereign/STATE.md` returns zero
    - `grep -qi 'APPEND ONLY' templates/sovereign/SOVEREIGN.md` returns zero
    - `node -e "JSON.parse(require('fs').readFileSync('templates/sovereign/config.json','utf8'))"` exits 0 (valid JSON)
    - `node -e "const c=JSON.parse(require('fs').readFileSync('templates/sovereign/config.json','utf8')); process.exit(c.model_profile&&c.commit_docs!==undefined?0:1)"` exits 0
    - `grep -q 'v2.0.0' templates/sovereign/MANIFEST.md` returns zero
  </acceptance_criteria>
  <verify>
    <automated>node -e "const c=JSON.parse(require('fs').readFileSync('templates/sovereign/config.json','utf8')); process.exit(c.model_profile==='balanced'&&c.commit_docs===true?0:1)" && grep -q '\*\*Phase:\*\*' templates/sovereign/STATE.md && grep -qi 'APPEND ONLY' templates/sovereign/SOVEREIGN.md</automated>
  </verify>
  <done>templates/sovereign/ contains the five seed files; STATE.md uses **Field:** value lines, SOVEREIGN.md gate log is marked append-only, config.json is valid JSON with the loadConfig keys, all footers say v2.0.0.</done>
</task>

</tasks>

<verification>
- `ls docs/adr/ADR-0*.md | wc -l` == 6
- `cd engine && node --test test/` exits 0
- `node engine/bin/sovereign-tools.cjs version` includes 2.0.0
- `ls templates/sovereign/` has 5 files; config.json parses; STATE.md has **Field:** lines
</verification>

<success_criteria>
The six locked ADRs are durable on disk, the zero-dep CJS `engine/` package shell loads and passes its smoke test, and the `.sovereign/` seed templates exist with patch-friendly field lines. Plans 02-05 can now fill in the router, commands, init contract, and full test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-foundation/01-01-SUMMARY.md`
</output>
