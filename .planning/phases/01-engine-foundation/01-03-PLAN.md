---
phase: 01-engine-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - engine/bin/lib/state.cjs
  - engine/bin/lib/manifest.cjs
  - engine/bin/lib/gate.cjs
  - engine/bin/sovereign-tools.cjs
  - engine/test/state.test.cjs
  - engine/test/gate.test.cjs
autonomous: true
requirements: [ENG-02, ENG-03, STATE-02]

must_haves:
  truths:
    - "An agent can `state load` and receive {config, state_raw, manifest_raw, *_exists} in one JSON blob."
    - "An agent can `state save`/`state patch` STATE.md by field-level regex against **Field:** lines (no whole-file rewrite)."
    - "Every `state save` re-derives and rewrites MANIFEST.md from authoritative state, kept within a ~500-token budget."
    - "An agent can `gate open <n>` and `gate pass <n>` and the records are appended (never overwritten) to .sovereign/SOVEREIGN.md."
  artifacts:
    - path: "engine/bin/lib/state.cjs"
      provides: "cmdStateLoad, cmdStatePatch/cmdStateSave, stateReplaceField"
      contains: "stateReplaceField"
    - path: "engine/bin/lib/manifest.cjs"
      provides: "regenerateManifest(cwd) — derives + writes MANIFEST.md under token budget"
      contains: "regenerateManifest"
    - path: "engine/bin/lib/gate.cjs"
      provides: "cmdGateOpen, cmdGatePass — append-only to SOVEREIGN.md"
      contains: "appendFileSync"
  key_links:
    - from: "state save"
      to: "regenerateManifest"
      via: "state.cjs requires manifest.cjs and calls it after patching STATE.md"
      pattern: "regenerateManifest"
    - from: "stateReplaceField"
      to: ".sovereign/STATE.md"
      via: "regex against **Field:** lines"
      pattern: "\\\\\\*\\\\\\*.*:\\\\\\*\\\\\\*"
---

<objective>
Build the state + gate core of engine layer B: field-level STATE.md patching, the derived-MANIFEST regeneration on every save (SOVEREIGN's one extension over GSD), and append-only phase gates in SOVEREIGN.md. These are the context-reset survival primitives — a fresh agent re-orients from disk because the engine keeps STATE/MANIFEST current and gates durable.

Purpose: Deliver `state load|save|patch` and `gate open|pass` so STATE-02 (derived MANIFEST under budget) and ENG-02/ENG-03 are satisfied. The `init` contract (plan 05) reads `state_raw`/`manifest_raw`; this plan produces them.
Output: `engine/bin/lib/{state,manifest,gate}.cjs`, wired switch cases in `sovereign-tools.cjs`, and `engine/test/{state,gate}.test.cjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-engine-foundation/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md

<!-- Reference shapes (read, lift, retarget .planning -> .sovereign): -->
<!-- ~/.claude/get-shit-done/bin/lib/state.cjs : cmdStateLoad(27), cmdStatePatch(132), stateReplaceField(212), stateReplaceFieldWithFallback(232) -->
<!-- templates/sovereign/STATE.md (from plan 01) : the **Field:** lines to patch -->
<!-- templates/sovereign/MANIFEST.md (from plan 01) : the sections to regenerate -->
<!-- templates/sovereign/SOVEREIGN.md (from plan 01) : the append-only Phase Gate Log -->

<interfaces>
<!-- core.cjs already exports (plan 02): output, error, loadConfig, findProjectRoot, safeReadFile -->
<!-- This plan adds: -->

// state.cjs
cmdStateLoad(cwd, raw)              // -> output({ config, state_raw, manifest_raw, state_exists, manifest_exists, sovereign_exists })
cmdStatePatch(cwd, patches, raw)    // patches: [{field,value}]; regex-replace each **Field:** line; then regenerateManifest(cwd)
stateReplaceField(content, field, value)  // returns patched content or null if field absent

// manifest.cjs
regenerateManifest(cwd)            // derive Phase/Blockers/Next-Action from STATE.md + Key Decisions from docs/adr/ ; write MANIFEST.md; enforce ~500-token budget (chars/4 heuristic; truncate quick-ref tables to top-N or warn)

// gate.cjs
cmdGateOpen(cwd, phase, raw)       // appendFileSync a "### Gate: Phase <n> — OPENED <ts>" block to SOVEREIGN.md
cmdGatePass(cwd, phase, raw)       // appendFileSync a "### Gate: Phase <n> — PASSED <ts>" block to SOVEREIGN.md
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: state.cjs — load + field-level patch (stateReplaceField)</name>
  <files>engine/bin/lib/state.cjs, engine/test/state.test.cjs</files>
  <read_first>
    - ~/.claude/get-shit-done/bin/lib/state.cjs (cmdStateLoad 27, cmdStatePatch 132, stateReplaceField 212)
    - engine/bin/lib/core.cjs (output/error/loadConfig/safeReadFile to require)
    - templates/sovereign/STATE.md (the **Field:** value lines this patches)
  </read_first>
  <behavior>
    - stateReplaceField('**Phase:** 0', 'Phase', '1') returns content with '**Phase:** 1'.
    - stateReplaceField for an absent field returns null (caller leaves file unchanged).
    - cmdStateLoad returns an object with state_raw, manifest_raw, config, and *_exists booleans.
    - cmdStatePatch applied to [{field:'Phase',value:'2'},{field:'Status',value:'In progress'}] writes both fields and leaves other lines untouched (byte-for-byte except the two lines).
  </behavior>
  <action>
    Create `engine/bin/lib/state.cjs` (require `./core.cjs`). Implement and export:
    - `stateReplaceField(content, fieldName, newValue)` — copy GSD's verbatim: try `**Field:**` bold pattern first (`(\\*\\*${escaped}:\\*\\*\\s*)(.*)`), then plain `Field:` pattern; return patched content or null. Include `escapeRegex`.
    - `cmdStateLoad(cwd, raw)` — read `.sovereign/STATE.md` (state_raw) and `.sovereign/MANIFEST.md` (manifest_raw) via safeReadFile; load config via loadConfig; output `{ config, state_raw, manifest_raw, state_exists:<bool>, manifest_exists:<bool>, sovereign_exists:<bool> }`.
    - `cmdStatePatch(cwd, patches, raw)` — patches is an array of `{field,value}`; read STATE.md, apply stateReplaceField for each (skip + collect a `warnings` entry for any field that returns null), write the file back ONLY if changed; then call `require('./manifest.cjs').regenerateManifest(cwd)`; output `{ patched:<count>, warnings, manifest_regenerated:true }`. (Treat `state save` as an alias of patch for now.)
    Add `// @ts-check`.
  </action>
  <acceptance_criteria>
    - `node -e "const s=require('./engine/bin/lib/state.cjs'); process.exit(s.stateReplaceField('**Phase:** 0','Phase','1').includes('**Phase:** 1')?0:1)"` exits 0
    - `node -e "const s=require('./engine/bin/lib/state.cjs'); process.exit(s.stateReplaceField('x','Nope','1')===null?0:1)"` exits 0
    - `cd engine && node --test test/state.test.cjs` exits 0
    - `grep -q 'stateReplaceField' engine/bin/lib/state.cjs` returns zero
    - `grep -q "require('./manifest.cjs')" engine/bin/lib/state.cjs` returns zero
  </acceptance_criteria>
  <verify>
    <automated>cd engine && node --test test/state.test.cjs</automated>
  </verify>
  <done>state.cjs loads STATE+MANIFEST+config as one blob and patches STATE.md field-by-field via stateReplaceField, triggering MANIFEST regeneration after a patch; state.test.cjs proves field patch + absent-field null + multi-field patch.</done>
</task>

<task type="auto">
  <name>Task 2: manifest.cjs — derived MANIFEST regeneration under ~500-token budget</name>
  <files>engine/bin/lib/manifest.cjs</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md ("Keeping MANIFEST <500 tokens and always-current" — derived view, token budget in code, chars/4 heuristic)
    - templates/sovereign/MANIFEST.md (the sections to fill: Current Status, Next Recommended Action, Current Blockers, Key Decisions, Stack)
    - templates/sovereign/STATE.md + templates/sovereign/SOVEREIGN.md (authoritative sources to derive from)
    - engine/bin/lib/state.cjs (stateReplaceField is reusable for reading **Field:** values; or write a small readField helper)
  </read_first>
  <action>
    Create `engine/bin/lib/manifest.cjs` (require `./core.cjs`). Implement and export `regenerateManifest(cwd)`:
    - Read `.sovereign/STATE.md`; extract `**Phase:**`, `**Status:**` (rename to Gate Status), `**Blockers:**`, `**Next Action:**` values via a `readField(content, name)` regex helper (mirror stateReplaceField's pattern, capture group 2).
    - Read `docs/adr/` (project-level, via cwd) OR `.sovereign/docs/adr/` if present; build a Key Decisions quick-ref of up to top-N (N=8) ADR titles (filename → title line).
    - Compose a MANIFEST.md string from the template sections, filling Current Status / Next Recommended Action / Current Blockers / Key Decisions table. Header note: "DERIVED — regenerated on every state save, do not hand-edit". Footer `sovereign-base/sovereign v2.0.0`.
    - Enforce token budget: `estTokens = Math.ceil(manifestString.length / 4)`. If `estTokens > 500`, truncate the Key Decisions table to fewer rows (drop oldest) and re-measure; if still over after truncation to top-3, append an HTML comment `<!-- WARN: MANIFEST over 500-token budget (est <n>) -->`. Return `{ written:true, est_tokens:<n>, truncated:<bool>, over_budget:<bool> }`.
    - Write the result to `.sovereign/MANIFEST.md` via fs.writeFileSync (the engine is the ONLY writer of MANIFEST — ADR-012).
    Add `// @ts-check`.
  </action>
  <acceptance_criteria>
    - `node -e "const m=require('./engine/bin/lib/manifest.cjs'); process.exit(typeof m.regenerateManifest==='function'?0:1)"` exits 0
    - `grep -q 'regenerateManifest' engine/bin/lib/manifest.cjs` returns zero
    - `grep -q '/ 4' engine/bin/lib/manifest.cjs || grep -q 'length / 4' engine/bin/lib/manifest.cjs` returns zero (chars/4 token heuristic present)
    - `grep -qi '500' engine/bin/lib/manifest.cjs` returns zero (budget enforced in code)
    - Covered by state.test.cjs Task-1 patch flow: after a patch, `.sovereign/MANIFEST.md` exists in the tmp fixture and contains the patched Phase value (asserted in test/state.test.cjs)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'regenerateManifest' engine/bin/lib/manifest.cjs && grep -Eq 'length / 4|/ 4' engine/bin/lib/manifest.cjs && grep -qi '500' engine/bin/lib/manifest.cjs</automated>
  </verify>
  <done>manifest.cjs derives MANIFEST.md fields from STATE.md + docs/adr/, writes it as the sole MANIFEST writer, and enforces a ~500-token budget via a chars/4 heuristic (truncate-then-warn), returning a status object.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: gate.cjs — append-only gate open/pass + wire all switch cases</name>
  <files>engine/bin/lib/gate.cjs, engine/bin/sovereign-tools.cjs, engine/test/gate.test.cjs</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (gate open/pass row: "append-only writes to SOVEREIGN.md ... audit trail")
    - templates/sovereign/SOVEREIGN.md (the append-only Phase Gate Log section to append under)
    - engine/bin/sovereign-tools.cjs (the // TODO(plan 03) switch insertion points from plan 02)
    - engine/bin/lib/core.cjs (output/error)
  </read_first>
  <behavior>
    - cmdGateOpen(cwd,'1') appends a block containing 'Phase 1' and 'OPENED' to .sovereign/SOVEREIGN.md and does NOT remove prior content.
    - cmdGatePass(cwd,'1') appends a block containing 'Phase 1' and 'PASSED'; calling open then pass yields a file containing BOTH blocks in order.
    - gate commands error (exit 1) when no phase argument is given.
  </behavior>
  <action>
    Create `engine/bin/lib/gate.cjs` (require `./core.cjs`). Implement and export:
    - `cmdGateOpen(cwd, phase, raw)` — if `!phase` call error('gate phase required'); build a block `\n### Gate: Phase ${phase} — OPENED ${new Date().toISOString()}\n` and `fs.appendFileSync('.sovereign/SOVEREIGN.md', block)`; output `{ gate:'open', phase, appended:true }`.
    - `cmdGatePass(cwd, phase, raw)` — same with `PASSED`; output `{ gate:'pass', phase, appended:true }`.
    Then in `engine/bin/sovereign-tools.cjs`, replace the `// TODO(plan 03)` placeholders by wiring real switch cases:
    - `case 'state':` — sub-dispatch on `args[0]`: `load` -> `cmdStateLoad`; `save`/`patch` -> parse `--field`/`--value` (or repeated `--field name --value v`) into a patches array via parseNamedArgs and call `cmdStatePatch`.
    - `case 'gate':` — sub-dispatch on `args[0]`: `open <n>` -> `cmdGateOpen`; `pass <n>` -> `cmdGatePass`.
    Require state.cjs and gate.cjs at top of sovereign-tools.cjs. Leave the `// TODO(plan 04): init` and the commit/model/validate placeholders (plan 04) untouched.
  </action>
  <acceptance_criteria>
    - `cd engine && node --test test/gate.test.cjs` exits 0
    - `grep -q 'appendFileSync' engine/bin/lib/gate.cjs` returns zero
    - `grep -q "case 'gate'" engine/bin/sovereign-tools.cjs` returns zero
    - `grep -q "case 'state'" engine/bin/sovereign-tools.cjs` returns zero
    - In a tmp fixture with a seeded .sovereign/SOVEREIGN.md: `node bin/sovereign-tools.cjs gate open 1 --cwd <tmp>` then `gate pass 1 --cwd <tmp>` leaves a SOVEREIGN.md containing both 'OPENED' and 'PASSED' (asserted in gate.test.cjs)
    - `node engine/bin/sovereign-tools.cjs gate open` (no phase) exits non-zero
  </acceptance_criteria>
  <verify>
    <automated>cd engine && node --test test/gate.test.cjs && grep -q "case 'gate'" bin/sovereign-tools.cjs && grep -q "case 'state'" bin/sovereign-tools.cjs</automated>
  </verify>
  <done>gate.cjs appends OPENED/PASSED records to SOVEREIGN.md without overwriting; sovereign-tools.cjs routes `state load|save|patch` and `gate open|pass <n>`; gate.test.cjs proves append-only ordering and the no-phase error.</done>
</task>

</tasks>

<verification>
- `cd engine && node --test test/` exits 0 (smoke + core + state + gate)
- `node bin/sovereign-tools.cjs state load --cwd <project-with-.sovereign>` returns JSON with state_raw + manifest_raw
- gate open then pass appends two ordered blocks to SOVEREIGN.md
</verification>

<success_criteria>
state load/save/patch, derived-MANIFEST regeneration under a code-enforced ~500-token budget, and append-only gate open/pass all work and are tested. The init contract (plan 05) can now surface state_raw/manifest_raw; plan 04 adds commit/model/validate at the remaining switch insertion points.
</success_criteria>

<output>
After completion, create `.planning/phases/01-engine-foundation/01-03-SUMMARY.md`
</output>
