---
phase: 10-engine-additions
plan: 05
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03", "10-04"]
files_modified:
  - engine/bin/sovereign-tools.cjs
  - engine/bin/lib/init.cjs
  - engine/test/router-m3.test.cjs
  - engine/test/init.test.cjs
autonomous: true
requirements: [ENG-08]

must_haves:
  truths:
    - "sovereign-tools bridge hash|check, extension preview|install|list|audit, adopt scan are routed to the new modules"
    - "init bridge, init adopt, init extension each return a greenfield-safe orientation blob (paths + config + relevant artifacts)"
    - "the router dispatches via array-arg parsing already in the file (parseListArg/positional); no shell strings"
    - "node --test passes for the whole engine suite; engine/package.json dependencies stays {}"
  artifacts:
    - path: "engine/bin/sovereign-tools.cjs"
      provides: "case 'bridge' | 'extension' | 'adopt' switch arms wired to the new cmd* exports"
      contains: "case 'bridge'"
      min_lines: 360
    - path: "engine/bin/lib/init.cjs"
      provides: "bridge/adopt/extension workflow blobs added to buildInit"
      contains: "case 'bridge'"
      min_lines: 360
    - path: "engine/test/router-m3.test.cjs"
      provides: "node:test spawning the real bin for bridge/adopt/extension end-to-end"
      contains: "node:test"
      min_lines: 70
  key_links:
    - from: "engine/bin/sovereign-tools.cjs"
      to: "engine/bin/lib/bridge.cjs"
      via: "require + case 'bridge' dispatch"
      pattern: "require\\(['\"]\\./lib/bridge\\.cjs['\"]\\)"
    - from: "engine/bin/sovereign-tools.cjs"
      to: "engine/bin/lib/extension.cjs"
      via: "require + case 'extension' dispatch"
      pattern: "require\\(['\"]\\./lib/extension\\.cjs['\"]\\)"
    - from: "engine/bin/sovereign-tools.cjs"
      to: "engine/bin/lib/adopt.cjs"
      via: "require + case 'adopt' dispatch"
      pattern: "require\\(['\"]\\./lib/adopt\\.cjs['\"]\\)"
    - from: "engine/bin/lib/init.cjs"
      to: "buildInit switch"
      via: "bridge/adopt/extension orientation blobs"
      pattern: "case 'adopt'"
---

<objective>
Wire the three new modules into the router (`engine/bin/sovereign-tools.cjs`) and add `init bridge|adopt|extension` orientation workflows to `engine/bin/lib/init.cjs`, then prove the surface end-to-end with an integration `node --test`. This is the integration plan — it depends on all four module plans (01-04) existing, so it runs LAST (Wave 3).

Purpose: Exposes the engine substrate as the public `sovereign-tools` commands the M3 skills call, and gives each skill its one-call orientation blob (Core Value).
Output: router `case 'bridge'|'extension'|'adopt'`; `buildInit` cases for bridge/adopt/extension; `engine/test/router-m3.test.cjs`; extended `engine/test/init.test.cjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-engine-additions/10-CONTEXT.md
@.planning/research/M3-NOTES.md

<read_first>
- `engine/bin/sovereign-tools.cjs` — the router: the top require block, `runCommand`'s `switch (command)`, the existing `parseListArg(args, 'files')` (already used by `commit --files`), `parseMultiwordArg`, the `case 'gate'`/`case 'validate'` subcommand-dispatch shape, and the `default` not_implemented arm. ADD `require('./lib/bridge.cjs')`, `require('./lib/extension.cjs')`, `require('./lib/adopt.cjs')` and three new cases. Update the usage string + the Commands list comment.
- `engine/bin/lib/init.cjs` — `buildInit(cwd, workflow)`'s `switch (workflow)`, `orientationConfig`, `contextInjection(cwd)`, `relevantAdrs(cwd)`, `existsBlock(cwd)`, `withProjectContext`. ADD `case 'bridge' | 'adopt' | 'extension'` BEFORE `default`. The `default` arm currently produces a generic fast-lane stub — the new cases give bridge/adopt/extension PROPER paths instead.
- `engine/test/init.test.cjs` — the init-blob assertions to extend (mirror an existing case's assertions for the three new workflows).
- `engine/test/pack-smoke.test.cjs` — the spawn-the-real-installed-bin + JSON-parse-with-@file:-fallback pattern to mirror for router-m3.test.cjs (here spawn the SOURCE bin, not the packed one).
- `.planning/research/M3-NOTES.md` §2/§3/§1 + CONTEXT `init bridge|adopt|extension workflows` decision — what each orientation blob lists (bridge: bridgeable source docs API_SPEC/SECURITY_MODEL/CONTEXT/ADRs + the registry path; adopt: project_root + git presence + scan paths; extension: the `.sovereign/extensions/` dir + config).
</read_first>

<interfaces>
New module exports to require + dispatch:
```js
// bridge.cjs (Plan 01)
cmdBridgeHash(cwd, files, raw); cmdBridgeCheck(cwd, bridgeId, raw)
// extension.cjs (Plan 04)
cmdExtensionPreview(cwd, source, raw); cmdExtensionInstall(cwd, source, raw);
cmdExtensionList(cwd, raw); cmdExtensionAudit(cwd, contentPath, raw)
// adopt.cjs (Plan 03)
cmdAdoptScan(cwd, raw)
```
Existing router arg helpers (reuse — do not add new ones): `parseListArg(args, 'files')`, `parseMultiwordArg`, positional `args[2]`.
buildInit blob shape (mirror existing cases): `{ models:{}, config: orientationConfig, phase, context_injection, paths, exists }` wrapped by `withProjectContext`. Import `execGit` from `./core.cjs` for the adopt `in_git` probe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire bridge/extension/adopt router cases + init workflows</name>
  <files>engine/bin/sovereign-tools.cjs, engine/bin/lib/init.cjs</files>
  <action>
    ROUTER (`engine/bin/sovereign-tools.cjs`): add the three requires next to the existing lib requires (`require('./lib/bridge.cjs')` → cmdBridgeHash/cmdBridgeCheck; `require('./lib/extension.cjs')` → the four cmdExtension*; `require('./lib/adopt.cjs')` → cmdAdoptScan). In `runCommand`'s switch, add three cases mirroring the existing `case 'gate'`/`case 'validate'` subcommand shape:
    - `case 'bridge'`: `sub = args[1]`. `hash` → `files = parseListArg(args, 'files')`; `cmdBridgeHash(cwd, files, raw)`. `check` → `bridgeId = args[2] && !args[2].startsWith('--') ? args[2] : null`; `cmdBridgeCheck(cwd, bridgeId, raw)`. else `error('Unknown bridge subcommand: ' + (sub||'(none)') + ' (expected hash|check)')`.
    - `case 'extension'`: `sub = args[1]`; `arg = args[2] && !args[2].startsWith('--') ? args[2] : null`. `preview`→`cmdExtensionPreview(cwd, arg, raw)`; `install`→`cmdExtensionInstall(cwd, arg, raw)`; `list`→`cmdExtensionList(cwd, raw)`; `audit`→`cmdExtensionAudit(cwd, arg, raw)`; else error expected `preview|install|list|audit`.
    - `case 'adopt'`: `sub = args[1]`; `scan`→`cmdAdoptScan(cwd, raw)`; else error expected `scan`.
    Update the `error(...)` usage Commands string to include `bridge (hash|check)`, `extension (preview|install|list|audit)`, `adopt (scan)`. Keep the EXISTING arg helpers (array-arg; never a shell string).

    INIT (`engine/bin/lib/init.cjs`): import `execGit` from `./core.cjs`. In `buildInit`'s switch, add three cases BEFORE `default`, each `{ models: {}, config: orientationConfig, phase, context_injection: contextInjection(cwd), paths: {...}, exists }`:
    - `case 'bridge'`: paths include `bridge_dir: '.sovereign/bridges'`, `registry: '.sovereign/bridges/registry.json'`, `bridge_doc: '.sovereign/BRIDGE.md'`, `api_spec: '.sovereign/API_SPEC.md'`, `security_model: '.sovereign/SECURITY_MODEL.md'`, `glossary: '.sovereign/CONTEXT.md'`, `state: '.sovereign/STATE.md'`. (`context_injection.relevant_adrs` already lists ADRs the bridge summarizes.)
    - `case 'adopt'`: paths include `state`, `manifest`, `sovereign_dir`; compute `const inGit = execGit(cwd, ['rev-parse', '--is-inside-work-tree']).exitCode === 0;` and add `detected: { in_git: inGit }` to the blob. (project_root is supplied by withProjectContext.)
    - `case 'extension'`: paths include `extensions_dir: '.sovereign/extensions'`, `state: '.sovereign/STATE.md'`, `manifest: '.sovereign/MANIFEST.md'`. (Per CONTEXT: per-decision logs live at `.sovereign/extensions/<date>-<skill>.md`.)
    Each stays greenfield-safe (no throw on missing `.sovereign/` / non-git dir — execGit never throws).
  </action>
  <verify>
    <automated>cd engine && node --check bin/sovereign-tools.cjs && node --check bin/lib/init.cjs && node bin/sovereign-tools.cjs init bridge --cwd . --pick paths.registry && node bin/sovereign-tools.cjs init extension --cwd . --pick paths.extensions_dir && node bin/sovereign-tools.cjs adopt scan --cwd . --pick project_root</automated>
  </verify>
  <done>Router has require + case for bridge/extension/adopt dispatching to the cmd* exports via existing array-arg helpers; buildInit returns proper bridge/adopt/extension blobs (registry + extensions_dir + in_git); the --pick smoke commands above print non-empty values; both files pass node --check.</done>
</task>

<task type="auto">
  <name>Task 2: Write router-m3.test.cjs integration suite + extend init.test.cjs</name>
  <files>engine/test/router-m3.test.cjs, engine/test/init.test.cjs</files>
  <action>
    Create `engine/test/router-m3.test.cjs` (node:test + node:assert/strict). Mirror pack-smoke's spawn-the-bin + `@file:` JSON-parse helper, but spawn the SOURCE bin `path.join(ENGINE_ROOT, 'bin', 'sovereign-tools.cjs')` via `execFileSync('node', [bin, ...args, '--cwd', tmpProject])`. Build a tmp project (some with `.sovereign/`, some bare/non-git) per case. Cover:
    1. `bridge hash --files a.md b.md` (against tmp files) → parsed JSON has `files: [{path, sha256}]` (2 entries) + a `combined` 64-hex.
    2. `bridge check` in a tmp project with NO registry → `{ fresh: true, ... }` (greenfield).
    3. `adopt scan` in a tmp project with a package.json + src/ → JSON has `manifests`, `detected`, `structure.truncated` (boolean), `deep_read_candidates` (array).
    4. `extension list` with an INJECTED-failure not possible via the bin, so assert the command RUNS and returns the `{ ok, exitCode, stdout, stderr, source }` shape WITHOUT throwing; gate with a `hasNpm()` skip (mirror pack-smoke) since it shells to npx — `t.skip` offline. (Unit-level exit-code branching already covered in extension.test.cjs Plan 04.)
    5. Unknown subcommand (`bridge bogus`) → the bin exits non-zero (assert `execFileSync` throws / status !== 0).

    Extend `engine/test/init.test.cjs` with three cases: `buildInit(cwd,'bridge')`/`'adopt'`/`'extension'` (require buildInit directly, no spawn) →
    - bridge blob: `paths.registry === '.sovereign/bridges/registry.json'`, `paths.api_spec` present, `project_root` + `sovereign_version` present (withProjectContext applied).
    - adopt blob: `detected.in_git` is a boolean; `paths.sovereign_dir` present.
    - extension blob: `paths.extensions_dir === '.sovereign/extensions'`.
    Assert all three are greenfield-safe on a bare tmp dir (no throw, models === {}).
  </action>
  <verify>
    <automated>cd engine && node --test test/router-m3.test.cjs test/init.test.cjs</automated>
  </verify>
  <done>node --test passes for router-m3.test.cjs + init.test.cjs; bridge/adopt/extension are exercised through the real bin and through buildInit; the npx-dependent extension case skips offline; unknown subcommand exits non-zero.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + zero-dep regression gate</name>
  <files>engine/test/router-m3.test.cjs</files>
  <action>
    Run the ENTIRE engine test suite to confirm the wiring did not regress any existing module, and assert the zero-dependency invariant explicitly. No new production code in this task — it is the phase-level green gate. If any pre-existing suite (init/doctor/validate/pack-smoke) broke due to the new init cases or router requires, fix the wiring in Plan-05 Task 1's files (not by weakening tests). Confirm `engine/package.json` `dependencies` is still `{}` and that no new module added a third-party `require` (only `node:*` + `./...` local libs).
  </action>
  <verify>
    <automated>cd engine && node --test && node -e "const d=require('./package.json').dependencies||{}; if(Object.keys(d).length!==0) throw new Error('non-zero deps: '+JSON.stringify(d)); console.log('zero-dep ok')"</automated>
  </verify>
  <done>`node --test` (whole suite) is green; `engine/package.json` dependencies is exactly `{}`; no third-party requires were introduced by any Phase-10 module.</done>
</task>

</tasks>

<verification>
- `cd engine && node --test` → entire suite green (live npx/extension cases skip offline).
- `grep -n "require('./lib/bridge.cjs')\|require('./lib/extension.cjs')\|require('./lib/adopt.cjs')" engine/bin/sovereign-tools.cjs` → all three required.
- `grep -n "case 'bridge'\|case 'extension'\|case 'adopt'" engine/bin/sovereign-tools.cjs` → all three routed.
- `grep -n "case 'bridge'\|case 'adopt'\|case 'extension'" engine/bin/lib/init.cjs` → all three init workflows present.
- `node engine/bin/sovereign-tools.cjs init bridge --cwd . --pick paths.registry` → prints `.sovereign/bridges/registry.json`.
- `engine/package.json` `dependencies` still `{}`.
</verification>

<success_criteria>
- ROADMAP Phase 10 success criteria #1-#4 are reachable through the public CLI: `bridge hash|check`, `extension preview|install|list|audit`, `adopt scan`, and `init bridge|adopt|extension` all dispatch correctly and return greenfield-safe blobs.
- Criterion #5 holds: full `node --test` green; engine dependencies still `{}`; all output via existing `output()`/`@file:` spill.
- Integration proven end-to-end through the real bin, not just unit-level.
</success_criteria>

<output>
After completion, create `.planning/phases/10-engine-additions/10-05-SUMMARY.md`.
</output>
