---
phase: 04-fast-lane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/references/unverified-marker.md
autonomous: true
requirements: [CONV-03]
must_haves:
  truths:
    - "The SOVEREIGN:UNVERIFIED marker spec exists as a single reference doc that sentinel and the future anchor-docs/verify-self skills can both cite"
    - "The doc fixes the literal token, the comment form, the valid contexts, the scan rule, and the gate threshold (surface now, block later)"
  artifacts:
    - path: "engine/references/unverified-marker.md"
      provides: "The SOVEREIGN:UNVERIFIED marker specification (CONV-03), authored as a mini-ADR-style reference"
      contains: "SOVEREIGN:UNVERIFIED"
      min_lines: 40
  key_links:
    - from: "engine/agents/sovereign-sentinel.md"
      to: "engine/references/unverified-marker.md"
      via: "the literal token both define — sentinel scans for what this doc specifies"
      pattern: "SOVEREIGN:UNVERIFIED"
---

<objective>
Author the `SOVEREIGN:UNVERIFIED` marker specification (CONV-03) as a single
reference doc at `engine/references/unverified-marker.md`. This is the contract
the `sentinel` skill (plan 04-06) scans against, and the contract the future
`anchor-docs`/`verify-self` skills (M2) will *write* against. It is authored
FIRST in this wave so sentinel can cite it by path.

Purpose: SOVEREIGN's anti-hallucination beachhead. The marker makes "code written
under genuine uncertainty" a first-class, greppable, persisted concern instead of
a forgotten thought. Define + scan now; block-at-gate is deferred to M2.

Output: `engine/references/unverified-marker.md` — a mini-ADR-style spec covering
the token, the comment form, the meaning/valid contexts, the scan rule, and the
gate threshold.
</objective>

<execution_context>
This is HAND-AUTHORED reference content (R-004). The orchestrator authors the file
directly with the Write tool — no executor, no test scaffold. Acceptance is
grep-verifiable against the authored file plus a doctor check.
</execution_context>

<context>
@.planning/phases/04-fast-lane/04-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# The CONSUMER shape already declared in Phase 2 — this spec must stay compatible with it.
@engine/agents/sovereign-sentinel.md

# v1's commenting standard already shows a SOVEREIGN:UNVERIFIED block — mine its form, drop the
# v1-specific "Confidence:" line in favor of the locked CONTEXT.md form.
@archive/v1/COMMENTING.md

# Pattern reference: how existing engine references are written (tone, mini-ADR shape, the
# "> Feeds CONV-XX" forward-pointer footer).
@engine/references/listing-budget.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author engine/references/unverified-marker.md (the CONV-03 spec)</name>
  <read_first>
    - `.planning/phases/04-fast-lane/04-CONTEXT.md` lines 36-41 — the LOCKED spec: token, form, meaning, scan rule, gate threshold. This is the source of truth; do not invent fields.
    - `archive/v1/COMMENTING.md` lines 176-198 — v1's SOVEREIGN:UNVERIFIED block (form to mine; note v1 used a "Confidence:" line + "Review before:" — the locked v2 form is `<comment> SOVEREIGN:UNVERIFIED — <reason> | ref: <url-or-ADR> | <YYYY-MM-DD>`, reason required, ref/date optional).
    - `engine/agents/sovereign-sentinel.md` lines 19-31, 50-57 — the consumer already greps the literal token and emits `unverified_markers: [{file,line,text}]`. The spec must make that grep well-defined.
    - `engine/references/listing-budget.md` — copy the reference tone + the closing `> Feeds CONV-XX` forward-pointer convention.
  </read_first>
  <action>
    Write `engine/references/unverified-marker.md` as a mini-ADR-style reference (CONV-03).
    Required sections, in this order:

    1. **Title + one-paragraph intent.** "The `SOVEREIGN:UNVERIFIED` marker — SOVEREIGN's
       persisted record of code written under genuine uncertainty." State it is the contract
       `sentinel` scans and the future `anchor-docs`/`verify-self` skills write.

    2. **## The token.** The literal string `SOVEREIGN:UNVERIFIED` placed inside a code comment.
       Language-agnostic: any comment syntax in any language; the token is the anchor (so a single
       grep finds it across the whole tree). State exactly this so the scan is deterministic.

    3. **## The form.** The locked one-line form, verbatim:
       `<comment> SOVEREIGN:UNVERIFIED — <reason> | ref: <url-or-ADR> | <YYYY-MM-DD>`
       Rules: `reason` is REQUIRED; `ref` (a URL or an ADR id like `ADR-011`) and the `<YYYY-MM-DD>`
       date are OPTIONAL. Show 2-3 worked examples across different comment syntaxes
       (e.g. a `//` example, a `#` example, a `<!-- -->` example) so the language-agnostic claim is concrete.

    4. **## Valid contexts (when to mark).** The three locked cases: (a) unverified third-party
       API shape, (b) assumed behavior not yet proven, (c) stale-knowledge risk. State markers are
       set by a human OR by the future `anchor-docs`/`verify-self` skills (M2) — never by sentinel
       (sentinel only READS them).

    5. **## The scan rule.** `sentinel` greps the changed files (or the whole tree) for the literal
       token and reports each as `file:line — reason`. Tie this to the agent's return schema:
       each hit becomes one `unverified_markers` entry `{file, line, text}`. Reference
       `engine/agents/sovereign-sentinel.md` by path.

    6. **## Gate threshold (surface now, block later).** Unresolved markers are **findings** in
       sentinel's report — surfaced, NOT yet blocking. Hard blocking at a deployment/pre-flight gate
       is DEFERRED to M2 (`pre-flight`). State this explicitly so no one wires blocking now.

    7. **Closing forward-pointer footer**, matching listing-budget.md's convention, e.g.:
       `> Feeds CONV-03 (Phase 4) and the M2 anti-hallucination skills (anchor-docs, verify-self).`

    Keep it tight (~50-90 lines). This is a spec, not an essay.
  </action>
  <acceptance_criteria>
    - File `engine/references/unverified-marker.md` exists.
    - Grep `SOVEREIGN:UNVERIFIED` in the file returns multiple hits (token defined + examples).
    - Grep `| ref:` (the locked form's optional ref segment) returns at least one hit.
    - Grep `reason` (required-field rule) returns at least one hit.
    - Grep, case-insensitive, `defer` returns at least one hit (gate threshold deferred to M2).
    - Grep `pre-flight` returns at least one hit (the deferred blocking gate is named).
    - Grep `sovereign-sentinel` returns at least one hit (consumer cited by path).
    - File is >= 40 lines.
  </acceptance_criteria>
  <verify>
    <automated>test -f engine/references/unverified-marker.md && grep -q "SOVEREIGN:UNVERIFIED" engine/references/unverified-marker.md && grep -q "| ref:" engine/references/unverified-marker.md && grep -qi "defer" engine/references/unverified-marker.md && grep -q "pre-flight" engine/references/unverified-marker.md && grep -q "sovereign-sentinel" engine/references/unverified-marker.md && [ "$(wc -l < engine/references/unverified-marker.md)" -ge 40 ] && echo PASS</automated>
  </verify>
  <done>The marker spec exists, fixes the token + locked form + valid contexts + scan rule + deferred gate threshold, and names its consumer (sentinel) by path — so plan 04-06 can cite it.</done>
</task>

</tasks>

<verification>
- `engine/references/unverified-marker.md` exists and passes the grep acceptance checks.
- The spec is compatible with the already-shipped `engine/agents/sovereign-sentinel.md` consumer
  (same literal token; one hit → one `unverified_markers` entry).
</verification>

<success_criteria>
CONV-03 satisfied: the `SOVEREIGN:UNVERIFIED` marker specification is defined as a single
reference doc, ready for `sentinel` (04-06) to scan against and for M2 skills to write against.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fast-lane/04-01-SUMMARY.md`.
</output>
