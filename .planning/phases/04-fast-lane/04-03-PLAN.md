---
phase: 04-fast-lane
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - engine/skills/grill-with-docs/SKILL.md
autonomous: true
requirements: [SKL-02, SKL-06]
must_haves:
  truths:
    - "A user can run grill-with-docs and have a plan interrogated against CONTEXT.md + ADRs one question at a time"
    - "Each question is recommendation-first — the skill gives its recommended answer before waiting, and explores the codebase to answer where possible"
    - "The skill updates CONTEXT.md / ADRs inline as decisions crystallize, and offers ADRs sparingly (hard-to-reverse + surprising + real trade-off)"
    - "The skill orients with a single `sovereign-tools init grill-with-docs` call; thin orchestrator with 'Why this matters' + recommendation-first footer; auto-triggerable"
  artifacts:
    - path: "engine/skills/grill-with-docs/SKILL.md"
      provides: "The grill-with-docs Fast Lane skill (SKL-02) — plan interrogator against the domain model, recommendation-first, one question at a time, updates docs inline"
      contains: "init grill-with-docs"
      min_lines: 60
  key_links:
    - from: "engine/skills/grill-with-docs/SKILL.md"
      to: "sovereign-tools init grill-with-docs"
      via: "single orientation call returning glossary_path + manifest_path + relevant_adrs"
      pattern: "init grill-with-docs"
    - from: "engine/skills/grill-with-docs/SKILL.md"
      to: ".sovereign/CONTEXT.md and .sovereign/docs/adr/"
      via: "inline updates as decisions crystallize"
      pattern: "adr"
---

<objective>
Hand-author the `grill-with-docs` Fast Lane skill (SKL-02) as a thin orchestrator
at `engine/skills/grill-with-docs/SKILL.md`. It interrogates a plan against
`CONTEXT.md` + ADRs ONE QUESTION AT A TIME, RECOMMENDATION-FIRST (give the
recommended answer before waiting), explores the codebase to answer where it can,
and updates CONTEXT.md / ADRs INLINE as decisions crystallize. It offers ADRs
SPARINGLY (only hard-to-reverse + surprising + real trade-off).

Depends on 04-02 (ubiquitous-language): grill consumes the CONTEXT.md glossary
that ubiquitous-language establishes, and flags terminology drift against it.

Purpose: challenge the plan before code is written — the cheapest place to catch
"building the wrong thing correctly."
Output: `engine/skills/grill-with-docs/SKILL.md`.
</objective>

<execution_context>
HAND-AUTHORED skill content (R-004). Write tool, no executor. Mine v1 + Matt
Pocock voice; rebuild on the engine (drop v1 frontmatter). Acceptance is
grep-verifiable plus `validate skills` + `doctor`.
</execution_context>

<context>
@.planning/phases/04-fast-lane/04-CONTEXT.md
@.planning/REQUIREMENTS.md

# THE PATTERN: single init call + spill, "Why this matters", recommendation-first footer.
@engine/skills/council/SKILL.md

# THE INIT BLOB: fast-lane default case — context_injection.{manifest_path,glossary_path}.
# NOTE: the default case does NOT include relevant_adrs (only council/sovereign-init do). The
# skill lists ADRs itself by reading .sovereign/docs/adr/ — call this out in the action.
@engine/bin/lib/init.cjs

# V1 CONTENT TO MINE: the grilling dimensions (terminology/assumption/domain-consistency/hidden-
# complexity/acceptance-criteria), the ⚡ TERMINOLOGY FLAG block, the inline 📝 CONTEXT.md /
# 📝 ADR LOGGED blocks, the recommendation-first phrasing. DROP v1 frontmatter.
@archive/v1/skills/fast-lane/grill-with-docs/SKILL.md

# Consumes the glossary the sibling skill writes — same path contract.
@engine/skills/ubiquitous-language/SKILL.md
</context>

<interfaces>
<!-- init blob (fast-lane default case): -->
`node sovereign-tools.cjs init grill-with-docs` returns the same fast-lane shape as 04-02
(models:{}, config subset, phase, context_injection.{manifest_path, glossary_path},
paths.{state,manifest}, exists). It does NOT pre-list ADRs — the skill reads
`.sovereign/docs/adr/` itself when it needs ADR content. Bookkeeping (state save, commit)
is delegated to `sovereign-tools`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author engine/skills/grill-with-docs/SKILL.md (thin orchestrator, SKL-02 + SKL-06)</name>
  <read_first>
    - `engine/skills/council/SKILL.md` lines 47-54 (init+spill) and 134-146 (footer) — structural template.
    - `engine/skills/ubiquitous-language/SKILL.md` (sibling, authored in 04-02) — match the house style established there (heading set, init block phrasing, footer shape) so the two fast-lane skills read consistently.
    - `engine/bin/lib/init.cjs` lines 299-318 — confirm the default fast-lane blob fields; note relevant_adrs is NOT in the default case (council-only), so this skill reads `.sovereign/docs/adr/` directly.
    - `archive/v1/skills/fast-lane/grill-with-docs/SKILL.md` — mine the 5 grilling dimensions (Step 3), the ⚡ TERMINOLOGY FLAG block, the inline 📝 CONTEXT.md and 📝 ADR LOGGED blocks (Step 4), the completion report (Step 5), and recommendation-first phrasing. DROP its frontmatter.
    - `.planning/phases/04-fast-lane/04-CONTEXT.md` line 31 — locked: one question at a time, recommendation-first, explore codebase to answer, update CONTEXT.md/ADRs inline, offer ADRs sparingly (hard-to-reverse + surprising + real trade-off).
  </read_first>
  <action>
    Create dir `engine/skills/grill-with-docs/` and write `SKILL.md`.

    **Frontmatter (real spec ONLY — no v1 fields):**
    - `name: grill-with-docs`
    - `description:` trigger-led, tight: e.g. "Interrogate a plan against your domain model one hard
      question at a time — recommendation-first — flagging terminology drift and undocumented
      assumptions, and updating CONTEXT.md and ADRs inline as decisions crystallize." Under ~250 chars.
    - NO `disable-model-invocation` (auto-triggerable). Optional `argument-hint`.

    **Body (thin orchestrator):**

    1. `## Why this matters` — mine v1: "the most expensive mistake is building the wrong thing
       correctly." 1-2 short paragraphs.

    2. `## When to use this` — use-for / don't-use-for (mine v1).

    3. `## The flow`:
       - **Orient with ONE call** — show the exact init+spill block for `init grill-with-docs`;
         parse `context_injection.glossary_path` / `.manifest_path`, `paths.state` / `.manifest`,
         `config.commit_docs`. State "zero other orientation reads." Note ADRs are read on demand
         from `.sovereign/docs/adr/` (not pre-listed in the fast-lane blob).
       - **Read the grounding content** — glossary (from glossary_path) + any relevant ADRs — and
         read the user's plan against it before asking anything ("arrives informed").
       - **The grilling — ONE question at a time, recommendation-first.** Work the dimensions only
         where they surface real tension: terminology alignment (port the ⚡ TERMINOLOGY FLAG block
         — "in this project we've defined that as X"), undocumented-assumption surfacing,
         domain-model/bounded-context consistency, hidden complexity / edge cases, acceptance
         criteria. EACH question states the recommended answer FIRST, then waits. Explore the
         codebase (Grep/Read) to answer where the code already settles a question.
       - **Inline updates.** As decisions crystallize: add new terms to the glossary (📝 CONTEXT.md
         UPDATED block) and log ADRs (📝 ADR LOGGED block) — but offer ADRs SPARINGLY: only when the
         decision is hard-to-reverse AND surprising AND has a real trade-off. State this gate explicitly.
       - **Persist + footer** — delegate to the engine, mirroring council:
         ```bash
         node "$ENGINE/bin/sovereign-tools.cjs" state save
         node "$ENGINE/bin/sovereign-tools.cjs" commit "grill-with-docs: <summary>" \
           --files <glossary_path> <relevant adr paths> <paths.state> <paths.manifest>
         ```

    4. `## Navigation` — recommendation-first footer. Recommended next varies: if a hard decision
       surfaced, `/council`; otherwise proceed to build / `/tdd`. Alternatives: `/handoff`,
       `/ubiquitous-language` (if terms need defining first).

    Keep the body thin (~80-130 lines).
  </action>
  <acceptance_criteria>
    - Dir + file `engine/skills/grill-with-docs/SKILL.md` exist.
    - Frontmatter has `name: grill-with-docs` and `description:`; NO v1 fields (`triggers:`, `works-best-with`, `min-model`, `tokens:`, bare `phase:`).
    - Grep `disable-model-invocation` returns NOTHING.
    - Grep `init grill-with-docs` returns at least one hit.
    - Grep `@file:` present.
    - Grep `## Why this matters` present (SKL-06).
    - Grep, case-insensitive, `one question at a time` present (SKL-02).
    - Grep, case-insensitive, `recommend` returns multiple hits (recommendation-first).
    - Grep, case-insensitive, `adr` present (logs ADRs inline / sparingly).
    - Grep `▶ NEXT` (or explicit Navigation footer with recommended next) present.
    - `sovereign-tools validate skills` passes; `sovereign-tools doctor` clean.
    - File >= 60 lines.
  </acceptance_criteria>
  <verify>
    <automated>F=engine/skills/grill-with-docs/SKILL.md; test -f "$F" && grep -q "name: grill-with-docs" "$F" && ! grep -qE "^(triggers:|works-best-with|min-model|tokens:|phase:)" "$F" && ! grep -q "disable-model-invocation" "$F" && grep -q "init grill-with-docs" "$F" && grep -q "@file:" "$F" && grep -q "## Why this matters" "$F" && grep -qi "one question at a time" "$F" && grep -qi "recommend" "$F" && grep -qi "adr" "$F" && [ "$(wc -l < "$F")" -ge 60 ] && node engine/bin/sovereign-tools.cjs validate skills && node engine/bin/sovereign-tools.cjs doctor && echo PASS</automated>
  </verify>
  <done>grill-with-docs exists as a thin orchestrator: real frontmatter, single init call + spill, one-question-at-a-time recommendation-first grilling against CONTEXT.md + ADRs with inline updates and sparing ADR offers, "Why this matters", recommendation-first footer, auto-triggerable; validate + doctor pass.</done>
</task>

</tasks>

<verification>
- All grep acceptance checks pass; `validate skills` + `doctor` pass.
- Dir name matches `install.cjs` FAST_LANE (`grill-with-docs`).
- House style consistent with the sibling 04-02 skill.
</verification>

<success_criteria>
SKL-02 satisfied (interrogates a plan against CONTEXT.md + ADRs one question at a time,
recommendation-first, updates docs inline) and SKL-06 satisfied for this skill.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fast-lane/04-03-SUMMARY.md`.
</output>
