---
phase: 04-fast-lane
plan: 06
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - engine/skills/sentinel/SKILL.md
autonomous: true
requirements: [SKL-04, SKL-06]
must_haves:
  truths:
    - "A user can run sentinel as a native-tier post-work reviewer after a unit of work lands"
    - "Sentinel scans changed files for SOVEREIGN:UNVERIFIED markers (per the 04-01 spec), and checks commenting-standard compliance, spec alignment, and ADR consistency"
    - "Sentinel emits a structured verdict (PASS / CONDITIONAL PASS / BLOCKED) listing findings by severity"
    - "The skill orients with a single `sovereign-tools init sentinel` call, guards on agents_installed, and dispatches the sovereign-sentinel agent (and/or runs checks inline); thin orchestrator with 'Why this matters' + recommendation-first footer; auto-triggerable"
  artifacts:
    - path: "engine/skills/sentinel/SKILL.md"
      provides: "The sentinel Fast Lane skill (SKL-04) — native-tier reviewer: UNVERIFIED scan + commenting + spec + ADR checks → structured verdict"
      contains: "init sentinel"
      min_lines: 60
  key_links:
    - from: "engine/skills/sentinel/SKILL.md"
      to: "engine/references/unverified-marker.md"
      via: "scans changed files for the marker the 04-01 spec defines"
      pattern: "SOVEREIGN:UNVERIFIED"
    - from: "engine/skills/sentinel/SKILL.md"
      to: "sovereign-sentinel agent"
      via: "Task dispatch (agents_installed guard) returning the validated verdict JSON"
      pattern: "sovereign-sentinel"
---

<objective>
Hand-author the `sentinel` Fast Lane skill (SKL-04) as a thin orchestrator at
`engine/skills/sentinel/SKILL.md`. It is the native-tier post-work reviewer:
(a) scans changed files for `SOVEREIGN:UNVERIFIED` markers per the 04-01 spec,
(b) checks commenting-standard compliance (per archive/v1/COMMENTING.md),
(c) checks spec alignment (acceptance criteria vs implementation),
(d) checks ADR consistency — then emits a STRUCTURED VERDICT
(PASS / CONDITIONAL PASS / BLOCKED) with findings by severity. Native tier only
(CodeRabbit Tier-2 deferred to M2).

Depends on 04-01 (the marker spec): sentinel scans for the token that spec defines,
so the spec must exist and be cite-able by path. Sentinel dispatches the
`sovereign-sentinel` agent (already defined in Phase 2) and/or runs the checks
inline (Claude's discretion — prefer dispatch for the heavier spec-alignment read).

Purpose: something must review written work — the anti-hallucination beachhead.
Output: `engine/skills/sentinel/SKILL.md`.
</objective>

<execution_context>
HAND-AUTHORED skill content (R-004). Write tool, no executor. Acceptance is
grep-verifiable plus `validate skills` + `doctor`.
</execution_context>

<context>
@.planning/phases/04-fast-lane/04-CONTEXT.md
@.planning/REQUIREMENTS.md

# THE PATTERN: single init call + spill, the agents-guard (hard error, no fallback), Task
# dispatch with models/agent, structured verdict rendering, "Why this matters", footer.
@engine/skills/council/SKILL.md

# THE MARKER SPEC THIS SKILL SCANS AGAINST (authored in 04-01) — cite it by path.
@engine/references/unverified-marker.md

# THE AGENT THIS SKILL DISPATCHES (Phase 2): return schema {ok, verdict PASS|CONCERNS|BLOCKED,
# unverified_markers[], findings[]}. NOTE the agent enum is CONCERNS; the skill RENDERS the
# human-facing verdict as "CONDITIONAL PASS" (mirror council's enum→label rendering).
@engine/agents/sovereign-sentinel.md

# THE INIT BLOB: sentinel is the one fast-lane skill whose init lists a required agent.
# REQUIRED_AGENTS.sentinel = ['sovereign-sentinel'] → agents_installed/missing_agents apply here.
@engine/bin/lib/init.cjs

# THE COMMENTING STANDARD sentinel checks against (HexDoc-style: why/contract/danger, not what).
@archive/v1/COMMENTING.md
</context>

<interfaces>
<!-- init blob — sentinel is NOT the bare fast-lane default: REQUIRED_AGENTS.sentinel is set,
     so the agents guard is live. -->
`node sovereign-tools.cjs init sentinel` returns the fast-lane shape PLUS a meaningful
agents_installed/missing_agents (checkAgents probes for `sovereign-sentinel.md`):
```
{ models: {}, config: {...}, phase: {...},
  context_injection: { manifest_path, glossary_path },
  paths: { state, manifest }, exists: {...},
  agents_installed: true|false, missing_agents: [...] }
```
The dispatched `sovereign-sentinel` agent returns:
```
{ ok, verdict: "PASS|CONCERNS|BLOCKED",
  unverified_markers: [{file, line, text}],
  findings: [{severity: "info|warn|block", file, message}] }
```
The skill RENDERS `CONCERNS` to the user as "CONDITIONAL PASS" (the human-facing verdict label),
mirroring council's CONDITIONAL_PASS rendering. The orchestrator passes the agent the changed-file
PATHS (path-passing, not content), exactly as council passes paths to its advisors.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author engine/skills/sentinel/SKILL.md (thin orchestrator, SKL-04 + SKL-06)</name>
  <read_first>
    - `engine/skills/council/SKILL.md` lines 44-89 — the FULL orchestration template: ONE init call + spill (step 1), the agents-guard hard-error-no-fallback (step 2), Task dispatch with model + path-passing (steps 4-6), the verdict enum→label rendering (line 132), the side-effects + footer (step 8). Sentinel mirrors this shape closely.
    - `engine/agents/sovereign-sentinel.md` — the return schema the skill consumes + renders (verdict CONCERNS→"CONDITIONAL PASS"; unverified_markers; findings by severity info|warn|block).
    - `engine/references/unverified-marker.md` (authored in 04-01) — cite it by path; the scan rule (each token hit → one `file:line — reason`) lives there.
    - `engine/bin/lib/init.cjs` lines 153-157 (REQUIRED_AGENTS.sentinel = ['sovereign-sentinel']) and 186-196 (checkAgents) — confirm the agents guard is live for this skill.
    - `archive/v1/COMMENTING.md` — the commenting standard the skill checks against (module header, public-function contract comments, why/contract/danger; cite by path).
    - `.planning/phases/04-fast-lane/04-CONTEXT.md` line 33 + lines 43-46 — locked behaviors + Claude's discretion (finding/severity format; dispatch vs inline — either acceptable, prefer dispatch for spec-alignment).
  </read_first>
  <action>
    Create dir `engine/skills/sentinel/` and write `SKILL.md`.

    **Frontmatter (real spec ONLY — no v1 fields):**
    - `name: sentinel`
    - `description:` trigger-led, tight: e.g. "Native-tier post-work review — scan changed files
      for SOVEREIGN:UNVERIFIED markers and check commenting-standard, spec alignment, and ADR
      consistency, then return a PASS / CONDITIONAL PASS / BLOCKED verdict with findings by
      severity." Under ~250 chars.
    - NO `disable-model-invocation` (auto-triggerable). Optional `argument-hint` for the
      changed-file scope.

    **Body (thin orchestrator):**

    1. `## Why this matters` — something must check work AFTER it is written; AI writes confident
       code under uncertainty. Sentinel makes that uncertainty (UNVERIFIED markers) and
       spec/ADR/commenting drift visible before they ship. 1-2 short paragraphs.

    2. `## When to use this` — use-for (after a unit of work lands; before handing off / at a phase
       boundary) / don't-use-for (mid-edit; trivial doc tweaks). Note: native tier only; CodeRabbit
       Tier-2 is deferred to M2 — if invoked with a Tier-2 flag, print a one-line "not yet
       available, running native tier" notice (mirror council's --express/--deep notice).

    3. `## The flow`:
       - **Orient with ONE call** — exact init+spill block for `init sentinel`; parse
         `agents_installed` / `missing_agents`, `context_injection.{manifest_path,glossary_path}`,
         `paths`, `config`. State "zero other orientation reads."
       - **Determine the changed-file scope** — the set of paths to review (e.g. from the user's
         argument or `git diff --name-only`); path-passing.
       - **Agents guard (hard error, no fallback)** — if `agents_installed` is false, STOP, print
         `missing_agents` + the fix `npx sovereign init --full`. Never silently fall back to a
         general-purpose agent (mirror council step 2).
       - **Review** — dispatch `Task(subagent_type="sovereign-sentinel")` with the changed-file
         PATHS (prefer dispatch for the heavier spec-alignment read; inline is acceptable per
         discretion). The agent performs the four checks: (a) scan for `SOVEREIGN:UNVERIFIED`
         markers per `engine/references/unverified-marker.md` (each hit → `file:line — reason`),
         (b) commenting-standard compliance per `archive/v1/COMMENTING.md`, (c) spec alignment
         (acceptance criteria vs implementation), (d) ADR consistency. It returns the verdict JSON.
       - **Render the verdict** — print the structured report: verdict (render `CONCERNS` as
         "CONDITIONAL PASS"), the unverified-marker list (`file:line — reason`), and findings grouped
         by severity (info / warn / block). State the gate threshold from 04-01: unresolved markers
         are surfaced as findings, NOT blocking now (blocking deferred to M2 pre-flight).
       - **Persist + footer** — optionally write the report under `.sovereign/` and delegate
         bookkeeping to the engine:
         ```bash
         node "$ENGINE/bin/sovereign-tools.cjs" state save
         node "$ENGINE/bin/sovereign-tools.cjs" commit "sentinel: <verdict>" \
           --files <report path, if written> <paths.state> <paths.manifest>
         ```

    4. `## Navigation` — recommendation-first footer (mirror council's verdict-branched footer):
       After PASS → proceed / `/handoff`; after CONDITIONAL PASS → resolve listed findings then
       re-run; after BLOCKED → fix block-severity findings + unresolved markers before shipping.

    Keep the body thin (~90-140 lines).
  </action>
  <acceptance_criteria>
    - Dir + file `engine/skills/sentinel/SKILL.md` exist.
    - Frontmatter has `name: sentinel` and `description:`; NO v1 fields (`triggers:`, `works-best-with`, `min-model`, `tokens:`, bare `phase:`).
    - Grep `disable-model-invocation` returns NOTHING.
    - Grep `init sentinel` returns at least one hit.
    - Grep `@file:` present.
    - Grep `## Why this matters` present (SKL-06).
    - Grep `SOVEREIGN:UNVERIFIED` present (the marker scan, SKL-04 + CONV-03 link).
    - Grep `unverified-marker.md` present (cites the 04-01 spec by path).
    - Grep `sovereign-sentinel` present (dispatches the agent).
    - Grep `agents_installed` present (the hard-error guard).
    - Grep, case-insensitive, `commenting` present (commenting-standard check).
    - Grep `CONDITIONAL PASS` present AND grep `BLOCKED` present (structured verdict).
    - Grep `▶ NEXT` (or explicit Navigation footer) present.
    - `sovereign-tools validate skills` passes; `sovereign-tools doctor` clean.
    - File >= 60 lines.
  </acceptance_criteria>
  <verify>
    <automated>F=engine/skills/sentinel/SKILL.md; test -f "$F" && grep -q "name: sentinel" "$F" && ! grep -qE "^(triggers:|works-best-with|min-model|tokens:|phase:)" "$F" && ! grep -q "disable-model-invocation" "$F" && grep -q "init sentinel" "$F" && grep -q "@file:" "$F" && grep -q "## Why this matters" "$F" && grep -q "SOVEREIGN:UNVERIFIED" "$F" && grep -q "unverified-marker.md" "$F" && grep -q "sovereign-sentinel" "$F" && grep -q "agents_installed" "$F" && grep -qi "commenting" "$F" && grep -q "CONDITIONAL PASS" "$F" && grep -q "BLOCKED" "$F" && [ "$(wc -l < "$F")" -ge 60 ] && node engine/bin/sovereign-tools.cjs validate skills && node engine/bin/sovereign-tools.cjs doctor && echo PASS</automated>
  </verify>
  <done>sentinel exists as a thin orchestrator: real frontmatter, single init call + spill, agents-guard, dispatch of sovereign-sentinel for the four checks (UNVERIFIED scan per the 04-01 spec + commenting + spec + ADR), structured PASS/CONDITIONAL PASS/BLOCKED verdict with severity findings, "Why this matters", recommendation-first footer, auto-triggerable; validate + doctor pass.</done>
</task>

</tasks>

<verification>
- All grep acceptance checks pass; `validate skills` + `doctor` pass.
- Dir name matches `install.cjs` FAST_LANE (`sentinel`).
- The skill cites `engine/references/unverified-marker.md` (04-01) and dispatches `sovereign-sentinel` (Phase 2) — both consumers wired.
</verification>

<success_criteria>
SKL-04 satisfied (native-tier reviewer: UNVERIFIED scan + commenting + spec + ADR checks →
structured verdict) and SKL-06 satisfied for this skill.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fast-lane/04-06-SUMMARY.md`.
</output>
