---
phase: 04-fast-lane
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/skills/ubiquitous-language/SKILL.md
autonomous: true
requirements: [SKL-01, SKL-06]
must_haves:
  truths:
    - "A user can run ubiquitous-language and define/update the CONTEXT.md glossary one term at a time"
    - "The skill detects conflicts with existing terms ('your glossary defines X as Y, but you mean Z')"
    - "The skill orients with a single `sovereign-tools init ubiquitous-language` call (no other orientation reads)"
    - "The skill is a thin orchestrator with a 'Why this matters' section and a recommendation-first navigation footer, and is auto-triggerable (no disable-model-invocation)"
  artifacts:
    - path: "engine/skills/ubiquitous-language/SKILL.md"
      provides: "The ubiquitous-language Fast Lane skill (SKL-01) — glossary builder, one term at a time, conflict-detecting, thin orchestrator over the engine"
      contains: "init ubiquitous-language"
      min_lines: 60
  key_links:
    - from: "engine/skills/ubiquitous-language/SKILL.md"
      to: "sovereign-tools init ubiquitous-language"
      via: "the single orientation call that returns glossary_path + manifest_path"
      pattern: "init ubiquitous-language"
    - from: "engine/skills/ubiquitous-language/SKILL.md"
      to: ".sovereign/CONTEXT.md"
      via: "writes confirmed terms to the glossary path from init"
      pattern: "CONTEXT\\.md"
---

<objective>
Hand-author the `ubiquitous-language` Fast Lane skill (SKL-01) as a thin
orchestrator over the engine, at `engine/skills/ubiquitous-language/SKILL.md`.
It establishes/updates the `.sovereign/CONTEXT.md` glossary ONE TERM AT A TIME,
detecting conflicts with existing terms and recommending a canonical term +
`_Avoid_` synonyms. CONTEXT.md is a glossary ONLY (no implementation detail).

Build order: this is FIRST among the skills — its CONTEXT.md output feeds
`grill-with-docs` (04-03) and Council context injection. The installer's
`FAST_LANE` const already lists this exact dir name (`ubiquitous-language`).

Purpose: shared vocabulary before specs. SOVEREIGN's DDD-glossary discipline.
Output: `engine/skills/ubiquitous-language/SKILL.md`.
</objective>

<execution_context>
HAND-AUTHORED skill content (R-004). The orchestrator authors the file with the
Write tool. Mine v1 heavily for voice/content; rebuild on the engine (drop ALL
v1 frontmatter fields). Acceptance is grep-verifiable against the authored file
plus `validate skills` and `doctor`.
</execution_context>

<context>
@.planning/phases/04-fast-lane/04-CONTEXT.md
@.planning/REQUIREMENTS.md

# THE PATTERN TO FOLLOW (Phase-3 thin orchestrator): real frontmatter, single init call + @file:
# spill, "Why this matters", recommendation-first nav footer.
@engine/skills/council/SKILL.md

# THE INIT BLOB this skill consumes (default fast-lane case): context_injection.{manifest_path,
# glossary_path}, paths.{state,manifest}, phase, exists. NOTE models:{} for fast-lane skills.
@engine/bin/lib/init.cjs

# V1 CONTENT TO MINE (voice, the term-definition loop, conflict-detection block, bounded-context
# detection, the CONTEXT.md entry format). DROP its frontmatter (phase/triggers/works-best-with/
# min-model/tokens) — those are the v1 anti-fields ADR explicitly bans.
@archive/v1/skills/fast-lane/ubiquitous-language/SKILL.md

# The installer dir-name contract — the authored dir MUST be exactly "ubiquitous-language".
@engine/bin/lib/install.cjs
</context>

<interfaces>
<!-- The init blob this skill orients from (fast-lane default case in buildInit). The skill
     reads ZERO other files to orient; it reads CONTENT (CONTEXT.md) only when it needs it,
     via context_injection.glossary_path. -->

`node sovereign-tools.cjs init ubiquitous-language` returns:
```
{
  project_root, sovereign_version,
  models: {},                       // fast-lane skills resolve no reasoning models
  config: { model_profile, commit_docs, council_mode_default, parallelization },
  phase: { current, name, gate_status, active_tracks },
  context_injection: { manifest_path: ".sovereign/MANIFEST.md",
                       glossary_path:  ".sovereign/CONTEXT.md" },
  paths: { state: ".sovereign/STATE.md", manifest: ".sovereign/MANIFEST.md" },
  exists: { sovereign_dir, manifest, constitution, glossary },
  agents_installed: true, missing_agents: []
}
```
Bookkeeping (`state save`, `commit`) is delegated to `sovereign-tools`, mirroring council/SKILL.md.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author engine/skills/ubiquitous-language/SKILL.md (thin orchestrator, SKL-01 + SKL-06)</name>
  <read_first>
    - `engine/skills/council/SKILL.md` lines 1-6 (frontmatter shape) and 47-54 (the ONE init call + @file: spill block) and 134-146 (recommendation-first nav footer). This is the structural template.
    - `engine/bin/lib/init.cjs` lines 299-322 — the default fast-lane blob this skill consumes (confirm glossary_path/manifest_path/exists fields).
    - `archive/v1/skills/fast-lane/ubiquitous-language/SKILL.md` — mine: the term-definition loop (Steps 2-3), the ⚡ CONFLICT DETECTED block (Step 4), the 📌 BOUNDED CONTEXT block (Step 5), the CONTEXT.md entry format (Step 6), the completion report (Step 7). KEEP the voice; DROP the v1 frontmatter entirely.
    - `engine/bin/lib/install.cjs` line 42 — confirm `ubiquitous-language` is the exact FAST_LANE name.
    - `.planning/phases/04-fast-lane/04-CONTEXT.md` line 30 — locked behavior: one term at a time, conflict detection, canonical term + `_Avoid_` synonyms, glossary-ONLY (no implementation detail).
  </read_first>
  <action>
    Create dir `engine/skills/ubiquitous-language/` and write `SKILL.md`.

    **Frontmatter (real Agent Skills spec ONLY — NO v1 fields):**
    - `name: ubiquitous-language`
    - `description:` lead with the trigger use-case, tight (keep listing budget): e.g.
      "Build your project's shared glossary one term at a time — define the core domain nouns
      and verbs, detect conflicts with existing terms, and lock a canonical word for each before
      any spec is written." Single sentence/clause, under ~250 chars. No `disable-model-invocation`
      (this skill IS auto-triggerable — that is the locked decision).
    - OPTIONAL `argument-hint`. Do NOT add triggers/phase/works-best-with/min-model/tokens.

    **Body (thin orchestrator — flow + content only; delegate bookkeeping to the engine):**

    1. `## Why this matters` — plain-language. Mine v1's "every failed project has a language
       problem" framing. 1-2 short paragraphs. Why one canonical term per concept saves more
       engineering hours than almost anything.

    2. `## When to use this` — use-for / don't-use-for bullets (mine v1 Step "When to Use").

    3. `## The flow` (thin orchestrator). Number the steps:
       - **Orient with ONE call.** Show the exact init+spill block:
         ```bash
         INIT=$(node "$ENGINE/bin/sovereign-tools.cjs" init ubiquitous-language)
         if [[ "$INIT" == @file:* ]]; then INIT=$(cat "${INIT#@file:}"); fi
         ```
         Parse `context_injection.glossary_path` / `.manifest_path`, `exists.glossary`,
         `paths.state` / `.manifest`, `config.commit_docs`. State plainly: "the skill performs
         zero other orientation reads."
       - **Read the glossary content** (only now) from `context_injection.glossary_path` to know
         what's already defined.
       - **Term loop — one term at a time.** For each concept the user names: clarify what it IS
         (not what it does), boundary-check against similar terms, recommend a CANONICAL term plus
         `_Avoid_` synonyms (recommendation-first — give the recommended definition before waiting).
       - **Conflict detection.** When a new term overlaps an existing glossary entry, surface it
         in the "your glossary defines X as Y, but you mean Z" shape (port v1's ⚡ CONFLICT block).
       - **Bounded-context flag** (optional, when 2+ domains emerge) — port v1's 📌 block.
       - **Write each confirmed term** to `context_injection.glossary_path` in the standard entry
         format (Definition / Used in / Avoid / Example). State CONTEXT.md is a glossary ONLY —
         no implementation detail.
       - **Persist + footer.** Delegate to the engine, mirroring council/SKILL.md:
         ```bash
         node "$ENGINE/bin/sovereign-tools.cjs" state save     # regenerates MANIFEST
         node "$ENGINE/bin/sovereign-tools.cjs" commit "ubiquitous-language: +<N> terms" \
           --files <glossary_path> <paths.state> <paths.manifest>
         ```
         `commit` is gated by the engine on commit_docs + gitignore — do not branch on those here.

    4. `## Navigation` — recommendation-first footer (mine council's `▶ NEXT` shape). Recommended
       next: `/grill-with-docs` (now that terms are locked, stress-test a plan against them).
       Alternatives: `/council`, `/handoff`.

    Keep the body thin (~70-120 lines). Heavy detail (the full entry format) can be inlined since
    it's short; do not balloon it.
  </action>
  <acceptance_criteria>
    - Dir + file `engine/skills/ubiquitous-language/SKILL.md` exist.
    - Frontmatter has `name: ubiquitous-language` and a `description:`; grep finds NO v1 fields:
      `triggers:`, `works-best-with`, `min-model`, `tokens:`, and no bare `phase:` line all absent.
    - Grep `disable-model-invocation` returns NOTHING (skill must stay auto-triggerable).
    - Grep `init ubiquitous-language` returns at least one hit (single orientation call).
    - Grep `@file:` returns at least one hit (spill guard present).
    - Grep `## Why this matters` returns exactly the heading (SKL-06).
    - Grep `CONTEXT.md` returns at least one hit (writes the glossary).
    - Grep, case-insensitive, `one term at a time` returns at least one hit (SKL-01 behavior).
    - Grep `▶ NEXT` (or an explicit "Navigation" footer with a recommended next) present (SKL-06).
    - `sovereign-tools validate skills` passes (name <=64 lowercase-hyphen no claude/anthropic, desc within cap).
    - `sovereign-tools doctor` still reports clean (<=7 auto-triggerable).
    - File is >= 60 lines.
  </acceptance_criteria>
  <verify>
    <automated>F=engine/skills/ubiquitous-language/SKILL.md; test -f "$F" && grep -q "name: ubiquitous-language" "$F" && ! grep -qE "^(triggers:|works-best-with|min-model|tokens:|phase:)" "$F" && ! grep -q "disable-model-invocation" "$F" && grep -q "init ubiquitous-language" "$F" && grep -q "@file:" "$F" && grep -q "## Why this matters" "$F" && grep -q "CONTEXT.md" "$F" && grep -qi "one term at a time" "$F" && [ "$(wc -l < "$F")" -ge 60 ] && node engine/bin/sovereign-tools.cjs validate skills && node engine/bin/sovereign-tools.cjs doctor && echo PASS</automated>
  </verify>
  <done>ubiquitous-language exists as a thin orchestrator: real frontmatter (no v1 fields), single init call + spill, one-term-at-a-time conflict-detecting glossary flow writing CONTEXT.md, "Why this matters", recommendation-first footer, auto-triggerable; validate + doctor pass.</done>
</task>

</tasks>

<verification>
- File passes all grep acceptance checks above.
- `validate skills` and `doctor` both pass (frontmatter compliant; listing budget held).
- Dir name matches `install.cjs` FAST_LANE (`ubiquitous-language`).
</verification>

<success_criteria>
SKL-01 satisfied (glossary one term at a time, conflict detection) and SKL-06 satisfied for this
skill (thin orchestrator: single init call + nav footer + "Why this matters", auto-triggerable).
</success_criteria>

<output>
After completion, create `.planning/phases/04-fast-lane/04-02-SUMMARY.md`.
</output>
