---
phase: 04-fast-lane
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - engine/skills/handoff/SKILL.md
autonomous: true
requirements: [SKL-03, SKL-06]
must_haves:
  truths:
    - "A user can run handoff and compress the current session into a resumable handoff doc at .sovereign/HANDOFF.md"
    - "The handoff captures decisions made, current phase, next action, and unresolved items — so another agent resumes with full context"
    - "The skill updates STATE via the engine (state save → MANIFEST regen) rather than hand-editing"
    - "The skill orients with a single `sovereign-tools init handoff` call; thin orchestrator with 'Why this matters' + recommendation-first footer; auto-triggerable"
  artifacts:
    - path: "engine/skills/handoff/SKILL.md"
      provides: "The handoff Fast Lane skill (SKL-03) — session compression to a resumable HANDOFF.md, STATE updated via the engine"
      contains: "init handoff"
      min_lines: 60
  key_links:
    - from: "engine/skills/handoff/SKILL.md"
      to: "sovereign-tools init handoff"
      via: "single orientation call returning manifest_path + state path + phase"
      pattern: "init handoff"
    - from: "engine/skills/handoff/SKILL.md"
      to: ".sovereign/HANDOFF.md"
      via: "writes the compressed resumable handoff document"
      pattern: "HANDOFF\\.md"
---

<objective>
Hand-author the `handoff` Fast Lane skill (SKL-03) as a thin orchestrator at
`engine/skills/handoff/SKILL.md`. It compresses the current session into a
resumable handoff doc at `.sovereign/HANDOFF.md` — decisions made, current phase,
next action, unresolved items — so a fresh agent resumes with full context. It
updates STATE via the engine (`state save`, which regenerates MANIFEST), and
includes a "resuming from a handoff" path.

Independent of the other skills (lowest coupling — reads context, writes one doc).
Placed in wave 2 so all five skills land as a reviewable batch after the glossary
foundation.

Purpose: context-reset survival — the baseline category promise.
Output: `engine/skills/handoff/SKILL.md`.
</objective>

<execution_context>
HAND-AUTHORED skill content (R-004). Write tool, no executor. Mine v1 voice;
rebuild on the engine (drop v1 frontmatter). Acceptance is grep-verifiable plus
`validate skills` + `doctor`.
</execution_context>

<context>
@.planning/phases/04-fast-lane/04-CONTEXT.md
@.planning/REQUIREMENTS.md

# THE PATTERN: single init call + spill, "Why this matters", recommendation-first footer.
@engine/skills/council/SKILL.md

# THE INIT BLOB: fast-lane default case — context_injection.{manifest_path,glossary_path},
# paths.{state,manifest}, phase.{current,name,gate_status}.
@engine/bin/lib/init.cjs

# V1 CONTENT TO MINE: the session-scan checklist, the HANDOFF.md document template, the
# "resuming from a handoff" brief, the SOVEREIGN:UNVERIFIED-markers-this-session line. DROP
# v1 frontmatter (phase:system/triggers/works-best-with/min-model/tokens).
@archive/v1/skills/fast-lane/handoff/SKILL.md
</context>

<interfaces>
<!-- init blob (fast-lane default case): -->
`node sovereign-tools.cjs init handoff` returns the fast-lane shape: models:{}, config subset,
phase.{current,name,gate_status,active_tracks}, context_injection.{manifest_path,glossary_path},
paths.{state,manifest}, exists. STATE bookkeeping is delegated to `sovereign-tools state save`
(which regenerates MANIFEST) — the skill does NOT hand-edit STATE.md/MANIFEST.md.
There is no engine path for HANDOFF.md in the blob; the skill writes
`.sovereign/HANDOFF.md` directly (it is plain content, not engine-managed state).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author engine/skills/handoff/SKILL.md (thin orchestrator, SKL-03 + SKL-06)</name>
  <read_first>
    - `engine/skills/council/SKILL.md` lines 47-54 (init+spill) and 134-146 (footer) — structural template.
    - `engine/skills/ubiquitous-language/SKILL.md` (authored in 04-02) — match house style (headings, init block phrasing, footer).
    - `engine/bin/lib/init.cjs` lines 299-318 — the default fast-lane blob fields (phase, manifest_path, state path).
    - `archive/v1/skills/fast-lane/handoff/SKILL.md` — mine: Step 1 session-scan checklist, Step 3 HANDOFF.md template (What this session was about / Completed / Decisions / In progress / Open questions / Blockers / UNVERIFIED markers added / Phase status / Exact resume instructions / Files changed), the "Resuming From a Handoff" brief. DROP v1 frontmatter.
    - `.planning/phases/04-fast-lane/04-CONTEXT.md` line 32 — locked: compress session into resumable HANDOFF.md (decisions/current phase/next action/unresolved); update STATE via the engine.
  </read_first>
  <action>
    Create dir `engine/skills/handoff/` and write `SKILL.md`.

    **Frontmatter (real spec ONLY — no v1 fields):**
    - `name: handoff`
    - `description:` trigger-led, tight: e.g. "Compress the current session into a resumable
      handoff document — decisions made, current phase, the exact next action, and unresolved
      items — so any agent picks up exactly where you stopped." Under ~250 chars.
    - NO `disable-model-invocation` (auto-triggerable). Optional `argument-hint`.

    **Body (thin orchestrator):**

    1. `## Why this matters` — mine v1: "agent sessions are not infinite … run it before every
       session ends; it costs almost nothing and saves everything." 1-2 short paragraphs.

    2. `## When to use this` — use-for / don't-use-for (mine v1; note cross-project handoff is the
       deferred `/bridge`, out of scope for M1).

    3. `## The flow`:
       - **Orient with ONE call** — exact init+spill block for `init handoff`; parse
         `context_injection.manifest_path`, `phase.{current,name,gate_status}`, `paths.state` /
         `.manifest`, `config.commit_docs`. State "zero other orientation reads."
       - **Scan the session** — port v1's checklist: goal, decisions made (logged?), what was
         built/changed, what's in progress, blockers, last agreed next action.
       - **Write HANDOFF.md** to `.sovereign/HANDOFF.md` using the v1 template (compressed): what
         the session was about, completed, decisions, in-progress (with exact stop point + next
         step), open questions, blockers, `SOVEREIGN:UNVERIFIED` markers added this session,
         phase status, exact resume instructions, files changed.
       - **Update STATE via the engine** — `state save` (regenerates MANIFEST); do NOT hand-edit
         STATE.md/MANIFEST.md. Then commit:
         ```bash
         node "$ENGINE/bin/sovereign-tools.cjs" state save
         node "$ENGINE/bin/sovereign-tools.cjs" commit "handoff: session checkpoint" \
           --files .sovereign/HANDOFF.md <paths.state> <paths.manifest>
         ```
       - **Resuming from a handoff** — a short subsection: when the user says "resume from
         handoff," read `.sovereign/HANDOFF.md` + the manifest and print the resume brief (port
         v1's "RESUMING:" block).

    4. `## Navigation` — recommendation-first footer. Recommended next: end the session here, OR
       (on resume) the first-thing-to-do pulled from HANDOFF.md. Alternatives: `/grill-with-docs`,
       `/council`.

    Keep the body thin (~80-130 lines).
  </action>
  <acceptance_criteria>
    - Dir + file `engine/skills/handoff/SKILL.md` exist.
    - Frontmatter has `name: handoff` and `description:`; NO v1 fields (`triggers:`, `works-best-with`, `min-model`, `tokens:`, bare `phase:`).
    - Grep `disable-model-invocation` returns NOTHING.
    - Grep `init handoff` returns at least one hit.
    - Grep `@file:` present.
    - Grep `## Why this matters` present (SKL-06).
    - Grep `HANDOFF.md` returns at least one hit (writes the resumable doc).
    - Grep `state save` present (STATE updated via the engine, not hand-edited).
    - Grep, case-insensitive, `resum` returns at least one hit (resumable / resuming path).
    - Grep `▶ NEXT` (or explicit Navigation footer) present.
    - `sovereign-tools validate skills` passes; `sovereign-tools doctor` clean.
    - File >= 60 lines.
  </acceptance_criteria>
  <verify>
    <automated>F=engine/skills/handoff/SKILL.md; test -f "$F" && grep -q "name: handoff" "$F" && ! grep -qE "^(triggers:|works-best-with|min-model|tokens:|phase:)" "$F" && ! grep -q "disable-model-invocation" "$F" && grep -q "init handoff" "$F" && grep -q "@file:" "$F" && grep -q "## Why this matters" "$F" && grep -q "HANDOFF.md" "$F" && grep -q "state save" "$F" && grep -qi "resum" "$F" && [ "$(wc -l < "$F")" -ge 60 ] && node engine/bin/sovereign-tools.cjs validate skills && node engine/bin/sovereign-tools.cjs doctor && echo PASS</automated>
  </verify>
  <done>handoff exists as a thin orchestrator: real frontmatter, single init call + spill, session-scan → HANDOFF.md compression + resume brief, STATE updated via `state save`, "Why this matters", recommendation-first footer, auto-triggerable; validate + doctor pass.</done>
</task>

</tasks>

<verification>
- All grep acceptance checks pass; `validate skills` + `doctor` pass.
- Dir name matches `install.cjs` FAST_LANE (`handoff`).
</verification>

<success_criteria>
SKL-03 satisfied (session compressed into a resumable handoff doc, STATE updated via the engine)
and SKL-06 satisfied for this skill.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fast-lane/04-04-SUMMARY.md`.
</output>
