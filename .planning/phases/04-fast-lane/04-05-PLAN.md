---
phase: 04-fast-lane
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - engine/skills/tdd/SKILL.md
autonomous: true
requirements: [SKL-05, SKL-06]
must_haves:
  truths:
    - "A user can run tdd and be driven through a red-green-refactor loop using the project's OWN test runner"
    - "The skill tests observable behavior at the interface, not implementation, and mocks only at system boundaries"
    - "The skill is stack-agnostic — no runtime QA, no assumed framework (that is an anti-feature)"
    - "The skill orients with a single `sovereign-tools init tdd` call; thin orchestrator with 'Why this matters' + recommendation-first footer; auto-triggerable"
  artifacts:
    - path: "engine/skills/tdd/SKILL.md"
      provides: "The tdd Fast Lane skill (SKL-05) — red-green-refactor loop, behavior-at-the-interface, stack-agnostic"
      contains: "init tdd"
      min_lines: 60
  key_links:
    - from: "engine/skills/tdd/SKILL.md"
      to: "sovereign-tools init tdd"
      via: "single orientation call (phase, config); the skill drives the project's own tests"
      pattern: "init tdd"
---

<objective>
Hand-author the `tdd` Fast Lane skill (SKL-05) as a thin orchestrator at
`engine/skills/tdd/SKILL.md`. It drives a red-green-refactor loop: write a FAILING
test (RED) → minimal code to pass (GREEN) → refactor. It tests OBSERVABLE BEHAVIOR
at the interface (not implementation), mocks ONLY at system boundaries, and is
STACK-AGNOSTIC — it uses the project's own test runner. No runtime/browser QA
(that is an explicit anti-feature per FEATURES.md and REQUIREMENTS out-of-scope).

The only Fast Lane skill with no dependents — fully standalone. Wave 2 for the
reviewable batch.

Purpose: test-discipline credibility — a "build it properly" system must have it.
Output: `engine/skills/tdd/SKILL.md`.
</objective>

<execution_context>
HAND-AUTHORED skill content (R-004). Write tool, no executor. Mine the GSD tdd
reference + Matt Pocock voice; rebuild on the engine. Acceptance is grep-verifiable
plus `validate skills` + `doctor`.
</execution_context>

<context>
@.planning/phases/04-fast-lane/04-CONTEXT.md
@.planning/REQUIREMENTS.md

# THE PATTERN: single init call + spill, "Why this matters", recommendation-first footer.
@engine/skills/council/SKILL.md

# THE INIT BLOB: fast-lane default case (models:{}, config subset, phase, paths). tdd writes
# NO .sovereign state (drives the project's own tests) — it still orients via init for phase/config.
@engine/bin/lib/init.cjs

# VOICE / DISCIPLINE TO MINE: red-green-refactor cycle, "behavior not coverage", the
# expect(fn(input)).toBe(output) heuristic, test-at-the-interface, mock-only-at-boundaries.
@/Users/wizzie/.claude/get-shit-done/references/tdd.md

# Anti-feature boundary: no real-browser/runtime QA — stack-agnostic, project's own runner only.
@.planning/research/FEATURES.md
</context>

<interfaces>
<!-- init blob (fast-lane default case): -->
`node sovereign-tools.cjs init tdd` returns the fast-lane shape (models:{}, config subset, phase,
context_injection.{manifest_path,glossary_path}, paths.{state,manifest}, exists). Unlike the
other four skills, tdd writes NO SOVEREIGN state and does NOT call `state save`/`commit` for
SOVEREIGN docs — it drives the PROJECT's own test runner and the project's own commits. It still
orients via the single init call (for phase/config) to honor SKL-06.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author engine/skills/tdd/SKILL.md (thin orchestrator, SKL-05 + SKL-06)</name>
  <read_first>
    - `engine/skills/council/SKILL.md` lines 47-54 (init+spill) and 134-146 (footer) — structural template.
    - `engine/skills/ubiquitous-language/SKILL.md` (authored in 04-02) — match house style.
    - `engine/bin/lib/init.cjs` lines 299-318 — the default fast-lane blob fields.
    - `/Users/wizzie/.claude/get-shit-done/references/tdd.md` lines 1-32 — mine: "design quality not coverage," the `expect(fn(input)).toBe(output)` heuristic, the RED→GREEN→REFACTOR framing, behavior-at-the-interface, mock-only-at-boundaries.
    - `.planning/research/FEATURES.md` lines 74-82 (anti-features: no real-browser/runtime QA) and 102 — tdd is stack-agnostic, uses the project's own runner, lowest coupling.
    - `.planning/phases/04-fast-lane/04-CONTEXT.md` line 34 — locked: red-green-refactor; test observable behavior at the interface not implementation; mock only at system boundaries; stack-agnostic; no runtime QA (anti-feature).
  </read_first>
  <action>
    Create dir `engine/skills/tdd/` and write `SKILL.md`.

    **Frontmatter (real spec ONLY — no v1 fields):**
    - `name: tdd`
    - `description:` trigger-led, tight: e.g. "Drive a red-green-refactor loop — write a failing
      test for the behavior you want, the minimal code to pass it, then refactor — testing
      observable behavior at the interface using your project's own test runner." Under ~250 chars.
    - NO `disable-model-invocation` (auto-triggerable). Optional `argument-hint`.

    **Body (thin orchestrator):**

    1. `## Why this matters` — TDD is about design quality, not coverage metrics: writing the test
       first forces you to design the interface before the implementation. 1-2 short paragraphs.

    2. `## When to use this` — use-for (business logic with defined I/O, API contracts, parsers,
       validation, algorithms, state machines) / don't-use-for (UI layout, config, glue, one-off
       scripts, exploratory prototyping). Include the heuristic: "if you can write
       `expect(fn(input)).toBe(output)` before writing `fn`, TDD improves the result."

    3. `## The loop`:
       - **Orient with ONE call** — exact init+spill block for `init tdd`; parse `phase`, `config`.
         State plainly that tdd writes NO SOVEREIGN state — it drives the PROJECT's own test runner
         and the project's own commits (so unlike the other skills, no `state save`/SOVEREIGN
         `commit`). It still orients via init to honor the single-call rule.
       - **RED** — write ONE failing test describing the desired observable behavior at the
         interface. Run the project's own test runner; the test MUST fail for the right reason.
       - **GREEN** — write the MINIMAL code to make it pass. Run the runner; it MUST pass.
       - **REFACTOR** — clean up with tests green; re-run.
       - State the two discipline rules explicitly: test OBSERVABLE BEHAVIOR at the interface (not
         implementation details), and MOCK ONLY at system boundaries (network, clock, filesystem) —
         never internal collaborators.
       - **Stack-agnostic note** — use whatever test command the project already has (e.g.
         `npm test`, `pytest`, `go test`, `cargo test`); do NOT assume a framework, and do NOT do
         real-browser/runtime QA (that is the project's own stack's job — an anti-feature here).

    4. `## Navigation` — recommendation-first footer. Recommended next: continue the loop for the
       next behavior, or `/sentinel` once a unit of work is green (review what was written).
       Alternatives: `/handoff`, `/grill-with-docs`.

    Keep the body thin (~80-120 lines).
  </action>
  <acceptance_criteria>
    - Dir + file `engine/skills/tdd/SKILL.md` exist.
    - Frontmatter has `name: tdd` and `description:`; NO v1 fields (`triggers:`, `works-best-with`, `min-model`, `tokens:`, bare `phase:`).
    - Grep `disable-model-invocation` returns NOTHING.
    - Grep `init tdd` returns at least one hit.
    - Grep `@file:` present.
    - Grep `## Why this matters` present (SKL-06).
    - Grep, case-insensitive, `red` AND `green` AND `refactor` each present (the loop).
    - Grep, case-insensitive, `boundar` present (mock only at system boundaries).
    - Grep, case-insensitive, `stack-agnostic` OR `own test runner` present.
    - Grep `▶ NEXT` (or explicit Navigation footer) present.
    - `sovereign-tools validate skills` passes; `sovereign-tools doctor` clean (this is the 5th and final auto-triggerable fast-lane skill — doctor must still report <=7).
    - File >= 60 lines.
  </acceptance_criteria>
  <verify>
    <automated>F=engine/skills/tdd/SKILL.md; test -f "$F" && grep -q "name: tdd" "$F" && ! grep -qE "^(triggers:|works-best-with|min-model|tokens:|phase:)" "$F" && ! grep -q "disable-model-invocation" "$F" && grep -q "init tdd" "$F" && grep -q "@file:" "$F" && grep -q "## Why this matters" "$F" && grep -qi "red" "$F" && grep -qi "green" "$F" && grep -qi "refactor" "$F" && grep -qi "boundar" "$F" && [ "$(wc -l < "$F")" -ge 60 ] && node engine/bin/sovereign-tools.cjs validate skills && node engine/bin/sovereign-tools.cjs doctor && echo PASS</automated>
  </verify>
  <done>tdd exists as a thin orchestrator: real frontmatter, single init call + spill, RED-GREEN-REFACTOR loop on the project's own runner, behavior-at-interface + mock-at-boundaries discipline, stack-agnostic (no runtime QA), "Why this matters", recommendation-first footer, auto-triggerable; validate + doctor pass.</done>
</task>

</tasks>

<verification>
- All grep acceptance checks pass; `validate skills` + `doctor` pass.
- Dir name matches `install.cjs` FAST_LANE (`tdd`).
- With all five fast-lane skills present, `doctor` confirms <=7 auto-triggerable (5 fast-lane + council disabled).
</verification>

<success_criteria>
SKL-05 satisfied (red-green-refactor loop, behavior-at-interface, stack-agnostic, no runtime QA)
and SKL-06 satisfied for this skill.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fast-lane/04-05-SUMMARY.md`.
</output>
