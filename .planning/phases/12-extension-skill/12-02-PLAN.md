---
phase: 12-extension-skill
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - engine/skills/import-skill/SKILL.md
autonomous: true
requirements: [EXT-01, EXT-02, M3-CC]
must_haves:
  truths:
    - "A user can discover third-party skills (installed via `extension list`, new via an optional `npx skills find <query>` whose `owner/repo@skill`/`skills.sh` targets are parsed leniently) and install via `extension install <source>` — SOVEREIGN wraps `npx skills`, never reimplements the registry."
    - "Before ANY install the skill runs all five vetting gates in order — necessity, conflict, security audit, recommendation-first, logged decision — and surfaces the INSTALL / DON'T-INSTALL / INSTALL-WITH-CAVEATS recommendation first."
    - "The security gate drives on the engine's `extension audit <source>` findings/verdict (which run `scanSkillContent` over the `skills use` content) — never ad-hoc skill-side scanning."
    - "Install runs via `extension install <source>` ONLY on a go decision; a `block` verdict is a hard DON'T-INSTALL."
    - "A decision record (timestamp, source, verdict, audit findings, rationale) is written to `.sovereign/extensions/<date>-<skill>.md`, then `state save` + `commit` via the engine."
    - "The skill is a thin orchestrator: one `init extension` orient (+@file: guard), `## Why this matters`, recommendation-first, nav footer, `disable-model-invocation: true`; it stays user-only (NOT FAST_LANE) so doctor's auto-trigger budget holds at 5."
  artifacts:
    - path: "engine/skills/import-skill/SKILL.md"
      provides: "The /import-skill thin-orchestrator extension protocol skill"
      min_lines: 70
      contains: "disable-model-invocation: true"
  key_links:
    - from: "engine/skills/import-skill/SKILL.md"
      to: "sovereign-tools init extension"
      via: "single orient call with @file: spill guard"
      pattern: "init extension"
    - from: "engine/skills/import-skill/SKILL.md security gate"
      to: "sovereign-tools extension audit"
      via: "drive recommendation on engine findings/verdict"
      pattern: "extension audit"
    - from: "engine/skills/import-skill/SKILL.md go-decision"
      to: "sovereign-tools extension install"
      via: "install only after a go + logged decision"
      pattern: "extension install"
---

<objective>
Hand-author `engine/skills/import-skill/SKILL.md` — the `/import-skill` extension protocol skill. A thin orchestrator that lets a user discover and adopt third-party skills through SOVEREIGN (wrapping the `npx skills` ecosystem, R-003) only after a five-gate vetting layer runs and the decision is logged, so no skill is adopted blind.

Purpose: Pulling arbitrary third-party skills into an agent's context is a direct prompt-injection / over-privilege / exfiltration risk. The skill makes the safe path the default: it materializes the candidate's content (via the engine's `extension audit`, which runs `skills use` then `scanSkillContent`), surfaces a recommendation FIRST, and refuses to install on a `block` verdict — with every decision logged to `.sovereign/extensions/`.

Output: One hand-authored SKILL.md that wraps the (now-corrected) Phase-12 engine commands and never reimplements scanning, the registry, or hashing. Grep- and `validate skills`-verifiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-extension-skill/12-CONTEXT.md

# The format spec + the sibling thin-orchestrator skill to mirror + the v1 design
@engine/references/skill-format.md
@engine/skills/bridge/SKILL.md
@archive/v1/SOVEREIGN_PROJECT.md

<interfaces>
<!-- The engine surface this skill wraps. Use these verbatim — do not reimplement. -->

Engine commands (call via `node "$ENGINE/bin/sovereign-tools.cjs" ...`):
- `init extension` → JSON blob; `paths.extensions_dir` = `.sovereign/extensions`, `paths.state`, `paths.manifest`; `config`; `phase`; `context_injection`. Apply the `@file:` spill guard.
- `extension list` → `{ ok, exitCode, stdout, stderr }` from `skills list` (installed inventory — the conflict gate input).
- `extension audit <source>` → `{ ok, source, findings, verdict }`. Runs `skills use <source>` and `scanSkillContent` over its stdout. verdict ∈ clean|review|block; ok = verdict !== 'block'. (This is the security gate — drive on it.)
- `extension install <source>` → `{ ok, exitCode, stdout, stderr, source }` from `skills add <source> -a claude-code --copy -y`. ok on exit code 0.
- `extension preview <source>` → wraps bare `skills use <source>` (the raw materialize, if you want the content directly; audit already does this internally).
- `state save` and `commit` (delegate ALL state/commit work to the engine — never hand-write).

Discovery (outside the engine, R-003): `npx skills find <query>` is INTERACTIVE; parse `owner/repo@skill` targets and `skills.sh` URLs leniently from its output. Do NOT reinvent search.

The FIVE gates (12-CONTEXT §"The /import-skill skill", order matters):
  1. Necessity — needed for the active tracks/current phase (from the init blob's phase)?
  2. Conflict — vs installed skills (`extension list`) AND vs existing ADRs.
  3. Security audit — `extension audit <source>`; surface findings + verdict; block = hard stop.
  4. Recommendation — INSTALL / DON'T-INSTALL / INSTALL-WITH-CAVEATS, stated FIRST.
  5. Logged decision — `.sovereign/extensions/<date>-<skill>.md` (timestamp, source, verdict, findings, rationale).

Mirror the bridge skill's SHAPE: frontmatter (name/description/disable-model-invocation/argument-hint), `## Why this matters`, `## When to use this`, numbered `## The flow`, a fenced `▶ NEXT` navigation footer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author engine/skills/import-skill/SKILL.md — the thin five-gate extension protocol orchestrator</name>
  <files>engine/skills/import-skill/SKILL.md</files>
  <read_first>
    - engine/references/skill-format.md (the hard rules: one-init-load, required body sections, listing budget, authoring checklist)
    - engine/skills/bridge/SKILL.md (the sibling thin-orchestrator shape to mirror — frontmatter, Why-this-matters, numbered flow, ▶ NEXT footer)
    - .planning/phases/12-extension-skill/12-CONTEXT.md §"The /import-skill skill" (the five gates + flow)
    - archive/v1/SOVEREIGN_PROJECT.md ~lines 445-462 (the v1 /import-skill design)
  </read_first>
  <action>
    Create `engine/skills/import-skill/SKILL.md` as a thin orchestrator mirroring `bridge/SKILL.md`. Wrap the (12-01-corrected) engine commands; never reimplement scanning, search, or the registry.

    **Frontmatter** (real Agent Skills fields only — no v1 fields):
    - `name: import-skill`
    - `description:` lead with the use-case (discover + safely adopt a third-party skill), name the vetting (security audit + logged decision). Stay within the cap.
    - `disable-model-invocation: true` (user-only / side-effecting — keeps the doctor auto-trigger budget at the 5 Fast Lane skills; M3-CC).
    - `argument-hint: "[owner/repo@skill | search query]"`

    **`## Why this matters`** — plain language for someone building their first real product: pulling a stranger's skill into your agent's context is the easiest way to get prompt-injected, over-permissioned, or have your secrets exfiltrated. This skill makes you LOOK before you adopt — and logs the call so future-you knows why this skill is here.

    **`## When to use this`** — when you want a capability you don't have and suspect the `npx skills` ecosystem has it; when evaluating whether an installed skill should stay. Anti-uses: not for authoring your own skills; not a way to bulk-install (every adoption is vetted one at a time).

    **`## The flow`** — a numbered, executable orchestrator. State up front it's a thin orchestrator: the engine owns the `npx skills` shell-out and the content scan; this skill owns the judgment (necessity/conflict/recommendation) and the decision record.

    1. **Orient (one call).** The exact `INIT=$(node "$ENGINE/bin/sovereign-tools.cjs" init extension)` + `if [[ "$INIT" == @file:* ]]; then INIT=$(cat "${INIT#@file:}"); fi` guard. Parse `paths.extensions_dir` (`.sovereign/extensions`), the active phase/tracks, and config. No other orientation reads.

    2. **Discover (optional).** Surface installed skills with `extension list`. If the user is searching, run `npx skills find <query>` and parse `owner/repo@skill` + `skills.sh` URLs leniently from its (interactive) output. Note R-003: SOVEREIGN wraps `npx skills`, never reinvents discovery.

    3. **The five vetting gates — BEFORE any install, recommendation-first.** Name all five explicitly:
       - **Gate 1 — Necessity:** is this needed for the active tracks / current phase (from the init blob)? If not, lean DON'T-INSTALL.
       - **Gate 2 — Conflict:** diff against installed skills (`extension list`) and against existing ADRs (does it contradict a locked decision / duplicate a skill?).
       - **Gate 3 — Security audit:** `node "$ENGINE/bin/sovereign-tools.cjs" extension audit <source>` — this runs `skills use <source>` and `scanSkillContent` over its content. Surface `findings` + `verdict`. **Drive on the engine's findings, never scan ad-hoc.** A `block` verdict is a hard DON'T-INSTALL.
       - **Gate 4 — Recommendation:** state INSTALL / DON'T-INSTALL / INSTALL-WITH-CAVEATS **FIRST**, then the reasoning from gates 1-3.
       - **Gate 5 — Logged decision:** write `.sovereign/extensions/<date>-<skill>.md` — timestamp, source, verdict, audit findings, the recommendation + rationale (and, at Claude's discretion, hashes — bridge owns hashing). This record is written whether or not the skill is installed.

    4. **Install — only on a go.** If (and only if) the decision is INSTALL (or INSTALL-WITH-CAVEATS the user accepts), run `extension install <source>` (the engine's `skills add --copy -a claude-code -y`). Never install on a `block` verdict.

    5. **Persist.** `state save` then `commit` the decision record via `sovereign-tools` (delegate — never hand-write state or branch on commit_docs).

    **Navigation footer** — a fenced `▶ NEXT` block, recommendation-first: the recommended next action + the command, plus alternatives (audit another candidate / list installed / re-run on a search).

    **Length:** ≥70 lines (`wc -l`). Keep the body thin (progressive disclosure — no fat inline reference dumps), but cover all five gates and the flow.

    Do NOT: reimplement `scanSkillContent`, search, or hashing; pass any orientation read other than the single `init extension`; auto-install without the recommendation + logged decision; set it auto-triggerable.
  </action>
  <verify>
    <automated>cd engine && test "$(wc -l < skills/import-skill/SKILL.md)" -ge 70 && grep -q "disable-model-invocation: true" skills/import-skill/SKILL.md && grep -q "init extension" skills/import-skill/SKILL.md && grep -q "extension list" skills/import-skill/SKILL.md && grep -q "extension audit" skills/import-skill/SKILL.md && grep -q "extension install" skills/import-skill/SKILL.md && grep -q ".sovereign/extensions" skills/import-skill/SKILL.md && grep -q "@file:" skills/import-skill/SKILL.md && grep -q "Why this matters" skills/import-skill/SKILL.md && grep -qi "commit" skills/import-skill/SKILL.md</automated>
  </verify>
  <done>SKILL.md exists at engine/skills/import-skill/, ≥70 lines, sets disable-model-invocation: true, orients with a single `init extension` (+@file: guard), names all five gates, wraps `extension list`/`extension audit`/`extension install`, logs to `.sovereign/extensions/`, has `## Why this matters` + recommendation-first `▶ NEXT` footer, and delegates commit to the engine.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the skill against the engine linter + doctor budget</name>
  <files>engine/skills/import-skill/SKILL.md</files>
  <read_first>
    - engine/bin/lib/validate.cjs cmdValidateSkills (the frontmatter linter — what it rejects)
  </read_first>
  <action>
    Run the engine's own gates against the authored file. `validate skills` with an EXPLICIT path to the new skill (per the validate command surface — it accepts paths), and `doctor` to confirm the auto-trigger listing budget still reports at the 5 Fast Lane skills (import-skill is `disable-model-invocation: true`, so it must NOT count toward the auto-trigger budget). If either fails, fix the frontmatter/body in Task 1's file (e.g. a v1 field crept in, description over cap) — do not weaken the gates. This task is the M3-CC compliance check.
  </action>
  <verify>
    <automated>cd engine && node bin/sovereign-tools.cjs validate skills skills/import-skill/SKILL.md && node bin/sovereign-tools.cjs doctor</automated>
  </verify>
  <done>`validate skills skills/import-skill/SKILL.md` passes (clean frontmatter, no v1 fields); `doctor` still reports the auto-trigger listing budget at the 5 Fast Lane skills (import-skill, being user-only, does not lift it).</done>
</task>

</tasks>

<verification>
- `wc -l engine/skills/import-skill/SKILL.md` ≥ 70.
- grep confirms: `disable-model-invocation: true`, `init extension`, `extension list`, `extension audit`, `extension install`, `.sovereign/extensions`, `@file:`, `Why this matters`, `commit`, and the five gate names.
- `node bin/sovereign-tools.cjs validate skills skills/import-skill/SKILL.md` exits clean.
- `node bin/sovereign-tools.cjs doctor` reports the auto-trigger budget at the 5 Fast Lane skills.
</verification>

<success_criteria>
- EXT-01: discovery surfaces `npx skills find` targets + `extension list`; install goes through `extension install` — SOVEREIGN wraps, never reimplements, the registry.
- EXT-02: all five gates run before install (necessity, conflict, security audit on engine findings, recommendation-first, logged decision in `.sovereign/extensions/`); never installs on a `block` verdict.
- M3-CC: thin orchestrator (one `init extension`, Why-this-matters, recommendation-first, nav footer, `disable-model-invocation: true`); `validate skills` clean; doctor budget held at 5.
</success_criteria>

<output>
After completion, create `.planning/phases/12-extension-skill/12-02-SUMMARY.md`
</output>
