---
phase: 07-api-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [engine/skills/api-design/SKILL.md]
autonomous: true
requirements: [ARCH-03, ARCH-08]
must_haves:
  truths:
    - "Running api-design guides the user through a protocol-agnostic contract (REST / GraphQL / gRPC / events), recommendation-first, one decision at a time, referencing the Phase-6 entities"
    - "The skill reads the Phase-6 domain model (.sovereign/docs/ENTITY_MODEL.md when present) and the CONTEXT.md glossary so the contract speaks the project's nouns, and degrades gracefully (suggesting /entity-design) when the model is absent"
    - "The skill produces a living .sovereign/docs/api/API_SPEC.md covering protocol(s), endpoints/messages, auth, versioning, errors, and pagination, and updates it in place on re-run rather than duplicating sections"
    - "Protocol and contract decisions that meet the three-condition gate are OFFERED to adr-log rather than re-implementing ADR recording (the two compose)"
    - "The skill is a thin orchestrator: orients via one `init api-design` call, opens with `## Why this matters`, recommendation-first one-decision-at-a-time, ends with a navigation footer, and sets disable-model-invocation: true"
  artifacts:
    - path: "engine/skills/api-design/SKILL.md"
      provides: "The contract-first, protocol-agnostic API-design skill that reads the entities, walks contract decisions one at a time, writes/updates API_SPEC.md, and composes with adr-log"
      min_lines: 70
      contains: "disable-model-invocation: true"
  key_links:
    - from: "engine/skills/api-design/SKILL.md"
      to: "sovereign-tools init api-design"
      via: "single init orient call with @file: spill guard"
      pattern: "init api-design"
    - from: "engine/skills/api-design/SKILL.md"
      to: ".sovereign/docs/ENTITY_MODEL.md"
      via: "reads the Phase-6 domain model so the contract exposes the real entities (degrades to /entity-design if absent)"
      pattern: "ENTITY_MODEL.md"
    - from: "engine/skills/api-design/SKILL.md"
      to: ".sovereign/docs/api/API_SPEC.md"
      via: "writes/updates the living contract in place (protocol, endpoints/messages, auth, versioning, errors, pagination)"
      pattern: "API_SPEC.md"
    - from: "engine/skills/api-design/SKILL.md"
      to: "adr-log"
      via: "offers protocol/contract decisions that pass the 3-condition gate to adr-log instead of writing ADRs itself (compose, don't duplicate)"
      pattern: "adr-log"
---

<objective>
Hand-author `engine/skills/api-design/SKILL.md` — the contract-first, protocol-agnostic API-design skill that turns the Phase-6 domain model into a living `API_SPEC.md`. It reads `.sovereign/docs/ENTITY_MODEL.md` (degrading to `/entity-design` if absent) and the `CONTEXT.md` glossary so the contract speaks the project's nouns, then walks the contract decisions one at a time, recommendation-first — protocol(s) (REST / GraphQL / gRPC / events), consumers, resources/messages mapped to entities, auth, versioning, error convention, pagination, rate limiting, webhook/event contracts. It writes/updates `.sovereign/docs/api/API_SPEC.md` in place on re-run, and for protocol/contract decisions that pass the three-condition gate it OFFERS `/adr-log` rather than writing ADRs itself. It stays at the contract level — no server/handler code (that's Construction).

Purpose: Establish the living API contract every downstream construction step implements against, over the Phase-6 entities, and prove the adr-log composition once more. Closes ARCH-03 and (for this skill) ARCH-08.
Output: One shipped skill (`engine/skills/api-design/SKILL.md`; `--full` install only, NOT in FAST_LANE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-api-design/07-CONTEXT.md

# The sibling M2 skill to MIRROR (shape, not content) — glossary read, one-at-a-time recommendation-first, offers adr-log, writes a docs file, has a `## <ARTIFACT>.md format` block, nav footer:
@engine/skills/entity-design/SKILL.md
# The skill api-design COMPOSES WITH (offers ADR-worthy protocol/contract choices to it):
@engine/skills/adr-log/SKILL.md
# The spec adr-log implements (api-design references the same 3-condition gate when deciding what to offer):
@engine/references/adr-format.md
# The authoring standard this skill follows:
@engine/references/skill-format.md
# Recommendation-first conversational pattern to mirror; council is the disable-model-invocation/init/nav-footer reference:
@engine/skills/grill-with-docs/SKILL.md
@engine/skills/council/SKILL.md
# The init <skill> default blob this skill consumes:
@engine/bin/lib/init.cjs

<facts>
<!-- Verified against the codebase — author against these, do not re-derive. -->

THE `init api-design` BLOB (default case in engine/bin/lib/init.cjs):
- `api-design` is NOT 'council'/'sovereign-init', so it hits the DEFAULT case in buildInit().
- It returns: `models: {}`, `config` (orientation subset), `phase`, and
  `context_injection: { manifest_path: ".sovereign/MANIFEST.md", glossary_path: ".sovereign/CONTEXT.md" }`,
  `paths: { state: ".sovereign/STATE.md", manifest: ".sovereign/MANIFEST.md" }`, `exists`.
- NOTE: the default-case `context_injection` does NOT carry an ENTITY_MODEL path or relevant_adrs.
  api-design reads `.sovereign/docs/ENTITY_MODEL.md` BY PATH itself (the literal path) — it does not
  expect init to hand it. Probe for the file before reading; if absent, degrade gracefully.
- The glossary gives the project's nouns: read CONTENT of `context_injection.glossary_path`
  (.sovereign/CONTEXT.md) AFTER the init call so the contract uses canonical terms. `exists.glossary`
  tells you if it's there.
- The skill performs ZERO other orientation reads beyond this one `init` call (then reads the
  glossary + ENTITY_MODEL.md content by the paths it knows — that's content reading, not orientation).

THE @file: SPILL GUARD (verbatim):
  INIT=$(node "$ENGINE/bin/sovereign-tools.cjs" init api-design)
  if [[ "$INIT" == @file:* ]]; then INIT=$(cat "${INIT#@file:}"); fi

THE FLOW (per 07-CONTEXT decisions — contract-first, protocol-agnostic):
- Read the Phase-6 domain model (`.sovereign/docs/ENTITY_MODEL.md` if present) and the glossary so the
  contract exposes the real entities by name. If ENTITY_MODEL.md is ABSENT, flag it and suggest running
  `/entity-design` first — designing a contract over an unmodeled domain just relocates the confusion.
- Walk the contract decisions ONE at a time, recommendation-first (recommendation FIRST with reasoning,
  then the user confirms/corrects; ask one thing, wait, continue — never batch). The decisions:
  - PROTOCOL — recommend per use case (REST for external CRUD, GraphQL for flexible reads, gRPC for
    internal service-to-service, events/MQTT for async/streaming); can be more than one.
  - CONSUMERS — frontend / mobile / third-party / another service (shapes the contract).
  - RESOURCES / MESSAGES — mapped to the entities from ENTITY_MODEL.md.
  - AUTH — per endpoint/message.
  - VERSIONING strategy; ERROR convention (a single error shape); PAGINATION; RATE LIMITING;
    webhook/event contracts where relevant.
- COMPOSE, DON'T DUPLICATE: protocol + contract decisions that pass the three-condition gate
  (hard-to-reverse + surprising + real trade-off) are OFFERED to `/adr-log` — point the user to it or
  describe the decision for it. api-design does NOT number or write ADRs itself; adr-log is the single
  owner of ADR recording.
- Stay at the CONTRACT level — no server/handler/route code, no generated OpenAPI/proto files (that's
  Construction; auto-generation is explicitly DEFERRED per 07-CONTEXT).

API_SPEC.md OUTPUT (write/update `.sovereign/docs/api/API_SPEC.md`):
- A header block (protocol(s), auth scheme, versioning policy, the standard error format, pagination
  convention), a per-endpoint/per-message section (purpose, request/response shape, errors, rate
  limit), an event/webhook contracts section, a versioning section. The `.sovereign/docs/api/` dir
  ships in the scaffolded `.sovereign/` tree.
- UPDATE IN PLACE on re-run — match on endpoint/section headings; do NOT duplicate sections (Claude's
  discretion on exactly how it detects "update in place" — match on headings is the intent).
- Exact layout is Claude's discretion (07-CONTEXT): the header block + per-endpoint/message blocks +
  an "Event contracts" + "Versioning" + "Error format" section is the intent. Mirror entity-design's
  `## API_SPEC.md format` block to show the intended shape.

ARCH-08 THIN-ORCHESTRATOR SHAPE (per skill-format.md; identical to the other M2 skills):
- Real Agent Skills frontmatter ONLY: `name`, `description` (lead with the use-case),
  `disable-model-invocation: true`. NO v1 fields (triggers/works-best-with/min-model/tokens/bare phase).
- disable-model-invocation: true → user-only (/api-design), side-effecting, OFF the auto-trigger budget.
- One `init api-design` orient call (+ @file: spill); reads content only from paths it knows.
- Required body sections in order: `## Why this matters` → `## When to use this` (with anti-uses) →
  the numbered flow → an `## API_SPEC.md format` block → a recommendation-first navigation footer.
- All state/commit delegated to `sovereign-tools` (state save, commit) — never hand-write state.

DEPENDENCY: conceptually builds on Phase 6 (the API exposes the entities; offers choices to adr-log),
but this is a FRESH wave-1 plan — both adr-log and entity-design already exist (Phase 6 complete), so
there is no in-phase plan dependency. api-design references `/adr-log` and `/entity-design` BY NAME.

INSTALL / BUDGET: api-design is `--full` only, NOT in install.cjs FAST_LANE — must NOT be
auto-triggerable. After this phase, `doctor` must still report the auto-trigger budget at the 5
Fast Lane skills (council + adr-log + entity-design + api-design all disabled).

VERIFY: `validate skills` is a NO-OP without an explicit path. The automated verify MUST pass:
  node engine/bin/sovereign-tools.cjs validate skills engine/skills/api-design/SKILL.md
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author engine/skills/api-design/SKILL.md</name>
  <files>engine/skills/api-design/SKILL.md</files>
  <read_first>
    - engine/skills/entity-design/SKILL.md — the sibling M2 skill to MIRROR: its shape, its `## Why this matters` / `## When to use this` / numbered flow / `## ENTITY_MODEL.md format` / `## Navigation` ordering, its glossary-read pattern, its one-piece-at-a-time recommendation-first walking, its offer-adr-log composition, and its engine-delegated state save/commit. api-design is the same shape over the API contract.
    - engine/skills/adr-log/SKILL.md — the skill api-design OFFERS for ADR-worthy protocol/contract choices; reference it by name (`/adr-log`), do NOT re-implement its numbering/gate/write.
    - engine/references/adr-format.md — the same three-condition gate api-design uses to decide WHICH protocol/contract choices are worth offering to adr-log (hard-to-reverse + surprising + real trade-off).
    - engine/references/skill-format.md — the thin-orchestrator standard (real frontmatter, single-init-load, required body sections, dropped v1 fields, listing budget).
    - engine/skills/grill-with-docs/SKILL.md — mirror the conversational one-question-at-a-time, recommendation-first shape; the offer-ADRs-sparingly composition; delegate writes to the engine.
    - engine/skills/council/SKILL.md — the `disable-model-invocation: true` frontmatter + HTML comment + init orient + `▶ NEXT` nav-footer pattern.
    - engine/bin/lib/init.cjs (default case + contextInjection) — confirm `init api-design` returns context_injection.glossary_path (.sovereign/CONTEXT.md) and paths.state, and does NOT hand an ENTITY_MODEL path (api-design reads .sovereign/docs/ENTITY_MODEL.md by path itself).
  </read_first>
  <action>
    Hand-author `engine/skills/api-design/SKILL.md`. Use the `<facts>` block as the source of truth; mirror entity-design's overall shape (it is the closest sibling) and grill-with-docs/council for the conversational + frontmatter + nav patterns. Sections in order:

    FRONTMATTER (real Agent Skills fields only):
    ```yaml
    ---
    name: api-design
    description: Design your API contract-first and protocol-agnostic — protocol (REST / GraphQL / gRPC / events), resources/messages mapped to your entities, auth, versioning, errors, and pagination — recommendation-first, one decision at a time. Records a living API_SPEC.md over your domain model and offers hard-to-reverse choices to /adr-log. Use after entity-design, before building the API.
    disable-model-invocation: true
    argument-hint: "\"<API surface or feature>\""
    ---
    ```
    Add an HTML comment below it (mirroring council/entity-design): disable-model-invocation: true keeps api-design user-invoked (/api-design) and side-effecting (writes API_SPEC.md + commits), OFF the listing auto-trigger budget — it is `--full` only, not a Fast Lane skill.

    `## Why this matters` — plain language: the API contract is the promise everything else holds you to — the frontend, the mobile app, the third party, the next service. Decided ad hoc, it leaks the database's shape, versions in inconsistent ways, and returns a different error format on every endpoint. Designing it contract-first — before any handler code — and grounding it in the entities you already modeled is the cheapest place to get the shape right. This skill walks the contract one decision at a time and records it so construction has one map to build against.

    `## When to use this` — Use it AFTER `entity-design` (the API exposes those entities) and BEFORE building the API, when adding a new API surface, or when the contract has drifted from the model. Anti-uses ("Don't use it for…"): writing server/route/handler code or generating OpenAPI/proto files (that's Construction — this stays at the contract level); modeling the domain (use `/entity-design` first); recording a single locked decision (use `/adr-log`).

    `## The flow` — a thin orchestrator: orient once, read the model + glossary, walk the contract one decision at a time, offer ADRs sparingly, delegate writes. Numbered steps:
      1 — Orient (one call). Show the exact bash with the @file: spill guard for `init api-design`. Parse `context_injection.glossary_path` (.sovereign/CONTEXT.md) and `paths.state`. The skill performs no other orientation reads.
      2 — Ground in the model + glossary. Read `.sovereign/docs/ENTITY_MODEL.md` (by path) — these entities are what the contract exposes. If it is ABSENT, STOP and flag it: recommend `/entity-design` first (a contract over an unmodeled domain just relocates the confusion). Read the glossary content for the project's canonical nouns.
      3 — Walk the contract one decision at a time, recommendation-first. For each: give your recommended answer FIRST with reasoning, then the user confirms or corrects. Ask ONE thing, wait, continue. Never batch. The decisions, in order: PROTOCOL(s) (recommend per use case — REST for external CRUD, GraphQL for flexible reads, gRPC for internal service-to-service, events/MQTT for async/streaming; can be more than one) → CONSUMERS (frontend / mobile / third-party / another service) → RESOURCES/MESSAGES mapped to the ENTITY_MODEL entities → AUTH per endpoint/message → VERSIONING strategy → ERROR convention (one shape) → PAGINATION → RATE LIMITING → webhook/event contracts where relevant.
      4 — Offer ADRs sparingly (compose, don't duplicate). When a protocol/contract choice is hard to reverse (protocol selection, versioning strategy, sync-vs-event integration shape) AND meets the three-condition gate (hard-to-reverse + surprising + real trade-off), OFFER `/adr-log` — point the user to it or describe the decision for it. Do NOT number or write ADRs here; adr-log owns that.
      5 — Record the contract. Write/update `.sovereign/docs/api/API_SPEC.md`: a header block (protocol(s), auth, versioning, standard error format, pagination), per-endpoint/per-message sections (purpose, request/response shape, errors, rate limit), an event/webhook contracts section, and a versioning section. Update IN PLACE on re-run — match on endpoint/section headings; don't duplicate.
      6 — Persist (delegate to the engine). `node "$ENGINE/bin/sovereign-tools.cjs" state save`, then `node "$ENGINE/bin/sovereign-tools.cjs" commit "api-design: <short summary>" --files .sovereign/docs/api/API_SPEC.md <paths.state> <paths.manifest>`. commit is gated by the engine on commit_docs + gitignore and sanitizes the message — the skill does NOT branch on those itself.

    Optionally include a short recommendation-first example line (mirroring grill-with-docs/entity-design) showing one decision proposed (e.g. "REST for the public surface, recommended because the consumers are a web app + third parties") with an ADR offer.

    `## API_SPEC.md format` — mirror entity-design's `## ENTITY_MODEL.md format` block: a fenced markdown example showing the intended shape — a header block (Protocol(s) / Auth / Versioning / Error format / Pagination), per-endpoint blocks (e.g. `### POST /orders` with purpose, request, response, errors, rate limit), an `## Event contracts` section, and a `## Versioning` section. End with a line: keep it at the contract level — no handler code, no generated OpenAPI/proto.

    `## Navigation` — a recommendation-first `▶ NEXT` footer: after the contract is recorded, recommend the next architecture step (`/stack-select` — choose the stack that implements this contract) as the primary next action, with `/adr-log` (record an outstanding protocol/contract decision) and `/entity-design` (revise the model if the contract surfaced a gap) as alternatives. Show where the spec landed (`.sovereign/docs/api/API_SPEC.md`).

    Keep it tight (~80-150 lines), prose tone matching entity-design/grill-with-docs/council. Authored prose verified by grep + `validate skills`.
  </action>
  <verify>
    <automated>test -f engine/skills/api-design/SKILL.md && grep -q "disable-model-invocation: true" engine/skills/api-design/SKILL.md && grep -q "init api-design" engine/skills/api-design/SKILL.md && grep -q "API_SPEC.md" engine/skills/api-design/SKILL.md && grep -q "ENTITY_MODEL.md" engine/skills/api-design/SKILL.md && grep -qi "REST" engine/skills/api-design/SKILL.md && grep -q "adr-log" engine/skills/api-design/SKILL.md && grep -q "@file:" engine/skills/api-design/SKILL.md && grep -q "Why this matters" engine/skills/api-design/SKILL.md && grep -q "commit" engine/skills/api-design/SKILL.md && grep -qv "min-model:" engine/skills/api-design/SKILL.md && node engine/bin/sovereign-tools.cjs validate skills engine/skills/api-design/SKILL.md && echo PASS</automated>
  </verify>
  <done>
    `engine/skills/api-design/SKILL.md` exists (≥70 lines) with real Agent Skills frontmatter (`name: api-design`, `description`, `disable-model-invocation: true`, no v1 fields), and contains: a single `init api-design` orient call with the `@file:` spill guard; reading `.sovereign/docs/ENTITY_MODEL.md` (degrade → `/entity-design` if absent) + the CONTEXT.md glossary so the contract speaks the project's nouns; one-decision-at-a-time recommendation-first walking of protocol(s) (REST/GraphQL/gRPC/events) / consumers / resources-messages / auth / versioning / errors / pagination / rate limiting / event contracts; OFFERING `/adr-log` for hard-to-reverse protocol/contract choices (no re-implemented ADR numbering); writing/updating `.sovereign/docs/api/API_SPEC.md` in place; an `## API_SPEC.md format` block; engine-delegated `state save` + `commit`; `## Why this matters`; and a recommendation-first navigation footer. `node engine/bin/sovereign-tools.cjs validate skills engine/skills/api-design/SKILL.md` passes.
  </done>
</task>

</tasks>

<verification>
- `engine/skills/api-design/SKILL.md` exists and ships (under `engine/skills/`) and is NOT added to install.cjs FAST_LANE (`--full` only).
- `validate skills <explicit path>` passes (path is mandatory — bare `validate skills` is a no-op, per the M1/Phase-6 verifiers).
- Grep proves: `disable-model-invocation: true`; one `init api-design` orient + `@file:` spill; reads `ENTITY_MODEL.md`; writes `API_SPEC.md`; a protocol mention (`REST`); OFFERS `adr-log` (composition, not re-implementation); engine-delegated `commit`; `## Why this matters`; nav footer; no v1 frontmatter (`min-model:`).
- Composition check: api-design references `/adr-log` by name and does NOT contain its own `NNNN`-numbering/ADR-write logic (adr-log is the single owner — Phase 6).
- Contract-level check: api-design designs the CONTRACT only — no server/handler/route code and no generated OpenAPI/proto (auto-generation is DEFERRED per 07-CONTEXT).
- `doctor` still reports the auto-trigger budget at the 5 Fast Lane skills (api-design is disable-model-invocation, so off the count; council + adr-log + entity-design + api-design all disabled).
</verification>

<success_criteria>
ARCH-03 satisfied: running `api-design` guides the user through a protocol-agnostic contract (REST / GraphQL / gRPC / events), recommendation-first, one decision at a time, referencing the Phase-6 entities; it produces a living `.sovereign/docs/api/API_SPEC.md` covering endpoints/messages, auth, versioning, errors, and pagination, updates it in place on re-run, and offers protocol/contract decisions that meet the three-condition gate to `adr-log` rather than re-implementing decision recording. ARCH-08 satisfied for this skill: thin orchestrator (single `init` orient, recommendation-first one-decision-at-a-time, `## Why this matters`, nav footer), `disable-model-invocation: true`, `validate skills` passes, doctor budget held at the 5 Fast Lane skills.
</success_criteria>

<output>
After completion, create `.planning/phases/07-api-design/07-01-SUMMARY.md`
</output>
