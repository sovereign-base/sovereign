---
phase: 05-conventions-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/skills/README.md
  - docs/skills/council.md
  - docs/skills/ubiquitous-language.md
  - docs/skills/grill-with-docs.md
  - docs/skills/handoff.md
  - docs/skills/sentinel.md
  - docs/skills/tdd.md
autonomous: true
requirements: [CONV-04]
must_haves:
  truths:
    - "Each M1 skill (council + the 5 fast-lane) has one documentation page a new user can read to understand what it does, when to use it, and what it touches"
    - "An index page lists all six skills and links to their pages"
    - "Docs live at repo-root docs/ (documentation surface), NOT in the package files allowlist (not installed into user projects)"
  artifacts:
    - path: "docs/skills/README.md"
      provides: "Index of the six M1 skill doc pages with one-line summaries + links"
      min_lines: 10
      contains: "council"
    - path: "docs/skills/sentinel.md"
      provides: "Per-skill doc page for sentinel"
      min_lines: 20
      contains: "sentinel"
  key_links:
    - from: "docs/skills/README.md"
      to: "docs/skills/council.md"
      via: "index links to each skill page"
      pattern: "council.md"
---

<objective>
Author one documentation page per M1 skill (CONV-04) under repo-root `docs/skills/`, plus an index. Distilled from the six shipped skills — a human-facing surface separate from the SKILL.md files the agent loads. Docs are NOT shipped in the package (repo documentation only, per §7).

Output: `docs/skills/README.md` (index) + six pages: `council.md`, `ubiquitous-language.md`, `grill-with-docs.md`, `handoff.md`, `sentinel.md`, `tdd.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-conventions-docs/05-CONTEXT.md
@engine/skills/council/SKILL.md
@engine/skills/ubiquitous-language/SKILL.md
@engine/skills/grill-with-docs/SKILL.md
@engine/skills/handoff/SKILL.md
@engine/skills/sentinel/SKILL.md
@engine/skills/tdd/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the six per-skill doc pages</name>
  <read_first>
    - engine/skills/{council,ubiquitous-language,grill-with-docs,handoff,sentinel,tdd}/SKILL.md — the source of truth for each page (distill, don't duplicate the full body).
    - .planning/phases/05-conventions-docs/05-CONTEXT.md (the page structure under CONV-04).
  </read_first>
  <action>
    For each of the six skills, author `docs/skills/<name>.md` with these sections (keep tight, ~30-60 lines): a one-line **hook** (the "Why this matters" in a sentence); **## What it does**; **## When to use it** (and when NOT to); **## How it works** (the flow in brief — orient via one `init` call, the key steps); **## Outputs** (which `.sovereign/` files it touches, if any); **## Navigation** (what to run before/after). For `council`, note `--standard` (express/deep deferred) and the binding verdict. For `sentinel`, note the native tier + the four checks + verdict. For `tdd`, note stack-agnostic + behavior-at-interface.
  </action>
  <acceptance_criteria>
    - All six files exist under docs/skills/ and each names its skill.
    - Each is >= 20 lines.
  </acceptance_criteria>
  <verify>
    <automated>for s in council ubiquitous-language grill-with-docs handoff sentinel tdd; do test -f "docs/skills/$s.md" && grep -q "$s" "docs/skills/$s.md" && [ "$(wc -l < docs/skills/$s.md)" -ge 20 ] || { echo "FAIL $s"; exit 1; }; done && echo PASS</automated>
  </verify>
  <done>Six per-skill doc pages exist, each names its skill and is >= 20 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Author the docs/skills index</name>
  <read_first>
    - docs/skills/*.md (the six pages from Task 1)
  </read_first>
  <action>
    Author `docs/skills/README.md`: a short intro (these are the M1 skills SOVEREIGN ships) + a table or list of all six skills with a one-line summary and a relative link to each page (`council.md`, etc.). Group as: Council, then Fast Lane (the five). Note `npx sovereign init --quick` installs the Fast Lane; `--full` adds council.
  </action>
  <acceptance_criteria>
    - docs/skills/README.md exists, links to all six pages (council.md, ubiquitous-language.md, grill-with-docs.md, handoff.md, sentinel.md, tdd.md).
  </acceptance_criteria>
  <verify>
    <automated>F=docs/skills/README.md; test -f "$F" && for s in council ubiquitous-language grill-with-docs handoff sentinel tdd; do grep -q "$s.md" "$F" || { echo "FAIL link $s"; exit 1; }; done && echo PASS</automated>
  </verify>
  <done>The index exists and links to all six skill pages.</done>
</task>

</tasks>

<verification>
- Six per-skill doc pages + index under docs/skills/.
- Each page names its skill, >= 20 lines; index links all six.
- CONV-04 satisfied.
</verification>

<success_criteria>
- A new user can navigate docs/skills/ to understand every M1 skill.
- Docs are repo-root only (not in the package files allowlist).
</success_criteria>

<output>
After completion, create `.planning/phases/05-conventions-docs/05-03-SUMMARY.md`.
</output>
