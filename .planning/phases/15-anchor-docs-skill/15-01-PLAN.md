---
phase: 15-anchor-docs-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/ADR-004-anchor-content-copyright.md
  - engine/skills/anchor-docs/SKILL.md
autonomous: true
requirements: [ANCHOR-01, M4-CC]

must_haves:
  truths:
    - "A user invoking anchor-docs ingests external docs by delegating to the engine `anchor add` (URL-by-default, content opt-in behind a copyright warning citing ADR-004) — storage is NOT reimplemented in the skill."
    - "The skill orients with a single `init anchor-docs` call (no ten-file reads) and follows the core-tier thin-orchestrator shape: `## Why this matters`, recommendation-first flow, navigation footer."
    - "The skill sets `disable-model-invocation: true`, so `doctor` (run from engine/) holds the auto-trigger budget at the 5 Fast Lane skills and `validate skills` passes for it."
  artifacts:
    - path: "docs/adr/ADR-004-anchor-content-copyright.md"
      provides: "The URL-by-default / content-opt-in / copyright-warning policy ANCHOR-01 references"
      contains: "# ADR-004:"
    - path: "engine/skills/anchor-docs/SKILL.md"
      provides: "The anchor-docs thin-orchestrator skill wrapping the Phase 14 engine `anchor` command"
      contains: "disable-model-invocation: true"
  key_links:
    - from: "engine/skills/anchor-docs/SKILL.md"
      to: "engine `anchor` command"
      via: "literal installed engine path"
      pattern: ".claude/sovereign-engine/sovereign-tools.cjs"
    - from: "engine/skills/anchor-docs/SKILL.md"
      to: "docs/adr/ADR-004-anchor-content-copyright.md"
      via: "citation at the content-opt-in decision point"
      pattern: "ADR-004"
---

<objective>
Author the hand-authored `anchor-docs` skill (a core-tier thin orchestrator wrapping the
Phase 14 engine `anchor` command) plus the policy ADR it cites, then prove both pass the
phase gates.

Purpose: Let a user anchor implementation to *current* external documentation (URL by
default, content opt-in behind a copyright warning) and surface which anchors have gone
stale — without the skill reimplementing any storage or staleness math (the engine owns that).

Output:
- `docs/adr/ADR-004-anchor-content-copyright.md` — the URL-by-default / content-opt-in
  copyright policy ANCHOR-01 references (currently a dangling reference).
- `engine/skills/anchor-docs/SKILL.md` — the skill, mirroring `engine/skills/bridge/SKILL.md`
  exactly in shape (real Agent-Skills frontmatter only; `disable-model-invocation: true`;
  `## Why this matters`; one-call orient; literal `.claude/sovereign-engine/sovereign-tools.cjs`
  path; navigation footer).
- Gates green: doctor holds `auto_count: 5`; `validate skills` passes; engine suite stays 164 green.

This is a HAND-AUTHORED skill phase (R-004): the tasks write markdown (a SKILL.md, an ADR),
with grep/command-checkable acceptance criteria. NO engine changes — the `anchor` command
shipped in Phase 14; this phase only WRAPS it. If a genuine engine gap is found, flag it,
do not silently patch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-anchor-docs-skill/15-CONTEXT.md
@.planning/phases/15-anchor-docs-skill/15-RESEARCH.md
@.planning/phases/15-anchor-docs-skill/15-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

# The literal template to mirror — diff anchor-docs against this:
@engine/skills/bridge/SKILL.md
# Second gated/warned user-invoked example (parallel to the copyright warning step):
@engine/skills/import-skill/SKILL.md
# The authoring contract (sections, frontmatter rules, one-call rule, >=70 line gate, footer):
@engine/references/skill-format.md
# The engine surface this skill DRIVES (read for exact flags/JSON — DO NOT modify):
@engine/bin/lib/anchor.cjs

<interfaces>
<!-- Contracts the executor needs — extracted from the shipped Phase 14 engine. Use these directly; no codebase exploration needed. -->

ENGINE PATH (literal, in EVERY bash block — never $ENGINE, never engine/bin/...):
  .claude/sovereign-engine/sovereign-tools.cjs

ORIENT — `init anchor-docs` returns (init.cjs case):
  { models, config, phase, context_injection,
    paths: { external_docs_dir: ".sovereign/external-docs", state: ".sovereign/STATE.md", manifest: ".sovereign/MANIFEST.md" },
    exists: {...} }
  The skill parses paths.external_docs_dir (where anchors live) — the only path it needs.

`anchor add` flags (anchor.cjs cmdAnchorAdd):
  --id        REQUIRED — slugified (lowercase, [^a-z0-9-] -> -); pass any string
  --source    REQUIRED — the URL (else: "error: anchor add: --source is required")
  --version   optional — defaults to literal "unknown"
  --date-retrieved  optional — defaults to today (UTC YYYY-MM-DD)
  --re-verify-by    optional — defaults to date-retrieved + 90 days
  --content   opt-in — @<file> | - (stdin) | literal string; ABSENT -> URL-only, content-stored:false
              (NOTE: --store-content boolean is parsed-but-unused; the opt-in is --content WITH a value;
               never pass a bare --content with no value — it parses to null = no opt-in)
  Writes .sovereign/external-docs/<slug>.md with headers source/version/date-retrieved/re-verify-by/content-stored.
  Emits JSON: { id, source, version, date_retrieved, re_verify_by, content_stored, path }

`anchor list` — no flags; greenfield-safe -> []. Emits array of
  { id, source, version, date_retrieved, re_verify_by, content_stored, stale }

`anchor check` — no flags; greenfield-safe -> { anchors: [], stale_count: 0 }.
  Emits { anchors: [...], stale_count: N }; stale = re_verify_by < today (strict; equal-to-today NOT stale)

GATE CAVEAT (CRITICAL — from research, confirmed live): plain `doctor` / `validate skills`
from repo root walk `.claude/skills` + `<cwd>/skills`, NOT `engine/skills/`, and report
0 skills (pass vacuously). ALL gate commands MUST run from engine/:
  cd engine && node bin/sovereign-tools.cjs doctor
  cd engine && node bin/sovereign-tools.cjs validate skills skills/anchor-docs/SKILL.md
Baseline today: doctor -> total_skills 16, auto_count 5, disabled_count 11.
Adding anchor-docs with disable-model-invocation:true -> total 17, auto_count 5, disabled_count 12.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-004 (the copyright policy the skill cites)</name>
  <files>docs/adr/ADR-004-anchor-content-copyright.md</files>
  <read_first>
    - docs/adr/ADR-011-drop-v1-frontmatter.md — the repo ADR format to mirror EXACTLY: `# ADR-NNN: Title`, then `**Status:** Accepted` + `**Date:**`, then `## Context` / `## Decision` / `## Consequences`. (Do NOT use the minimal `.sovereign/` runtime adr-format.md form — pitfall 4.)
    - 15-RESEARCH.md "ADR-004 — exact filename, format, and content" — the full drafted content to use.
    - engine/skills/anchor-docs/SKILL.md does NOT exist yet — write this ADR FIRST so Task 2 can cite a real file.
  </read_first>
  <action>
    Create `docs/adr/ADR-004-anchor-content-copyright.md` in the repo ADR format (mirror
    ADR-011 / ADR-014, NOT the runtime minimal form). Use the content drafted in
    15-RESEARCH.md (adjust prose only for accuracy):

    - Title line: `# ADR-004: External-doc anchors store the URL by default; full content is opt-in behind a copyright warning`
    - `**Status:** Accepted` and `**Date:** 2026-06-09`
    - `## Context` — anchor-docs anchors implementation to current external docs the agent's
      training may be stale on; the tempting move is to commit full doc text, but third-party
      docs are copyrighted and many licenses prohibit redistribution; the engine (anchor.cjs)
      is deliberately content-agnostic and never fetches a URL itself, so the copyright
      judgment cannot live in the engine.
    - `## Decision` — anchors store the source URL + metadata (`source`, `version`,
      `date-retrieved`, `re-verify-by`) BY DEFAULT; full content stored ONLY on explicit
      opt-in (`anchor add --content @file|-`), and the `anchor-docs` skill MUST surface a
      copyright/licensing warning before that opt-in; the engine stays content-agnostic
      (stores what it's handed); the warning + URL-by-default recommendation live at the
      SKILL layer.
    - `## Consequences` — (+) safe path is the default (URLs non-infringing + re-fetchable,
      the whole anti-staleness point); (+) engine stays zero-dep/offline/copyright-free;
      (+) full content still possible for own/permissively-licensed material as a deliberate
      warned choice; (−) default anchors depend on the URL still resolving at re-fetch
      (mitigated by version + date-retrieved metadata + the re-verify-by staleness check).

    NOTE: ADR-004 fills a gap in the docs/adr/ numbering (001-008 are not all present) —
    this is intended; it resolves the dangling reference from ANCHOR-01.
  </action>
  <acceptance_criteria>
    - G11: file exists in repo ADR format with `# ADR-004:`, `## Context`, `## Decision`, `## Consequences`, `**Status:** Accepted`.
    - Mirrors ADR-011/ADR-014 structure (NOT the runtime minimal form).
    - Decision states URL-by-default + content-opt-in + skill-layer copyright warning + content-agnostic engine.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/adr/ADR-004-anchor-content-copyright.md && grep -q '^# ADR-004:' docs/adr/ADR-004-anchor-content-copyright.md && grep -q '^## Context' docs/adr/ADR-004-anchor-content-copyright.md && grep -q '^## Decision' docs/adr/ADR-004-anchor-content-copyright.md && grep -q '^## Consequences' docs/adr/ADR-004-anchor-content-copyright.md && grep -q '^\*\*Status:\*\* Accepted' docs/adr/ADR-004-anchor-content-copyright.md && echo G11-PASS</automated>
  </verify>
  <done>ADR-004 exists in repo ADR format with Context/Decision/Consequences; records URL-by-default / content-opt-in / skill-layer copyright warning policy. (G11)</done>
</task>

<task type="auto">
  <name>Task 2: Author the anchor-docs SKILL.md (mirror bridge exactly)</name>
  <files>engine/skills/anchor-docs/SKILL.md</files>
  <read_first>
    - engine/skills/bridge/SKILL.md — the LITERAL template. anchor-docs must reproduce the same skeleton: frontmatter -> `## Why this matters` -> `## When to use this` (+ a "Don't" anti-use) -> `## The flow` (opens "A **thin orchestrator** — ..." then numbered steps) -> an optional format/decision block -> `## Navigation` footer with a fenced `▶ NEXT`.
    - engine/skills/import-skill/SKILL.md — the parallel for a recommendation-first warning/vetting step (the copyright warning mirrors this).
    - engine/references/skill-format.md — required sections, one-call orient rule, >=70 line gate, no v1 frontmatter.
    - 15-RESEARCH.md — "Recommended frontmatter (literal block)", "Exact bash snippets", "The copyright warning (placement + text)", "Recommended flow (numbered)". Use these literally.
    - <interfaces> block above — exact engine path + `anchor add|list|check` flags/JSON.
  </read_first>
  <action>
    Create `engine/skills/anchor-docs/SKILL.md`, mirroring bridge structurally (different
    domain). Use these CONCRETE values:

    FRONTMATTER (literal — real Agent-Skills fields ONLY, no v1 fields):
    ```yaml
    ---
    name: anchor-docs
    description: Anchor your implementation to current external documentation — capture the URL (and metadata) of the payment-gateway, SDK, or regional/gov-API docs you're building against, and see which anchors have gone stale so you re-check before trusting stale knowledge. Use when integrating against docs your training may be behind on.
    disable-model-invocation: true
    argument-hint: "[doc URL or anchor id]"
    ---
    ```

    `## Why this matters` — plain language: an agent's training goes stale on fast-moving
    external docs (payment gateways, SDKs, regional/gov APIs); building against half-remembered
    versions ships wrong endpoints/fields/auth. Anchoring captures the *current* source so you
    (and future you) can re-check against the real doc, and staleness surfacing tells you when
    an anchor is past its re-verify date so you re-check before trusting it.

    `## When to use this` — integrating against external docs your training may be behind on;
    before asserting a version/endpoint/config you're unsure of. Include a **Don't** line:
    don't use it as a general doc store/wiki, and don't paste copyrighted third-party content
    by default (URL-by-default is the safe path — see Step 4).

    `## The flow` — open with "A **thin orchestrator** — the engine (`anchor.cjs`) owns
    storage, slug sanitization, default dates, and staleness math; this skill owns *which*
    docs to anchor, the re-verify cadence recommendation, and the copyright-warning UX."
    Then numbered steps (mirror the bridge order):

    **1 — Orient (one call).**
    ```bash
    INIT=$(node ".claude/sovereign-engine/sovereign-tools.cjs" init anchor-docs)
    if [[ "$INIT" == @file:* ]]; then INIT=$(cat "${INIT#@file:}"); fi
    ```
    Parse `paths.external_docs_dir` (`.sovereign/external-docs`). Zero other orientation reads.

    **2 — Surface staleness FIRST.**
    ```bash
    node ".claude/sovereign-engine/sovereign-tools.cjs" anchor check
    ```
    If `stale_count > 0`, name the stale anchor ids and tell the user to re-check those
    before relying on them (the "know which anchors have gone stale" half of the goal).

    **3 — Gather the doc(s) to anchor** (the judgment): the `--id` slug from the source, the
    `--source` URL (required), an optional `--version` label (omit -> engine defaults to
    "unknown"), and a `--re-verify-by` cadence recommendation by doc-type (fast-moving
    payment/SDK APIs shorter; stable gov specs longer; omit -> engine defaults to +90 days).

    **4 — URL-by-default vs content (the copyright decision).** Recommendation-first, then the
    warning, then cite ADR-004. Use this concrete wording (from research):
    > **Recommendation: store the URL only (the default).** SOVEREIGN anchors store the source
    > URL + metadata so you can re-fetch the *current* doc on demand — the safe, non-infringing path.
    >
    > **Copyright warning — only if you choose `--content`:** Pasting a third-party doc's full
    > text into this repo commits someone else's copyrighted content into your version control.
    > Many docs (payment gateways, SDKs, gov/regional APIs) are licensed in ways that prohibit
    > redistribution. Store full content **only** when you've confirmed the license permits it
    > (or it's your own/public-domain material), and treat it as a deliberate, informed choice.
    > When in doubt, keep the URL and re-fetch.

    Cite ADR-004 here: "see ADR-004 for the URL-by-default / content-opt-in policy".

    **5 — anchor add** (delegate storage). URL-only (safe default):
    ```bash
    node ".claude/sovereign-engine/sovereign-tools.cjs" anchor add \
      --id stripe-payment-intents \
      --source "https://docs.stripe.com/api/payment_intents" \
      --version "2024-06-20" \
      --re-verify-by 2026-09-07
    ```
    With content (ONLY after the warning + explicit opt-in; never a bare `--content`):
    ```bash
    node ".claude/sovereign-engine/sovereign-tools.cjs" anchor add \
      --id stripe-payment-intents \
      --source "https://docs.stripe.com/api/payment_intents" \
      --content @./fetched-doc.md      # or: --content -   (stdin)
    ```

    **6 — Confirm.**
    ```bash
    node ".claude/sovereign-engine/sovereign-tools.cjs" anchor list
    ```
    Show the new anchor + the stale summary.

    **7 — Persist + navigate.** Delegate `state save` / `commit` to `sovereign-tools`
    (never hand-write state). Then the navigation footer.

    `## Navigation` — a fenced `▶ NEXT` block (recommendation-first), e.g.: anchor written ->
    `.sovereign/external-docs/<slug>.md`; re-run `anchor check` before relying on anchored
    docs; point FORWARD to `verify-self` (Phase 16) as the companion ("unsure about an API
    detail? `verify-self`") and note `sentinel` scans the `SOVEREIGN:UNVERIFIED` markers
    verify-self emits — but anchor-docs itself does NOT emit those markers.

    HARD CONSTRAINTS:
    - Body model-agnostic (steps = "run init anchor-docs, then the engine commands").
    - Every engine call uses the literal `.claude/sovereign-engine/sovereign-tools.cjs` — NEVER `$ENGINE`, never `engine/bin/...`.
    - Delegate ALL storage to `anchor add|list|check`; NEVER write `.sovereign/external-docs/*.md` directly.
    - No v1 frontmatter fields (`triggers`/`works-best-with`/`min-model`/`tokens`/bare `phase`).
    - Do NOT emit `SOVEREIGN:UNVERIFIED` markers (that's Phase 16).
    - `wc -l` >= 70.
  </action>
  <acceptance_criteria>
    - G1: `engine/skills/anchor-docs/SKILL.md` exists.
    - G2: frontmatter line `disable-model-invocation: true` present (exact).
    - G3: has `^## Why this matters`.
    - G4: has `^## Navigation` AND a `▶ NEXT` block.
    - G5: `wc -l` >= 70.
    - G6: no v1 frontmatter fields (`triggers:`/`works-best-with:`/`min-model:`/`tokens:`/bare `phase:`).
    - G7: literal `.claude/sovereign-engine/sovereign-tools.cjs` present AND no `$ENGINE`.
    - G8: invokes `anchor add|list|check` AND does not write `external-docs/*.md` directly.
    - G9: orients via `init anchor-docs` AND has the `@file:*` spill guard (one orient).
    - G10: surfaces a `copyright` warning AND documents `URL`-by-default.
    - G12: references `ADR-004`.
    All checkable by grep; all map to G1-G12 in 15-VALIDATION.md / 15-RESEARCH.md.
  </acceptance_criteria>
  <verify>
    <automated>F=engine/skills/anchor-docs/SKILL.md; test -f "$F" && grep -qx 'disable-model-invocation: true' "$F" && grep -q '^## Why this matters' "$F" && grep -q '^## Navigation' "$F" && grep -q '▶ NEXT' "$F" && [ "$(wc -l < "$F")" -ge 70 ] && ! grep -Eq '^(triggers|works-best-with|min-model|tokens|phase):' "$F" && grep -q '.claude/sovereign-engine/sovereign-tools.cjs' "$F" && ! grep -q '\$ENGINE' "$F" && grep -Eq 'anchor (add|list|check)' "$F" && ! grep -Eq 'external-docs/[^ ]*\.md' "$F" && grep -q 'init anchor-docs' "$F" && grep -q '@file:\*' "$F" && grep -iq 'copyright' "$F" && grep -iq 'url' "$F" && grep -q 'ADR-004' "$F" && echo G1-G12-PASS</automated>
  </verify>
  <done>SKILL.md exists, mirrors bridge shape, sets disable-model-invocation:true, orients with one `init anchor-docs` call, delegates to `anchor add|list|check` via the literal engine path, surfaces the copyright warning + URL-by-default and cites ADR-004, >=70 lines, no v1 fields, no markers. (G1-G12)</done>
</task>

<task type="auto">
  <name>Task 3: Run the phase gates (doctor / validate / regression) from engine/</name>
  <files></files>
  <read_first>
    - 15-VALIDATION.md "Per-Requirement Verification Map" (G1-G13) and "Validation Sign-Off".
    - 15-RESEARCH.md "Validation Architecture" — the `--cwd engine` caveat and live baseline (auto_count 5, total 16 -> 17).
    - <interfaces> GATE CAVEAT above — gates MUST run from engine/ or they pass vacuously on 0 skills.
  </read_first>
  <action>
    Run the phase gates. ALL skill/doctor gates MUST run from `engine/` (or with `--cwd engine`)
    or they walk 0 skills and pass vacuously (the critical research caveat).

    1. `validate skills` passes for anchor-docs (G14):
       `cd engine && node bin/sovereign-tools.cjs validate skills skills/anchor-docs/SKILL.md`
       -> expect `{"valid": true, ...}`, exit 0.

    2. Auto-trigger budget held at 5 (G13/G15):
       `cd engine && node bin/sovereign-tools.cjs doctor`
       -> expect `total_skills: 17, auto_count: 5, disabled_count: 12` (baseline 16/5/11 + this
       disabled skill). Gate on `auto_count == 5` EXACTLY — not just `doctor ok` (forgetting
       `disable-model-invocation` would bump it to 6 while doctor still reports ok; pitfall 5).

    3. Engine regression (no engine change this phase):
       `cd engine && node --test "test/**/*.test.cjs"`
       -> expect still 164 green (engine substrate untouched).

    If `validate skills` fails -> fix the frontmatter in Task 2's file (name lowercase-hyphen
    no claude/anthropic; description <=1024 chars). If `auto_count != 5` -> confirm
    `disable-model-invocation: true` is exactly present. If a test fails -> an engine file was
    touched (out of scope); revert it and flag the gap (do NOT patch the engine here).
  </action>
  <acceptance_criteria>
    - G14: `cd engine && node bin/sovereign-tools.cjs validate skills skills/anchor-docs/SKILL.md` -> `valid: true`, exit 0.
    - G13/G15: `cd engine && node bin/sovereign-tools.cjs doctor` -> `auto_count: 5` (exact), `total_skills: 17`, `disabled_count: 12`.
    - Regression: `cd engine && node --test "test/**/*.test.cjs"` -> still 164 green (no engine regression).
    - This task does NOT verify ANCHOR-02 end-to-end (that's Phase 16); anchor-docs only SURFACES staleness via `anchor check`.
  </acceptance_criteria>
  <verify>
    <automated>cd engine && node bin/sovereign-tools.cjs validate skills skills/anchor-docs/SKILL.md && node bin/sovereign-tools.cjs doctor | grep -Eq '"auto_count": ?5' && node bin/sovereign-tools.cjs doctor | grep -Eq '"total_skills": ?17' && node --test "test/**/*.test.cjs" 2>&1 | tail -20 | grep -Eq '# (pass|tests) ' && echo GATES-PASS</automated>
  </verify>
  <done>validate skills passes for anchor-docs; doctor holds auto_count 5 (total 17, disabled 12); engine suite still 164 green. (G13/G14/G15 + regression)</done>
</task>

</tasks>

<verification>
Run from repo root unless noted. Skill/doctor gates run from `engine/` (critical caveat).

Structural greps on engine/skills/anchor-docs/SKILL.md (G1-G12):
- exists; `disable-model-invocation: true`; `## Why this matters`; `## Navigation` + `▶ NEXT`;
  `wc -l` >= 70; no v1 frontmatter; literal `.claude/sovereign-engine/sovereign-tools.cjs` and no `$ENGINE`;
  `anchor add|list|check` present and no direct `external-docs/*.md` write; `init anchor-docs` + `@file:*`;
  `copyright` + `url`; `ADR-004`.

ADR (G11): `docs/adr/ADR-004-anchor-content-copyright.md` exists with `# ADR-004:` + `## Context/Decision/Consequences` + `**Status:** Accepted`.

Engine gates (G13/G14/G15 — run from engine/):
- `cd engine && node bin/sovereign-tools.cjs validate skills skills/anchor-docs/SKILL.md` -> valid: true
- `cd engine && node bin/sovereign-tools.cjs doctor` -> auto_count 5, total_skills 17, disabled_count 12
- `cd engine && node --test "test/**/*.test.cjs"` -> still 164 green
</verification>

<success_criteria>
- SC1 (ANCHOR-01): anchor-docs ingests external docs by delegating to engine `anchor add`
  (URL-by-default, content opt-in behind a copyright warning citing ADR-004); ADR-004 exists
  in repo ADR format; storage NOT reimplemented. (G8, G10, G11, G12)
- SC2 (M4-CC): orients with one `init anchor-docs` call; core-tier shape — `## Why this
  matters`, recommendation-first flow, navigation footer; >=70 lines; literal engine path;
  no v1 frontmatter. (G3, G4, G5, G6, G7, G9)
- SC3 (M4-CC): `disable-model-invocation: true` set; doctor holds auto_count 5; validate
  skills passes. (G2, G13, G14, G15)
- Engine suite still 164 green (no engine change this phase).
- NOT in scope: ANCHOR-02 end-to-end (Phase 16); verify-self; any engine change; auto-fetch.
</success_criteria>

<output>
After completion, create `.planning/phases/15-anchor-docs-skill/15-01-SUMMARY.md`.
</output>
